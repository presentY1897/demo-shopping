# 저장소 소유자가 직접 해야 하는 일

전체 마일스톤(M01~M15)에서 **사용자가 직접 해야 하는 작업**만 모았다. 나머지는 전부 코드·문서 작업이라 내가 처리한다.

각 항목은 **필요해지는 시점**에 배치했다. 미리 다 할 필요는 없고, 앞선 마일스톤을 진행하는 동안 다음 것을 준비하면 된다.

---

## A. 지금 당장 할 수 있는 것

**의존성이 없다. 오늘 다 끝낼 수 있고, 끝내면 M08 까지 사용자 작업이 사실상 없어진다.**

| # | 서비스 | 할 일 | 결과물 |
| --- | --- | --- | --- |
| 1 | **Neon** | 가입 → 프로젝트 생성 | 연결 문자열 ✅ *완료* |
| 2 | **Render** | 가입 → Blueprint 배포 | ✅ *완료 (2026-09-03)* — `shopping-api` · `shopping-search` 기동. `/api/v1/health` → `{"status":"ok","database":"ok","search":"ok"}`. 검색은 키 없이 401, CORS 는 프로덕션 오리진만 허용 |
| 3 | **Vercel** | 가입 → GitHub 연동 → 같은 저장소 접근 허용 | |
| 4 | **Cloudflare R2** | 대시보드에서 R2 활성화 → API 토큰 발급 | 계정 ID · 액세스 키 · 시크릿 |
| 5 | **Sentry** | 가입 → 프로젝트 2개(Node / Next.js) | DSN 2개 |
| 6 | **Discord** | 채널에 webhook 생성 | webhook URL |
| 7 | **Google Cloud** | 프로젝트 생성 → OAuth 동의 화면 → 클라이언트 ID 발급 | 클라이언트 ID · 시크릿 |
| 8 | **토스페이먼츠** | 개발자센터 가입 | 테스트 클라이언트 키 · 시크릿 키 |
| 9 | (선택) **Unsplash** | API 키 발급 | 스톡 수집 스크립트에 사용 |
| 10 | (선택) **AI 이미지 도구** | 도구 선택 + 계정 준비 | 실제 생성은 M05 |

### 7번 — Google OAuth 리다이렉트 URI

도메인과 포트가 확정됐으므로 **지금 전부 등록할 수 있다.**

```
http://localhost:4000/api/v1/auth/google/callback
https://api.demo-shopping.com/api/v1/auth/google/callback
```

**동의 화면이 "테스트" 모드면 등록한 테스트 사용자만 로그인된다**(100명 제한). 채용 담당자가 막힌다. `email` · `profile` 스코프만 쓰므로 심사 없이 게시할 수 있을 것으로 보이나 M04 착수 시 확인한다.

Vercel 프리뷰 URL 은 매번 달라지는데 Google 은 와일드카드 리다이렉트를 허용하지 않는다. 프리뷰 환경의 OAuth 는 M04 에서 별도로 다룬다.

### 8번 — 토스페이먼츠

실 계약이 아니라 **테스트 키**만 쓴다. 가입에 사업자 정보가 필요한지는 확인이 필요하다. 필요하다면 **가상 카드 결제만으로 전체 흐름이 완결**되므로(D-031) 토스 연동만 보류하고 진행할 수 있다.

---

## B. 진행이 되어야 할 수 있는 것

앞 작업의 산출물이 있어야 의미가 있다. **미리 하면 헛수고가 된다.**

| 할 일 | 선행 조건 | 시점 |
| --- | --- | --- |
| ~~**Render 서비스 생성**~~ | **✅ 완료 (2026-09-03)** — 아래 B-1 은 기록으로 남긴다 | M02 / TASK-0009 |
| **Vercel 프로젝트 3개 생성 · 도메인 연결** | `apps/shop` `apps/seller` `apps/admin` 이 있어야 빌드된다 | M02 / TASK-0010 |
| **DNS 레코드 4개 생성** | 연결 대상(Vercel · Render URL)이 먼저 생겨야 한다 | M02 / TASK-0008 |
| **Storybook 배포 대상 추가** | 컴포넌트가 있어야 한다 | M03 / TASK-0104 |
| **디자인 방향 피드백** | 토큰·컴포넌트 결과물을 봐야 한다 | M03 |
| **AI 이미지 생성 (80~100장)** | 쇼케이스 상품 20개의 이름·카테고리·색상 옵션이 정해져야 한다. **프롬프트 목록을 내가 만들어 드린다** | M05 / TASK-0037 |
| **스톡 사진 수집 (200~300장)** | 카테고리 40개가 확정돼야 한다 | M05 / TASK-0037 |
| **README 자기소개 · 연락처** | 프로젝트 설명은 내가 쓴다 | M15 / TASK-0100 |
| **이력서 · 포트폴리오에 링크 배포** | 배포가 끝나야 한다 | M15 |

`DNS 레코드` `Vercel 프로젝트` 는 **토큰을 주시면 내가 대신 처리**한다.

### B-1. Render 서비스 생성 (TASK-0009) — ✅ **완료 (2026-09-03)**

> 아래 절차는 재생성·복구용 기록이다. 실제 서비스 URL 은 `shopping-api-96sy.onrender.com` · `shopping-search-frrb.onrender.com`.

`render.yaml` 이 `main` 에 머지되면 바로 진행할 수 있다. 대시보드에서 서비스를 손으로
만들지 마시고 **반드시 Blueprint 로** 만들어 주세요. 손으로 만든 서비스에 나중에
Blueprint 를 붙이면 이름이 충돌하거나 수동 설정이 조용히 덮입니다.

**준비물** — 먼저 터미널에서 키를 하나 만들어 메모장에 둡니다. 두 번 입력합니다.

```bash
openssl rand -base64 32
```

그리고 Neon 콘솔에서 연결 문자열을 복사합니다 (`?sslmode=require` 가 붙어 있는지 확인).

**클릭 순서**

1. https://dashboard.render.com → 우측 상단 **New** → **Blueprint**
2. 저장소 목록에서 `presentY1897/demo-shopping` 옆의 **Connect**
3. Blueprint 이름은 아무거나(`demo-shopping`), **Branch: `main`**, Blueprint Path 는 비워 둠
4. Render 가 `render.yaml` 을 읽고 만들 서비스 2개(`shopping-api`, `shopping-search`)와
   **입력이 필요한 값 4칸**을 보여 줍니다. 이렇게 채웁니다.

   | 서비스 | 변수 | 넣을 값 |
   | --- | --- | --- |
   | `shopping-api` | `DATABASE_URL` | Neon 연결 문자열 |
   | `shopping-api` | `MEILI_MASTER_KEY` | 위에서 만든 키 |
   | `shopping-api` | `MEILI_HOST` | `https://REPLACE-ME.onrender.com` (6번에서 고칩니다) |
   | `shopping-search` | `MEILI_MASTER_KEY` | **위와 똑같은 키** |

5. **Deploy Blueprint** 클릭. 2~5분 기다립니다.
6. 왼쪽 메뉴 → `shopping-search` 서비스 → 상단에 보이는 URL
   (`https://shopping-search-xxxx.onrender.com`) 을 복사합니다.
7. `shopping-api` 서비스 → **Environment** → `MEILI_HOST` 를 6번에서 복사한 URL 로
   바꾸고 **Save**. API 가 자동으로 다시 배포됩니다.
8. 확인 — `shopping-api` 의 URL 뒤에 `/api/v1/health` 를 붙여 브라우저로 엽니다.

   ```
   {"status":"ok","database":"ok","search":"ok","uptime":3,"version":"0.0.0"}
   ```

   `search` 가 `"down"` 이면 대개 둘 중 하나입니다 — `MEILI_HOST` 오타, 또는 두
   서비스의 `MEILI_MASTER_KEY` 가 다릅니다. `database` 가 `"down"` 이면 `DATABASE_URL`
   입니다.

9. **서비스 URL 2개를 알려 주세요.** 값이 아니라 주소만 있으면 됩니다.
   `api.demo-shopping.com` 연결(DNS)은 TASK-0008 에서 이어집니다.

**주의 한 가지.** 무료 인스턴스 시간은 워크스페이스당 **월 750시간**이고 두 서비스가
나눠 씁니다. 15분 놀면 자동으로 잠들고 잠든 동안은 시간이 깎이지 않으니 평소에는
문제가 없지만, "항상 깨워 두기" 같은 설정은 하지 마세요 (2개 × 730시간 = 1460시간).

**검색 엔진이 공개 주소를 갖습니다.** 무료 플랜에는 비공개 서비스가 없어서 어쩔 수
없습니다. 마스터 키 없이는 모든 데이터 경로가 401 이고, 색인에는 공개 상품 정보만
들어갑니다. 유료로 올리면 비공개로 바꿀 수 있습니다.

---

## C. 진행에 따라 계속 필요한 것

| | 내용 | 횟수 |
| --- | --- | --- |
| **TASK 승인** | 마일스톤 착수 전 해당 TASK 문서 검토·승인 | 15회 |
| **설계 변경 승인** | 구현 중 문제가 나오면 작업 중단 → 문서 수정 → 승인 → 재개 (D-037) | 수시 |
| ~~**`main` 브랜치 보호 규칙 결정**~~ | **완료 (2026-09-03)** — 1~8번 전부 적용. [`branch-protection.md`](./branch-protection.md) | – |

---

## 요약

```
A. 지금        계정 9~10개 가입·키 발급 — 오늘 다 가능
B. 다음        Vercel 프로젝트 3개(M02) · 디자인 피드백(M03) · 이미지(M05) · README(M15)
B. 나중        나머지 배포 설정(M02) · 디자인 피드백(M03) · 이미지(M05) · README(M15)
C. 상시        TASK 승인 15회 + 설계 변경 승인
```

**A 를 오늘 끝내면 M08(결제)까지 추가로 준비할 것이 없다.** B 는 전부 해당 마일스톤에 도달했을 때 알려드린다.

## 비용

| 항목 | 비용 |
| --- | --- |
| 도메인 `demo-shopping.com` | 구매 완료 |
| Vercel · Render · Neon · R2 · Sentry · Discord | **0원** |
| AI 이미지 생성 | 도구에 따라 다름 (M05) |
| (선택) Railway 이전 | 월 $5~ |

## 내가 대신 할 수 있는 것

토큰·키를 주시면 클릭 작업을 줄일 수 있다.

| 주시면 | 제가 처리 |
| --- | --- |
| Cloudflare API 토큰 | DNS 레코드 생성·수정 |
| Vercel 토큰 | 프로젝트 생성·환경변수 등록·도메인 연결 |
| Neon API 키 | 프로젝트·브랜치 생성 |
| Unsplash API 키 | 스톡 이미지 수집 스크립트 |
| (이미 있음) `gh` | GitHub Secrets, 브랜치 보호 규칙, 저장소 메타 |

**반드시 직접 하셔야 하는 것**은 가입·약관 동의·결제수단 등록·Google OAuth 동의 화면 구성이다.

## 키 전달 방법

**키·토큰을 채팅에 붙여넣지 않는다.** `.gitignore` 와 커밋 훅으로 저장소 유입은 막지만, 채팅 기록에 남는 것은 막을 수 없다.

| 용도 | 두는 곳 |
| --- | --- |
| 로컬 개발용 | 워크트리의 `.env.local` (M01 이후 생성, gitignore 됨) |
| CI 용 | GitHub Secrets — 직접 등록 후 **이름만** 알려주기 |
| 배포 환경용 | Render / Vercel 대시보드의 환경변수 — 직접 등록 |

내가 필요한 것은 **키 자체가 아니라 "어떤 이름으로 등록했는지"** 뿐이다. 코드는 `process.env.DATABASE_URL` 을 참조할 뿐 값을 알 필요가 없다.

노출된 키는 즉시 재발급한다. 데이터가 없는 초기일수록 비용이 0에 가깝다.
