# TASK-0036: 재고 원장

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M05 카탈로그 |
| 상태 | 승인 대기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/stock-ledger` |
| 선행 작업 | TASK-0032 |

## 1. 목적

재고 변동을 전부 원장에 기록한다. "재고가 왜 줄었나"에 답할 수 있고, 이후 주문·취소·반품이 이 원장을 통해 재고를 움직이게 한다.

## 2. 범위

### 포함
- `StockLedger` — variant, 유형, 수량(±), 참조(주문 등), 사유, 처리자, `balanceAfter`
- 유형: INBOUND / SALE / CANCEL / RETURN_IN / ADJUST / RESERVE_CONFIRM
- 재고 변경 서비스 — **모든 재고 변경은 이 서비스만 통과**
- `ProductVariant.stock` 갱신과 원장 기록을 한 트랜잭션으로
- 원장 조회 API + 판매자 화면(Variant 별 이력)
- 재고 정합성 점검 (원장 합계 = 현재 재고)

### 제외
- 예약(Reservation) — M07
- 주문 연동 — M07 이후

## 3. 요구사항

- [ ] 모든 재고 변경이 원장에 남는다
- [ ] 원장 합계와 현재 재고가 항상 일치한다
- [ ] 재고를 음수로 만들 수 없다
- [ ] 동시 변경에도 정합성이 깨지지 않는다
- [ ] 판매자가 Variant 별 재고 이력을 볼 수 있다

## 4. 설계

```
adjustStock({ variantId, type, quantity, refType, refId, reason, actorId })
  트랜잭션:
    ① SELECT ... FOR UPDATE (또는 조건부 UPDATE)
    ② 결과 재고 < 0 이면 실패
    ③ variant.stock 갱신
    ④ StockLedger 기록 (balanceAfter 포함)
```

`balanceAfter` 를 저장하는 이유: 원장을 처음부터 합산하지 않고도 특정 시점 재고를 알 수 있고, 정합성 점검이 쉬워진다.

### 여러 계정이 동시에 재고를 바꾸면

판매자가 입고 +5 를 처리하는 동시에 구매자 셋이 각각 1개씩 사는 상황이다. 카테고리(TASK-0029)와 달리 **낙관적 잠금을 쓰지 않는다.**

| | 카테고리 수정 | 재고 변경 |
| --- | --- | --- |
| 충돌 시 | 사용자에게 알리고 선택하게 함 | 알릴 대상이 없다 (구매자는 재고 숫자를 모른다) |
| 방식 | 낙관적 잠금(`version`) | **행 잠금으로 직렬화** |

```sql
BEGIN;
  SELECT stock FROM product_variant WHERE id = ? FOR UPDATE;  ← 여기서 순번이 정해진다
  -- 결과 재고 < 0 이면 ROLLBACK
  UPDATE product_variant SET stock = stock + ?;
  INSERT INTO stock_ledger (...);
COMMIT;
```

- 동시에 10건이 들어와도 **한 줄로 세워져 순서대로 처리**된다. 각자 자기 차례에 현재 재고를 다시 읽으므로 덮어쓰기가 없다.
- 재고가 부족한 요청만 실패하고 나머지는 성공한다. "누가 먼저 눌렀나"가 정직하게 결정된다.
- 원장은 append-only 라 충돌 자체가 없다. 잠그는 것은 `ProductVariant` 한 행뿐이다.
- 잠금 구간은 수 ms 다. 상품이 다르면 서로 다른 행이라 아예 경합하지 않는다.

**절대값이 아니라 조정량으로 받는 이유**도 같다(TASK-0115). "재고를 17로 설정"은 그 사이 팔린 수량을 덮어쓴다. "+5 입고"는 언제 처리되든 결과가 맞는다.

## 5. 구현 계획

1. 스키마·인덱스·마이그레이션
2. 재고 변경 서비스 (트랜잭션·동시성)
3. 음수 방지 검증
4. 원장 조회 API
5. 판매자 이력 화면
6. 정합성 점검 스크립트
7. 동시성 테스트

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 기록 | 입고 처리 | 원장 행 생성, `balanceAfter` 정확 | [ ] |
| F2 | 정합성 | 임의 변경 20회 후 점검 | 원장 합계 = 현재 재고 | [ ] |
| F3 | 음수 방지 | 재고보다 큰 차감 시도 | 실패, 재고 무변화 | [ ] |
| F4 | 동시성 | 재고 1개에 동시 차감 10회 | 1건만 성공, 9건 실패 | [ ] |
| F4b | 혼합 동시성 | 입고 +5 와 차감 ×3 동시 실행 | 최종 재고 = 초기 +5 −3, 원장 4건 | [ ] |
| F4c | 다른 상품 | 서로 다른 Variant 동시 변경 | 경합 없이 병렬 처리 | [ ] |
| F5 | 이력 조회 | 판매자 화면 | 유형·수량·사유·시각 표시 | [ ] |
| F6 | 트랜잭션 | 원장 기록 중 예외 발생 | 재고도 롤백 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5 적용** — 재고 서비스는 **분기 커버리지 100%**. 동시성 테스트 포함
- **2장**: P1~P5. **P6 해당 없음**
- **3~4장 전 항목 적용**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 원장 유형 목록을 `docs/design/erd.md` 3장에 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 서비스를 우회한 직접 UPDATE | 리포지토리에서 `stock` 직접 수정 금지. 코드 리뷰 체크 항목으로 고정 |
| R2 | 원장이 커져 조회가 느려짐 | variant + 생성일 인덱스, 조회는 기간 제한 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
| 2026-09-03 | D-208 레이어 분할에 따라 참조 갱신 — 재고 조정 API 는 TASK-0035 → **TASK-0115** |
