# TASK-0034: 판매자 상품 등록 · 수정

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M05 카탈로그 |
| 상태 | 폐기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/product-editor` |
| 선행 작업 | TASK-0032, TASK-0033 |

> ## 폐기 — 레이어별로 분할됨 (2026-09-03)
>
> D-208(TASK 분할 축은 레이어)에 따라 이 TASK 를 **둘로 나눴다.** 이 문서는 이력으로 남기며 여기서 새로
> 작업하지 않는다. 번호 `0034` 는 **결번으로 두고 재사용하지 않는다**.
>
> | 새 번호 | 제목 | 레이어 |
> | --- | --- | --- |
> | [TASK-0113](./TASK-0113-product-write-api.md) | 상품 등록 · 수정 API | 백엔드 (`apps/api`) |
> | [TASK-0114](./TASK-0114-product-editor-page.md) | 판매자 상품 편집기 화면 | 프론트 (`apps/seller`) |
>
> **왜 갈랐는가 — 그리고 여기서 구멍이 하나 드러났다.** 이 TASK 는 범위가 **전부 UI** 인데, 그 UI 가 부를
> **상품 생성 · 수정 API 가 어느 TASK 에도 없었다.** TASK-0032 는 스키마와 엔티티 단위 CRUD 골격까지이고,
> 편집기가 "저장" 한 번으로 기본 정보 · 속성값 · 이미지 · 옵션 · Variant 를 **한 트랜잭션에 넣는 계약**은
> 비어 있었다. **TASK-0113 이 그 구멍을 메운다.**
>
> **절단면**은 `packages/shared` 의 zod 스키마다 — `productWriteRequestSchema` ·
> `productDetailResponseSchema` · `productVariantInputSchema` 를 TASK-0113 이 정의하고, TASK-0114 가
> **같은 스키마로 모킹 데이터를 만든다.** 조합 수 상한(100)도 스키마 한 곳에만 둔다.
>
> ### 항목 이동표
>
> | 이 문서의 항목 | 옮겨간 곳 |
> | --- | --- |
> | 기본 정보 (이름 · 설명 · 카테고리) | 화면 **0114** / 저장 **0113** |
> | 카테고리 선택 시 속성 폼 자동 생성 | **0114** (정의 조회는 TASK-0030, 저장 전 검증은 **0113**) |
> | 이미지 업로드 (TASK-0033 위젯) | 위젯 연결 **0114** / URL · 순서 저장 **0113** |
> | 옵션 정의 UI | **0114** / 저장·차분 **0113** |
> | 옵션 조합 → Variant 표 자동 생성 (가격 · 재고 · SKU · 최대 구매 수량) | 표 UI · 일괄 입력 **0114** / 조합 상한 검증 · 저장 **0113** |
> | 임시저장(DRAFT) / 판매중(ACTIVE) 전환 | **0113** (publish · unpublish) / **0114** |
> | 수정 시 기존 값 로드, 옵션 변경 처리 | 상세 조회 · 차분 **0113** / 차분 미리 표시 **0114** |
> | 미리보기 (구매자 화면 기준) | **0114** F8 (밀도 STANDARD 고정) |
> | 설계 — 카테고리 변경 시 동일 key 유지 | **0114** (화면 전용 규칙) |
> | 설계 — Variant 표 일괄 입력 우선 | **0114** |
> | F1 속성 폼 / F2 카테고리 변경 / F3 Variant 12행 / F4 일괄 입력 / F5 임시저장 / F6 필수 검증 / F7 수정 / F8 미리보기 | **0114** F1~F8 (서버 쪽 대응은 **0113** F1 · F3 · F4 · F5 · F10) |
> | F4b 1회 최대 구매 수량 | **0114** F4b(입력) · **0113** F9(저장 · 응답). **구매자 화면에서의 강제**는 TASK-0043 · M07 로 이관 |
> | R1 조합 수 상한 100 | **0113** R2(서버 400) · **0114** R1(사전 안내) |
> | R2 주문에 사용된 Variant 는 비활성화만 | **0113** R3 · F5b |
> | 제외 항목 — 검색 노출(M06) | **0113** · **0114** 2장 제외에 유지 |
>

## 1. 목적

판매자가 상품을 등록하는 화면을 만든다. **카테고리를 고르면 속성 입력 폼이 자동 생성**되는 것이 이 화면의 핵심이다.

## 2. 범위

### 포함
- 기본 정보 (이름, 설명, 카테고리)
- **카테고리 선택 시 속성 폼 자동 생성** (TASK-0017 동적 폼 + TASK-0030 정의)
- 이미지 업로드 (TASK-0033 위젯)
- 옵션 정의 UI (옵션명 + 값 추가)
- **옵션 조합 → Variant 표 자동 생성** (가격·재고·SKU·**1회 최대 구매 수량** 일괄/개별 입력)
- 임시저장(DRAFT) / 판매중(ACTIVE) 전환
- 수정 시 기존 값 로드, 옵션 변경 처리
- 미리보기 (구매자 화면 기준)

### 제외
- 상품 목록·재고 관리 (TASK-0035)
- 검색 노출 (M06)

## 3. 요구사항

- [ ] 카테고리를 바꾸면 속성 폼이 즉시 바뀐다
- [ ] 옵션 3 × 4 를 넣으면 Variant 12행 표가 생성된다
- [ ] Variant 가격·재고를 일괄 입력할 수 있다
- [ ] 임시저장 후 이어서 작성할 수 있다
- [ ] 필수 속성이 비면 저장이 막히고 해당 필드에 오류가 표시된다

## 4. 설계

```
① 기본 정보 → ② 카테고리 선택 → ③ 속성 폼 (자동 생성) → ④ 이미지
                                   ↓
④ 옵션 정의 → ⑤ Variant 표 (조합 자동 생성) → ⑥ 가격·재고 입력 → 저장
```

카테고리 변경 시 이미 입력한 속성값은 **키가 같으면 유지**하고 나머지는 버린다. 전부 날리면 카테고리를 잘못 골랐다가 되돌릴 때 다시 입력해야 한다.

Variant 표는 행이 많아질 수 있으므로 일괄 입력(전체 가격 동일 적용)을 먼저 제공한다.

## 5. 구현 계획

1. 폼 골격 + 기본 정보
2. 카테고리 선택 → 속성 폼 연동
3. 이미지 위젯 연결
4. 옵션 정의 UI
5. Variant 표 생성·일괄 입력
6. 임시저장·발행
7. 수정 모드 (옵션 변경 처리)
8. 미리보기

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 속성 폼 자동 생성 | 카테고리 선택 | 정의된 속성만큼 필드 생성, 타입 반영 | [ ] |
| F2 | 카테고리 변경 | 다른 카테고리로 변경 | 폼 교체, 동일 key 값 유지 | [ ] |
| F3 | Variant 생성 | 옵션 3 × 4 입력 | 12행 표 생성 | [ ] |
| F4 | 일괄 입력 | 전체 가격 일괄 적용 | 12행 반영 | [ ] |
| F4b | 구매 수량 제한 | 최대 수량 2로 설정 | 저장 후 구매자 화면에서 2개까지만 선택 가능 | [ ] |
| F5 | 임시저장 | 저장 후 재진입 | 입력값 복원 | [ ] |
| F6 | 필수 검증 | 필수 속성 비우고 저장 | 해당 필드에 오류 표시 | [ ] |
| F7 | 수정 | 기존 상품 옵션에 값 추가 | Variant 추가, 기존 재고 유지 | [ ] |
| F8 | 미리보기 | 미리보기 실행 | 구매자 화면과 동일 레이아웃 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **2장**: P1~P5 적용. **P6 해당 없음** — 판매자 콘솔
- **3장 전 항목 적용**
- **4장 해당 없음**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 등록 흐름을 `docs/design/pages.md` 판매자 절에 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 옵션 값이 많으면 Variant 표가 수백 행 | 조합 수 상한(예: 100)을 두고 초과 시 안내 |
| R2 | 수정 시 옵션 삭제로 주문 이력 손상 | 주문에 사용된 Variant 는 비활성화만 허용 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
| 2026-09-03 | D-208 에 따라 레이어별로 분할. TASK-0113 · TASK-0114 로 대체. 원본에 없던 백엔드(상품 쓰기 API)는 TASK-0113 이 채움 |
