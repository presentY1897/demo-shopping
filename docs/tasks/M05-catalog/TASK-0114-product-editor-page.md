# TASK-0114: 판매자 상품 편집기 화면

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M05 카탈로그 |
| 상태 | 승인됨 |
| 작성일 | 2026-09-03 |
| 브랜치 | `feature/product-editor-page` |
| 선행 작업 | TASK-0113, TASK-0033, TASK-0017, TASK-0019, TASK-0023, TASK-0107 |

> **분할 유래** — TASK-0034(판매자 상품 등록 · 수정)의 화면 절반이다. D-208 에 따라 화면과 API 를 나눴고,
> API 는 TASK-0113 이 맡는다. 원본은 범위가 전부 UI 였는데 그 UI 가 부를 API 가 어디에도 없었다.

## 1. 목적

`apps/seller` 의 상품 등록 · 수정 화면을 만든다. **카테고리를 고르면 속성 입력 폼이 자동 생성**되고,
**옵션 조합에서 Variant 표가 자동 생성**되는 것이 이 화면의 핵심이다.

## 2. 범위

### 포함
- `/products/new` · `/products/[id]/edit` — 등록 · 수정
- 기본 정보 (이름 · 설명 · 카테고리)
- **카테고리 선택 시 속성 폼 자동 생성** — TASK-0030 의 정의 조회 + TASK-0017 의 동적 폼 생성기
- **카테고리 변경 시 같은 key 의 입력값 유지**, 나머지는 버림
- 이미지 업로드 위젯 연결 (TASK-0033) — 순서 변경 · 대표 지정
- **옵션 정의 UI** — 옵션명 추가, 값 추가·삭제·정렬
- **옵션 조합 → Variant 표 자동 생성** — 가격 · 재고 · SKU · **1회 최대 구매 수량**의 일괄 입력과 개별 입력
- 조합 수 상한(스키마의 값) 초과 시 안내
- 임시저장(`DRAFT`) / 발행(`ACTIVE`) 전환
- 수정 모드 — 기존 값 로드, 옵션 변경 시 어떤 조합이 추가·비활성화되는지 **저장 전 미리 표시**
- **미리보기** — 구매자 상세 화면 레이아웃으로 렌더
- `productWriteRequestSchema` · `productDetailResponseSchema` 로 **모킹 데이터를 만들고 `parse` 로 검증**
- **카탈로그 도메인 오류 코드를 화면에 그린다** ([TASK-0032](./TASK-0032-product-schema.md) 4.10 ·
  [TASK-0036](./TASK-0036-stock-ledger.md) 4.10 이 TASK-0113 과 함께 넘긴 것) — 코드 정의와
  `apps/seller` 카탈로그 문장은 [TASK-0113](./TASK-0113-product-write-api.md) 이 만든다. 이 TASK 는
  그 코드별로 **어디에 무엇을 띄울지**를 정한다(필드 옆 · 표 위 · 토스트)

### 제외 (이번에 하지 않는 것)
- **상품 저장 · 조회 API** → TASK-0113
- **속성 정의 관리 화면** → TASK-0031
- **이미지 업로드 위젯 자체** → TASK-0033. 여기서는 배치하고 결과 URL 을 폼에 연결한다
- **상품 목록 · 재고 조정 화면** → TASK-0116
- 검색 노출 → M06
- 실제 API 호출 테스트 — 백엔드는 모킹한다

## 3. 요구사항

### 기능 요구사항
- [ ] 카테고리를 바꾸면 속성 폼이 즉시 바뀐다
- [ ] 카테고리를 되돌리면 같은 key 의 입력값이 남아 있다
- [ ] 옵션 3 × 4 를 넣으면 Variant 12행 표가 생성된다
- [ ] Variant 가격 · 재고 · 최대 구매 수량을 일괄 입력할 수 있다
- [ ] 임시저장 후 다시 들어오면 입력값이 복원된다
- [ ] 필수 속성이 비면 발행이 막히고 **해당 필드에** 오류가 표시된다
- [ ] 수정 시 옵션을 늘리면 어떤 조합이 추가되는지 저장 전에 보인다
- [ ] 미리보기가 구매자 상세와 같은 레이아웃으로 보인다

### 비기능 요구사항
- Variant 표가 100행일 때도 입력이 버벅이지 않는다 (행 단위 렌더 분리)
- 콘솔 화면 — 밀도 토글 없음(2단계 고정), 데스크톱 우선

## 4. 설계

### 흐름

```
① 기본 정보 → ② 카테고리 선택 → ③ 속성 폼 (자동 생성) → ④ 이미지
                                   ↓
④ 옵션 정의 → ⑤ Variant 표 (조합 자동 생성) → ⑥ 가격·재고 입력 → 저장
```

카테고리 변경 시 이미 입력한 속성값은 **키가 같으면 유지**하고 나머지는 버린다. 전부 날리면 카테고리를
잘못 골랐다가 되돌릴 때 다시 입력해야 한다.

Variant 표는 행이 많아질 수 있으므로 **일괄 입력(전체 가격 동일 적용)을 먼저** 제공한다.

### 화면 / 컴포넌트

```
apps/seller/app/products/new/page.tsx
apps/seller/app/products/[id]/edit/page.tsx
        ├ ProductBasicFields      이름 · 설명 · 카테고리
        ├ AttributeFields         카테고리 정의로부터 생성 (TASK-0017 동적 폼)
        ├ ProductImageField       TASK-0033 위젯 래핑
        ├ OptionEditor            옵션명 · 값
        ├ VariantTable            조합 표 + 일괄 입력
        └ ProductPreview          구매자 상세 레이아웃
```

### 미리보기의 밀도

미리보기는 구매자 화면을 흉내내지만 **밀도 `STANDARD` 고정**으로 렌더한다. 콘솔에는 밀도 토글이 없고
(TASK-0019), 판매자가 미리보기 안에서 밀도를 바꾸는 것은 이 화면의 목적이 아니다. 밀도 3단계 검증은
구매자 상품 상세(TASK-0043)가 받는다. 그래서 **P6 · U4 는 해당 없음**이다.

### 절단면 — `packages/shared` 의 zod 스키마

TASK-0113 이 정의한 스키마를 그대로 쓰고, **모킹 응답도 같은 스키마로 만든다.**

| 스키마 | 이 화면에서의 용도 |
| --- | --- |
| `productWriteRequestSchema` | 저장 본문의 클라이언트 검증. **조합 수 상한 100 도 여기서 읽는다** |
| `productOptionInputSchema` · `productVariantInputSchema` | 옵션 에디터 · Variant 표의 행 타입 |
| `productDetailResponseSchema` | 수정 모드 로드 · 미리보기. **모킹 응답 `parse`** |
| `productStatusSchema` | `DRAFT` / `ACTIVE` 배지와 버튼 문구의 값 출처 |
| `productAttributeValuesSchema` | 속성값 컨테이너. 키별 타입은 TASK-0030 의 **동적 zod 스키마**가 판정 |

속성 폼은 정의(`AttributeDefinition`)에서 zod 스키마를 만들어 검증한다 — 서버가 저장 직전에 쓰는 것과
**같은 생성기**(TASK-0030)를 쓴다. 검증 규칙을 화면에 따로 적으면 서버와 어긋난다.

### 역할별 권한

`apps/seller` 진입은 TASK-0023 의 미들웨어가 판정한다. 스토어가 `ACTIVE` 가 아니면 이 화면 대신 안내
화면이 뜬다(TASK-0109). 저장 버튼은 `can('product.write')` 로 비활성 처리하되, 실제 차단은 서버의
403 이다.

## 5. 구현 계획

1. 폼 골격 + 기본 정보 (TASK-0017 어댑터)
2. 카테고리 선택 → 속성 폼 연동 (동일 key 유지 규칙)
3. 이미지 위젯 연결 (순서 · 대표)
4. 옵션 정의 UI
5. Variant 표 생성 · 일괄 입력 · 상한 안내
6. 임시저장 · 발행 전환
7. 수정 모드 (차분 미리 표시)
8. 미리보기
9. 모킹 `parse` 테스트, 키보드 순회, Lighthouse 측정

## 6. 완료 기준 (Definition of Done)

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 속성 폼 자동 생성 | 정의 5개짜리 카테고리 선택 (모킹) | 5개 필드 생성, 타입별 컨트롤 반영 | [ ] |
| F2 | 카테고리 변경 | 다른 카테고리로 변경 후 되돌림 | 폼 교체, 동일 key 값 유지 | [ ] |
| F3 | Variant 생성 | 옵션 3 × 4 입력 | 12행 표 생성 | [ ] |
| F4 | 일괄 입력 | 전체 가격 일괄 적용 | 12행 전부 반영 | [ ] |
| F4b | 구매 수량 제한 입력 | 최대 수량 2를 일괄 적용 | 저장 요청 본문의 각 Variant 에 `maxPurchaseQuantity: 2` | [ ] |
| F5 | 임시저장 | `DRAFT` 저장 후 수정 모드 재진입 (모킹 응답) | 입력값 복원 | [ ] |
| F6 | 필수 검증 | 필수 속성 비우고 발행 | 제출 차단 + **해당 필드에** 오류 표시 | [ ] |
| F7 | 수정 차분 표시 | 사이즈에 `XL` 추가 | "추가될 조합 3건" 안내, 저장 후 기존 재고 값 유지(모킹 응답 기준) | [ ] |
| F8 | 미리보기 | 미리보기 실행 | 구매자 상세와 동일 레이아웃(밀도 STANDARD) | [ ] |
| F9 | 조합 상한 | 조합 101개가 되는 옵션 입력 | 저장 전 안내, 제출 버튼 비활성 | [ ] |
| F10 | 대용량 표 | Variant 100행에서 셀 입력 | 입력 지연 체감 없음(프로파일러 상 프레임 드롭 없음) | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 이 TASK 는 **화면만 만드는 프론트 TASK** 이므로
1장 · 2장 · 5장(C1·C2) · 7장을 받는다.

| 장 | 적용 | 비고 |
| --- | --- | --- |
| 1장 코드 게이트 | Q1~Q4 · Q6 · Q7 전부 | |
| Q5 테스트 충실도 | **커버리지 수치 없음** → 아래 U 목록. 단 **조합 전개 · key 유지 규칙이 화면 쪽 순수 로직으로 남으면 그 부분은 분기 100%** | 대역: **백엔드 모킹.** 업로드 위젯도 스텁 |
| 2장 화면 게이트 | P1~P5 적용. **P6 해당 없음** — 콘솔은 밀도 2단계 고정, 미리보기는 STANDARD 고정 | |
| **3장 API 게이트** | **해당 없음** | 엔드포인트를 추가하지 않는다. API 는 TASK-0113 |
| **4장 데이터 게이트** | **해당 없음** | `schema.prisma` 무변경 |
| 5장 계약 게이트 | **C1 · C2** | C3 는 TASK-0113 |
| 7장 문서 게이트 | D1~D5 | |

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| P1 | LCP | Lighthouse 모바일 (`/products/new`) | 2.5s 이하 | [ ] |
| P2 | 접근성 | Lighthouse Accessibility | 90점 이상 | [ ] |
| P3 | 반응형 | 360 / 768 / 1440px | Variant 표는 가로 스크롤, 레이아웃 깨짐 0건 | [ ] |
| P4 | 키보드 조작 | Tab 순회 | 옵션 추가 · 표 셀 · 저장까지 도달·조작 가능 | [ ] |
| P5 | 상태 표현 | 로딩 / 빈(옵션 없음) / 에러 | 세 상태 모두 구현 | [ ] |
| U1 | 조건부 렌더 | 로딩 · 빈 · 에러 · 정상 | 4상태 각각 테스트 | [ ] |
| U2 | 폼 검증 오류 | F6 | 해당 필드에 오류 표시 | [ ] |
| U3 | 중복 클릭 차단 | 저장 연속 2회 클릭 | 요청 1회 | [ ] |
| U5 | 키보드 조작 | 마우스 없이 상품 1건 등록 | 성공 | [ ] |
| U6 | 서버 오류 표시 | 500 · 400 모킹 | 오류 표시 + 입력값 보존 | [ ] |
| C1 | 스키마 단일 출처 | 앱 코드 검색 | 응답 타입 재정의 0건 | [ ] |
| C2 | 모킹 검증 | 모킹 응답 전부를 해당 zod 스키마로 `parse` | 전부 통과 | [ ] |

**U4(밀도 3단계) 해당 없음** — `apps/shop` 전용 항목이고, 미리보기는 STANDARD 고정이다.

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태를 `완료` 로 변경 + `docs/tasks/README.md` · `M05-catalog/README.md` 인덱스 갱신 | [ ] |
| D2 | 등록 흐름을 `docs/design/pages.md` 판매자 절에 반영 | [ ] |
| D3 | 결정 변경 없음 확인 | [ ] |
| D4 | 새 환경변수 없음 확인 | [ ] |
| D5 | 새로 도입한 라이브러리 버전을 8장에 기록 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 옵션 값이 많으면 Variant 표가 수백 행이 된다 | 조합 상한을 `productWriteRequestSchema` 에서 읽어 초과 전에 안내한다(F9). 표는 행 단위로 렌더를 분리한다(F10) |
| R2 | 카테고리 변경 시 값 유지 규칙이 화면과 서버에서 갈릴 수 있다 | 규칙은 **화면 전용**이다. 서버는 최종 값만 검증한다. 규칙을 순수 함수로 분리해 테스트로 고정한다 |
| R3 | 미리보기가 구매자 상세(TASK-0043)와 어긋난다 | 미리보기는 **레이아웃 흉내**임을 화면에 명시하고, TASK-0043 완료 후 그 컴포넌트를 재사용하도록 교체한다 |
| R4 | 이미지 위젯(TASK-0033)이 선행이라 일정이 묶인다 | 위젯 인터페이스만 스텁으로 두고 병행 개발할 수 있다. 다만 F 항목 중 이미지 관련은 위젯 완료 후 측정한다 |

## 8. 확정된 버전

새 의존성 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-03 | 최초 작성. D-208 에 따라 TASK-0034 에서 분할 |
| 2026-09-04 | 카탈로그 도메인 오류 코드를 그리는 몫을 범위에 회수. TASK-0032 · 0036 의 4.10 이 TASK-0113·0114 로 넘겼으나 이 문서에는 없었다 |
