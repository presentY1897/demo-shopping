# TASK-0114: 판매자 상품 편집기 화면

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M05 카탈로그 |
| 상태 | 완료 |
| 작성일 | 2026-09-03 |
| 브랜치 | `feature/product-editor-page` |
| 선행 작업 | TASK-0113, TASK-0033, TASK-0017, TASK-0019, TASK-0023, TASK-0107 |

> **분할 유래** — TASK-0034(판매자 상품 등록 · 수정)의 화면 절반이다. D-208 에 따라 화면과 API 를 나눴고,
> API 는 TASK-0113 이 맡는다. 원본은 범위가 전부 UI 였는데 그 UI 가 부를 API 가 어디에도 없었다.

## 1. 목적

`apps/seller` 의 상품 등록 · 수정 화면을 만든다. **카테고리를 고르면 속성 입력 폼이 자동 생성**되고,
**옵션 조합에서 Variant 표가 자동 생성**되는 것이 이 화면의 핵심이다.

## 2. 범위

### 포함
- `/products/new` · `/products/[id]/edit` — 등록 · 수정
- 기본 정보 (이름 · 설명 · 카테고리)
- **카테고리 선택 시 속성 폼 자동 생성** — TASK-0030 의 정의 조회 + TASK-0017 의 동적 폼 생성기
- **카테고리 변경 시 같은 key 의 입력값 유지**, 나머지는 버림
- 이미지 업로드 위젯 연결 (TASK-0033) — 순서 변경 · 대표 지정
- **옵션 정의 UI** — 옵션명 추가, 값 추가·삭제·정렬
- **옵션 조합 → Variant 표 자동 생성** — 가격 · 재고 · SKU · **1회 최대 구매 수량**의 일괄 입력과 개별 입력
- 조합 수 상한(`PRODUCT_MAX_VARIANTS`) 초과 시 안내
- 임시저장(`DRAFT`) / 발행(`ACTIVE`) 전환. **발행은 별도 엔드포인트**(`POST /products/:id/publish`)다
- 수정 모드 — 기존 값 로드, 옵션 변경 시 어떤 조합이 추가·비활성화되는지 **저장 전 미리 표시**
- **초안이 미완성일 수 있다는 안내** ([TASK-0113](./TASK-0113-product-write-api.md) 7.3 이 넘긴 것) —
  임시저장은 필수 속성이 비어도 성공하고 발행만 전량을 요구하므로, 「판매 시작」이 왜 막혔는지를
  화면이 말해야 한다
- **옵션 축 자체는 수정에서 바꿀 수 없다는 안내** (TASK-0113 7.3 · 그 문서 R8) — 축을 더하거나 빼면
  API 가 400 이다. 값만 추가·삭제할 수 있다는 것을 수정 모드에서 미리 말한다
- **미리보기** — 구매자 상세 화면 레이아웃으로 렌더
- `productWriteRequestSchema` · `productDetailResponseSchema` 로 **모킹 데이터를 만들고 `parse` 로 검증**
- **카탈로그 도메인 오류 코드를 화면에 그린다** ([TASK-0032](./TASK-0032-product-schema.md) 4.10 ·
  [TASK-0036](./TASK-0036-stock-ledger.md) 4.10 이 TASK-0113 과 함께 넘긴 것) — 코드 정의와
  `apps/seller` 카탈로그 문장은 [TASK-0113](./TASK-0113-product-write-api.md) 이 만든다. 이 TASK 는
  그 코드별로 **어디에 무엇을 띄울지**를 정한다(필드 옆 · 표 위 · 토스트)

### 제외 (이번에 하지 않는 것)
- **상품 저장 · 조회 API** → TASK-0113
- **속성 정의 관리 화면** → TASK-0031
- **이미지 업로드 위젯 자체** → TASK-0033. 여기서는 배치하고 결과 URL 을 폼에 연결한다
- **상품 목록 · 재고 조정 화면** → TASK-0116
- 검색 노출 → M06
- 실제 API 호출 테스트 — 백엔드는 모킹한다

## 3. 요구사항

### 기능 요구사항
- [x] 카테고리를 바꾸면 속성 폼이 즉시 바뀐다
- [x] 카테고리를 되돌리면 같은 key 의 입력값이 남아 있다
- [x] 옵션 3 × 4 를 넣으면 Variant 12행 표가 생성된다
- [x] Variant 가격 · 재고 · 최대 구매 수량을 일괄 입력할 수 있다
- [x] 임시저장 후 다시 들어오면 입력값이 복원된다
- [x] 필수 속성이 비면 발행이 막히고 **해당 필드에** 오류가 표시된다
- [x] 수정 시 옵션을 늘리면 어떤 조합이 추가되는지 저장 전에 보인다
- [x] 미리보기가 구매자 상세와 같은 레이아웃으로 보인다

### 비기능 요구사항
- Variant 표가 100행일 때도 입력이 버벅이지 않는다 (행 단위 렌더 분리)
- 콘솔 화면 — 밀도 토글 없음(2단계 고정), 데스크톱 우선

## 4. 설계

### 흐름

```
① 기본 정보 → ② 카테고리 선택 → ③ 속성 폼 (자동 생성) → ④ 이미지
                                   ↓
④ 옵션 정의 → ⑤ Variant 표 (조합 자동 생성) → ⑥ 가격·재고 입력 → 저장
```

카테고리 변경 시 이미 입력한 속성값은 **키가 같으면 유지**하고 나머지는 버린다. 전부 날리면 카테고리를
잘못 골랐다가 되돌릴 때 다시 입력해야 한다.

Variant 표는 행이 많아질 수 있으므로 **일괄 입력(전체 가격 동일 적용)을 먼저** 제공한다.

### 화면 / 컴포넌트

```
apps/seller/src/app/products/new/page.tsx
apps/seller/src/app/products/[id]/edit/page.tsx
        └ ProductEditor           네 상태(로딩 · 없음 · 에러 · 정상)와 저장 결과를 소유
            ├ ProductBasicFields      이름 · 설명 · 카테고리
            ├ ProductAttributeFields   카테고리 정의로부터 생성 (TASK-0017 `DynamicForm`)
            ├ ProductImageField        TASK-0033 위젯 래핑
            ├ ProductOptionEditor      옵션명 · 값
            ├ VariantTable             조합 표 + 일괄 입력 (행은 `memo`)
            └ ProductPreview           구매자 상세 레이아웃
```

`src/` 가 계획에는 없었다. 세 앱 전부 `src/app/` 이므로 경로 쪽을 고친다.

**판정은 순수 함수로 뗀다**(R2, Q5 강화 대상). `apps/seller/src/lib/products/` 에 조합 전개 ·
차분 · 카테고리 변경 시 값 유지 · 저장 본문 만들기 · 실패의 배치가 들어가고, 컴포넌트는 그것을
그리기만 한다. `apps/api` 의 `variant-rules.ts` 를 **가져다 쓸 수 없다** — 앱을 가로질러 import
할 수 없으므로 조합 전개는 화면 쪽에 한 벌이 더 있고, 그래서 그쪽이 분기 100% 대상이다.

### 미리보기의 밀도

미리보기는 구매자 화면을 흉내내지만 **밀도 `STANDARD` 고정**으로 렌더한다. 콘솔에는 밀도 토글이 없고
(TASK-0019), 판매자가 미리보기 안에서 밀도를 바꾸는 것은 이 화면의 목적이 아니다. 밀도 3단계 검증은
구매자 상품 상세(TASK-0043)가 받는다. 그래서 **P6 · U4 는 해당 없음**이다.

### 절단면 — `packages/shared` 의 zod 스키마

TASK-0113 이 정의한 스키마를 그대로 쓰고, **모킹 응답도 같은 스키마로 만든다.**

**계획이 적은 이름 셋은 존재하지 않는다.** TASK-0113 이 착수 시 같은 것을 발견하고 그 문서 4장에
대조표를 남겼다 — 새 이름을 만들면 C1(단일 출처)이 그 자리에서 거짓이 되므로 기존 이름에 맞춘다.
아래가 실제 이름이고, 계획했던 이름은 오른쪽에 남겨 둔다.

| 실제 스키마 | 계획 시 이름 | 이 화면에서의 용도 |
| --- | --- | --- |
| `createProductRequestSchema` · `updateProductRequestSchema` | `productWriteRequestSchema` | 저장 본문의 클라이언트 검증 |
| `productPublishRequestSchema` | (없었음) | 발행 · 발행 취소 본문 (`version` 하나) |
| `productOptionInputSchema` · `productVariantInputSchema` | 〃 | 옵션 에디터 · Variant 표의 행 타입 |
| `variantDefaultsSchema` | (없었음) | 일괄 입력의 값. 새로 생기는 조합이 여기서 출발한다 |
| `productImageInputSchema` | (없었음) | 이미지 위젯이 돌려준 URL 을 저장 본문에 싣는 형태 |
| `productSchema` · `productResponseSchema` | `productDetailResponseSchema` | 수정 모드 로드 · 미리보기. **모킹 응답 `parse`** |
| `productStatusSchema` · `sellerSettableStatuses` | `productStatusSchema` | `DRAFT` / `ACTIVE` 배지와 버튼 문구의 값 출처 |
| `attributeValuesSchema` | `productAttributeValuesSchema` | 속성값 컨테이너. 키별 타입은 TASK-0030 의 **동적 zod 스키마**가 판정 |

**조합 수 상한은 `PRODUCT_MAX_VARIANTS` = 200 이다.** 계획이 적은 100 은 TASK-0032 가 그 상수를
세우기 전의 숫자이고, TASK-0113 이 착수 시 같은 자리를 고쳤다(그 문서 4장 「조합 수 상한」).
화면은 숫자를 다시 적지 않고 상수를 import 한다 — 두 곳에 적힌 순간 화면과 서버가 다른 값을 막는다.

속성 폼은 정의(`AttributeDefinition`)에서 zod 스키마를 만들어 검증한다 — 서버가 저장 직전에 쓰는 것과
**같은 생성기**(TASK-0030)를 쓴다. 검증 규칙을 화면에 따로 적으면 서버와 어긋난다.

### 발행을 어느 요청으로 하는가 — 착수 시 정했다

TASK-0113 이 `POST /products/:id/publish` 를 따로 세웠고 그 근거는 「저장과 판매 시작은 다른
의도이고, 하나의 필드로 보내면 둘 중 무엇이 거절됐는지 말할 수 없다」였다. **편집기는 그
엔드포인트를 쓰지 않는다.**

편집기는 언제나 완전한 본문을 들고 있다. 화면에 고쳐 놓은 것이 있는 채로 「판매 시작」을 누르는
것이 정상 동선이고, 그때 `publish` 를 부르면 둘 중 하나가 된다 — 화면의 수정을 버리거나, 저장
후 발행이라는 **반쯤 성공할 수 있는 두 요청**으로 쪼개거나. 한 요청·한 트랜잭션이 TASK-0113 이
세운 계약이고, 그것을 두 번 왕복으로 되돌리는 것이 이 화면의 몫일 리 없다.

**계약을 잃지 않는다.** 서버의 `publish` 는 `update(id, { version, status: 'ACTIVE' })` 를 그대로
부른다 — 행 잠금, 낙관적 잠금, 속성 전량 검증, `minPrice` 재계산이 전부 같은 경로다. 즉
`updateProduct` 에 `status` 를 실어 보내는 것과 **서버에서 일어나는 일이 동일**하고, 화면은
자기가 어느 의도를 보냈는지 알고 있으므로 거절을 가르는 데도 문제가 없다.

전용 엔드포인트 둘은 **저장할 것이 없이 스위치만 넘기는 화면**의 것이다 — 상품 목록의 행
(TASK-0116). 7.1 에 넘겼다.

### 도메인 오류 코드를 어디에 그리는가

**코드와 문장은 TASK-0113 이 만들었다. 이 TASK 의 몫은 배치다** — 같은 400 이라도 고칠 곳이 필드
안이냐 표냐 화면 전체냐가 다르고, 그 차이를 화면이 표현하지 않으면 코드를 나눈 값이 사라진다.

자리는 셋이다.

| 자리 | 언제 | 무엇이 붙나 |
| --- | --- | --- |
| **필드 옆** | 고칠 것이 하나의 입력 안에 있다 | `FormField` 의 `aria-describedby` 로 묶인 오류 문장. `useForm` 이 첫 무효 컨트롤로 포커스를 옮긴다 |
| **표 위** | 고칠 것이 옵션 · Variant 표 안에 흩어져 있다 | 표 바로 위의 `role="alert"` 안내. 표를 스크롤해도 문장이 따라다니지 않으므로 무엇을 고칠지 먼저 읽고 내려간다 |
| **토스트** | 결과를 알릴 뿐 고칠 것이 없다 | 저장 성공 · 발행 · 발행 취소, 그리고 응답이 아예 오지 않은 실패(`transport`) |

그 위에 하나 더 있다 — **배너**. 화면 전체가 막힌 상태(스토어 미승인, 낙관적 잠금 충돌)는 어느
입력의 문제도 아니고 토스트처럼 사라져서도 안 되므로, 폼 위에 남는다.

| `code` | 자리 | 왜 |
| --- | --- | --- |
| `PRODUCT_ATTRIBUTES_REQUIRED` | **필드 옆** | 응답이 `details[].field = attributes.<key>` 를 준다. 비어 있는 속성 **전부**를 한 번에 이름 짓는다(TASK-0113 F3) — 하나씩 고치게 하면 발행 버튼을 다섯 번 눌러야 한다 |
| `PRODUCT_TOO_MANY_VARIANTS` | **표 위** | `field = options` 다. 고칠 곳은 옵션 값 목록이고, 표에는 이미 상한을 넘긴 조합이 그려져 있다. **화면은 서버에 물어보기 전에 같은 상수로 먼저 막는다**(F9) — 이 코드는 그 예방이 새는 경우의 그물이다 |
| `PRODUCT_NOT_SELLABLE` | **표 위** | `field = status` 지만 status 는 입력이 아니다. 고칠 곳은 표의 「판매」 스위치이므로 표 위에 붙인다 |
| `PRODUCT_SELLER_INACTIVE` | **배너** | 403 이고 필드가 없다. 소유권 403(`FORBIDDEN`)과 **정반대의 다음 수**다 — 이쪽은 내 스토어가 맞고 심사가 안 끝났다. 폼을 계속 그리되 저장 버튼 위에 남긴다 |
| `PRODUCT_SKU_TAKEN` | **표 위** | 409 인데 필드가 없다(서버가 어느 행인지 모른다 — 부분 유니크 인덱스가 답한 것이다). 고칠 곳은 표의 SKU 열이므로 표 위에서 "SKU 를 바꿔 주세요"를 말한다. **다시 불러오기를 권하지 않는다** — 재조회로 풀리지 않는 409 다 |
| `PRODUCT_VERSION_CONFLICT` | **배너** | 나머지 409. 다시 불러오면 풀리므로 「최신 내용 불러오기」와 「그대로 저장」을 함께 준다 — 입점 화면의 충돌 배너(TASK-0109)와 같은 모양이고, 입력값은 하나도 덮어쓰지 않는다(DECISIONS 4) |
| `INVALID` (`images.<n>.url`) | **필드 옆** | 이미지 절 아래. 다른 스토어의 키를 가리키는 URL 은 이미지를 다시 올려야 한다(TASK-0113 F14) |
| `INVALID` (`options.…` · `variants.…`) | **표 위** | 중복 옵션명 · 중복 값 · 없는 조합. 전부 옵션·Variant 표 안의 문제다 |
| 그 밖의 코드 · `transport` | **토스트** | 이 화면이 이름을 갖지 못한 실패. 카탈로그가 답하고, 없으면 서버 문장이 그대로 나간다 |

`ATTRIBUTE_IN_USE` 는 **이 화면에서 나오지 않는다.** 속성 정의를 지우는 것은 `DELETE /attributes/:id`
이고 그 화면은 `apps/admin` 이다(HANDOFF 3.1). 판매자 카탈로그가 문장을 갖는 것은
`Record<UserFacingErrorCode, string>` 이 전수 검사라서이지 여기서 닿기 때문이 아니다.

배치는 `packages/ui` 의 `serverFieldErrors` 가 이미 가진 두 손잡이로 표현한다 — `fields` 는 폼이 아는
경로 목록이고, `codeFields` 는 필드를 말하지 않는 코드를 어디에 놓을지다. 화면에 `if (code === …)`
사슬을 쓰지 않는 이유이고, 표 위·배너·토스트의 갈림은 그 앞에서 한 번만 판정한다.

### 브라우저로 열 수 없다 — P2 를 무엇으로 재는가

**이 화면은 지금 브라우저로 열리지 않는다.** 로그인 경로가 Google OAuth 뿐이고 데모 계정은
TASK-0024 다(HANDOFF 1장). TASK-0109 · 0110 · 0112 가 전부 같은 벽에 부딪혔고, 셋 다 Lighthouse
대신 **조립된 화면에 axe 를 직접 돌렸다.** 이 TASK 도 같은 방식을 쓴다.

- **P2 는 그대로 게이트다.** `packages/ui` 가 스토리마다 axe 를 돌리지만 접근성 있는 컴포넌트의
  집합이 접근성 있는 화면은 아니다 — 필드 배선, 표의 행 헤더, 옵션 세트의 `fieldset`, 차분 안내의
  live region 은 조립된 뒤에만 존재한다. 그것이 여기서 재는 것이다.
- **P3 · P4 는 jsdom 에서 잰다.** `matchMedia` 를 세 폭으로 스텁하고(테스트 지원의 `VIEWPORTS`)
  Tab 순회를 실제로 돌린다. 픽셀 넘침은 jsdom 이 잴 수 없으므로, 넘칠 수 있는 것(Variant 표)을
  **가로 스크롤 컨테이너 안에 가두었는지**로 대신 잰다.
- **P1(LCP)은 게이트가 아니다** — D-217 이후 QUALITY-GATES 2장이 성능을 게이트에서 뺐다. 계획이
  적은 「P1 LCP 2.5s」는 그 결정 이전의 표다. 대신 무거운 것을 새로 들였는지를 **신고**한다.

### 역할별 권한

`apps/seller` 진입은 TASK-0023 의 미들웨어가 판정한다. 스토어가 `ACTIVE` 가 아니면 이 화면 대신 안내
화면이 뜬다(TASK-0109). 저장 버튼은 `can('product.write')` 로 비활성 처리하되, 실제 차단은 서버의
403 이다.

## 5. 구현 계획

1. 폼 골격 + 기본 정보 (TASK-0017 어댑터)
2. 카테고리 선택 → 속성 폼 연동 (동일 key 유지 규칙)
3. 이미지 위젯 연결 (순서 · 대표)
4. 옵션 정의 UI
5. Variant 표 생성 · 일괄 입력 · 상한 안내
6. 임시저장 · 발행 전환
7. 수정 모드 (차분 미리 표시)
8. 미리보기
9. 모킹 `parse` 테스트, 키보드 순회, 조립된 화면 axe (4장 「브라우저로 열 수 없다」)

## 6. 완료 기준 (Definition of Done)

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 결과 | 충족 |
| --- | --- | --- | --- | --- | --- |
| F1 | 속성 폼 자동 생성 | 정의 6개짜리 카테고리 선택 (모킹) | 정의 수만큼 필드 생성, 타입별 컨트롤 반영 | 코트를 고르면 여섯 정의가 **다섯 타입 전부**를 낸다 — textbox · combobox · spinbutton · checkbox · group. 타입마다 컨트롤을 확인하는 것이 핵심이다: 개수만 세면 전부 텍스트 상자로 그리는 구현이 통과한다 | [x] |
| F2 | 카테고리 변경 | 다른 카테고리로 변경 후 되돌림 | 폼 교체, 동일 key 값 유지 | 코트 → 티셔츠 → 코트 왕복에서 두 카테고리가 함께 정의하는 `brand` 만 남고 코트만 정의하는 `wool_ratio` 는 비어 돌아온다. **키만 보지 않는다** — 타입이 담을 수 없는 값과 없어진 선택지도 버린다(순수 함수 분기 100%) | [x] |
| F3 | Variant 생성 | 옵션 3 × 4 입력 | 12행 표 생성 | 화면에서 축 둘을 만들어 12행. 전개 순서는 **첫 축이 가장 느리게** 변하는 서버와 같은 순서이고, 그것을 순수 함수 검사가 따로 고정한다 | [x] |
| F4 | 일괄 입력 | 전체 가격 일괄 적용 | 12행 전부 반영 | 12행 전부 12,000. **이미 값이 있는 행도 덮어쓴다** — 채워진 행을 건너뛰면 고치려던 행만 남는다 | [x] |
| F4b | 구매 수량 제한 입력 | 최대 수량 2를 일괄 적용 | 저장 요청 본문의 각 Variant 에 `maxPurchaseQuantity: 2` | 화면에서 일괄 적용 후 저장했고, 대역에 남은 행의 `maxPurchaseQuantity` 가 2다. 비운 칸은 `null` 로 보낸다 — 빼면 방금 지운 일괄값을 물려받는다 | [x] |
| F5 | 임시저장 | `DRAFT` 저장 후 수정 모드 재진입 (모킹 응답) | 입력값 복원 | 필수 속성 `brand` 가 빈 채로 201, 상태 `DRAFT`, Variant 1개. **화면 검증도 함께 완화**해야 했다 — `schemaForFields` 는 `required` 를 그대로 걸므로, 초안 저장이 서버는 받는데 브라우저가 막는 상태였다. 폼은 초안 규칙으로 검증하고 발행에만 엄격한 통과를 더 요구한다 | [x] |
| F6 | 필수 검증 | 필수 속성 비우고 발행 | 제출 차단 + **해당 필드에** 오류 표시 | 요청이 나가지 않고 브랜드 입력 아래에 문장이 붙는다. 버튼 옆에서 막지 않고 **던져서** 막는다 — 텍스트 필드의 Enter 가 같은 문을 지나게 하려면 문이 하나여야 한다 | [x] |
| F6b | 서버가 거절한 필수 속성 | 화면 검증을 통과한 요청을 서버가 `PRODUCT_ATTRIBUTES_REQUIRED` 로 거절 | 응답의 `details[].field` 전부가 **해당 필드에** 표시 | 초안(`fit` 이 빔)을 발행하면 서버 판정이 `attributes.fit` 아래에 붙는다. 폼 필드 이름을 `attributes.<key>` 로 지은 것이 이것을 성립시킨다 | [x] |
| F7 | 수정 차분 표시 | 사이즈에 `XL` 추가 | "추가될 조합 3건" 안내, 저장 후 기존 재고 값 유지(모킹 응답 기준) | 「추가될 조합 3건」 · 「재고가 유지되는 조합 12건」. 값을 빼면 「판매 중지될 조합 4건」과 **행은 남는다**는 안내. 대역 쪽에서도 12 → 15행이 되고 기존 12행의 재고가 id 단위로 불변인 것을 따로 고정했다 | [x] |
| F7b | 옵션 축은 못 바꾼다 | 수정 모드 진입 | 축 추가·삭제 컨트롤 없음 + 이유 안내 (TASK-0113 4장) | 「옵션 추가」·「옵션 빼기」가 DOM 에 없고 이름 입력이 비활성이며 이유가 화면에 있다. 눌러도 항상 400 인 버튼은 없는 것보다 나쁘다 | [x] |
| F8 | 미리보기 | 미리보기 실행 | 구매자 상세와 동일 레이아웃(밀도 STANDARD) | 다이얼로그에 갤러리 · 이름 · 최저가 · 옵션 · 속성 표가 그려지고 `data-density="2"` 가 DOM 에 있다. **예행연습임을 패널이 스스로 말한다**(R3) — 구매자 상세는 TASK-0043 이고, 조용히 다른 것을 보여 주는 미리보기는 없는 것보다 나쁘다 | [x] |
| F9 | 조합 상한 | 조합이 `PRODUCT_MAX_VARIANTS`(200)를 넘는 옵션 입력 | 저장 전 안내 + **제출 차단**(버튼은 눌린다 — 아래 결과 칸) | 6 × 6 × 5 = 180 에서 값 하나를 더해 216 이 되는 **그 키 입력에** 안내가 뜨고, 저장은 표 위에서 거절된다. 축이 둘로는 상한을 넘길 수 없다 — 한 축의 값이 40개까지라 40 × 40 은 값 단위로 먼저 막힌다. **버튼을 비활성하지 않은 것은 결정이다**: 네이티브 `disabled` 는 Enter 의 암묵적 제출까지 막아 아무 말도 못 하게 만든다(TASK-0018 4.5 · TASK-0109 9장) | [x] |
| F10 | 대용량 표 | Variant 200행에서 셀 입력 | 한 셀 입력이 다른 행을 다시 그리지 않는다 (행 단위 렌더 분리) | 180행에서 한 셀을 고쳐도 나머지 179행이 **같은 DOM 노드**이고 값도 그대로다. 그것을 성립시키려고 `@shopping/ui` 의 `Table` 을 쓰지 않았다 — 그 컴포넌트는 셀을 부모 렌더 안에서 만들어 한 칸 입력에 1,000개 입력이 다시 그려진다. 행은 `memo` 이고 상태 갱신은 행 객체를 하나만 갈아 끼운다(`patchRow` 분기 검사) | [x] |
| F11 | 도메인 오류 코드 배치 | `PRODUCT_*` 6종을 각각 모킹 | 4장 표대로 필드 옆 · 표 위 · 배너 · 토스트 | 배치 판정을 순수 함수로 떼어 **분기 100%** 로 고정하고, 화면에서 넷을 실제로 몰아봤다 — 낙관적 잠금은 배너(불러오기·그대로 저장), 스토어 상태는 배너, SKU 충돌은 표 위(다시 불러오기를 권하지 않는다), 미도달은 토스트. `ATTRIBUTE_IN_USE` 는 이 화면에서 나오지 않는다(4장) | [x] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 이 TASK 는 **화면만 만드는 프론트 TASK** 이므로
1장 · 2장 · 5장(C1·C2) · 7장을 받는다.

| 장 | 적용 | 비고 |
| --- | --- | --- |
| 1장 코드 게이트 | Q1~Q4 · Q6 · Q7 전부 | |
| Q5 테스트 충실도 | **커버리지 수치 없음** → 아래 U 목록. 단 **조합 전개 · key 유지 규칙이 화면 쪽 순수 로직으로 남으면 그 부분은 분기 100%** | 대역: **백엔드 모킹.** 업로드 위젯도 스텁 |
| 2장 화면 게이트 | **P2~P5 적용. P1 은 게이트가 아니다** — D-217 이후 QUALITY-GATES 2장이 LCP 를 게이트에서 뺐고, 계획의 「P1 LCP 2.5s」는 그 결정 이전의 표다. **P6 해당 없음** — 콘솔은 밀도 2단계 고정, 미리보기는 STANDARD 고정 | 측정은 **axe**다 — 4장 「브라우저로 열 수 없다」 |
| **3장 API 게이트** | **해당 없음** | 엔드포인트를 추가하지 않는다. API 는 TASK-0113 |
| **4장 데이터 게이트** | **해당 없음** | `schema.prisma` 무변경 |
| 5장 계약 게이트 | **C1 · C2** | C3 는 TASK-0113 |
| 7장 문서 게이트 | D1~D5 | |

| # | 기준 | 측정 방법 | 목표 | 결과 | 충족 |
| --- | --- | --- | --- | --- | --- |
| Q1 | `pnpm typecheck` | 워크스페이스 전체 | error 0 | 8개 프로젝트 전부 error 0 | [x] |
| Q2 | `pnpm lint` | 워크스페이스 전체 | error 0 · warning 0 | 0 · 0 | [x] |
| Q3 | `pnpm build` | 워크스페이스 전체 | 전 앱 성공 | shared · ui · api-mocks · api · shop · seller · admin 전부 성공. 새 라우트 둘이 목록에 있다 — `/products/new`(정적) · `/products/[id]/edit`(동적) | [x] |
| Q4 | `pnpm test` | 워크스페이스 전체 | 전부 통과 | `apps/seller` **322 passed** (착수 시점 178) · `packages/api-mocks` **213** (착수 시점 182) · ui 843 · api 1,565 · shop 234 · admin 336 전부 통과 | [x] |
| Q5 | 순수 로직 분기 | 조합 전개 · 차분 · key 유지 · 실패 배치 | 분기 100% | 다섯 모듈(`combinations` · `attribute-values` · `variant-rows` · `product-request` · `product-failures`) **분기 131/131 · 라인 138/138 · 함수 76/76**. 도달할 수 없는 분기 둘은 코드에서 없앴다 — 닿지 않는 분기는 아무것도 검사하지 않으면서 100% 를 면제로 만든다 | [x] |
| Q7 | commitlint | 커밋 훅 | 위반 0 | 위반 0 | [x] |
| P2 | 접근성 | **조립된 편집기에 axe** (전 상태) — 4장 참조 | violation 0 | 11개 상태에 violation **0** — 읽는 중 · 빈 등록 폼 · 없는 상품 · 읽기 실패 · 전체 로드 · 정의 없는 카테고리 · 필드 옆 오류 · 표 위 거절 · 충돌 배너 · 스토어 상태 배너 · 미리보기. 검사 장치 자체가 비어 있지 않은 것도 확인했다(alt 없는 `img` 로 `image-alt` 가 실제로 뜬다) | [x] |
| P3 | 반응형 | 360 / 768 / 1440px (`matchMedia` 스텁) | Variant 표는 가로 스크롤 컨테이너 안, 레이아웃 깨짐 0건 | 세 폭에서 전 절이 그려지고 표는 `overflow-x-auto` · `tabindex=0` 인 이름 있는 영역 안에 있다. jsdom 은 픽셀을 재지 못하므로 **넘칠 수 있는 것이 갇혀 있는지**로 잰다 — 문서 자체가 가로로 스크롤되지 않는다는 뜻이다(TASK-0016 F4b) | [x] |
| P4 | 키보드 조작 | Tab 순회 | 옵션 추가 · 표 셀 · 저장까지 도달·조작 가능 | 값 추가 → **새 상자가 포커스를 받고** → 표 셀 → 저장까지 마우스 없이. 새 상자가 포커스를 못 받으면 키보드 사용자는 기존 값을 전부 지나 돌아와야 한다 | [x] |
| P5 | 상태 표현 | 로딩 / 빈(옵션 없음) / 에러 | 세 상태 모두 구현 | 로딩은 `role="status"` 로 **알린다**(스켈레톤만 그리지 않는다) · 빈 상태는 셋(옵션 없음 · 조합 없음 · 정의 없는 카테고리) · 에러는 읽기 실패와 저장 실패가 다른 자리다 | [x] |
| U1 | 조건부 렌더 | 로딩 · 빈 · 에러 · 정상 | 4상태 각각 테스트 | 넷 + **없음**. 404 를 에러에 접지 않은 것이 이 항목의 실질이다 — 오래된 북마크에는 다른 문장과 다른 다음 수가 필요하다 | [x] |
| U2 | 폼 검증 오류 | F6 · F6b | 해당 필드에 오류 표시 | 화면 판정과 서버 판정이 **같은 필드**에 붙는다 | [x] |
| U3 | 중복 클릭 차단 | 저장 연속 2회 클릭 | 요청 1회 | 같은 틱의 두 클릭에 POST 1회. 리렌더 전이라 `Button.loading` 만으로는 덮이지 않는 경우다 | [x] |
| U5 | 키보드 조작 | 마우스 없이 상품 1건 등록 | 성공 | 저장 버튼에서 Enter, 그리고 **상품명 상자에서 Enter** 둘 다 저장된다. 뒤엣것이 `Button.loading` 이 덮지 못하는 암묵적 제출이고, 문이 하나라서 성립한다 | [x] |
| U6 | 서버 오류 표시 | 500 · 400 모킹 | 오류 표시 + 입력값 보존 | 읽기 500 은 문의 번호와 다시 시도, 저장 미도달은 토스트이고 **고쳐 쓴 이름이 그대로 남는다** | [x] |
| C1 | 스키마 단일 출처 | 앱 코드 검색 | 응답 타입 재정의 0건 | 0건. 요청·응답 타입은 전부 `@shopping/shared` 에서 오고, 상한값(`PRODUCT_MAX_VARIANTS` · `PRODUCT_MAX_OPTIONS` · `PRODUCT_MAX_OPTION_VALUES`)도 import 한다 — 화면에 숫자를 다시 적지 않았다 | [x] |
| C2 | 모킹 검증 | 모킹 응답 전부를 해당 zod 스키마로 `parse` | 전부 통과 | 픽스처 둘과 **모든 응답**이 `defineFixture(productResponseSchema, …)` 를 지난다. 레지스트리 스펙이 `defineFixture` 를 거치지 않은 픽스처를 잡고, 저장 요청은 `readBody` 가 요청 스키마로 파싱한다 — 화면이 잘못된 본문을 보내면 대역이 거절한다 | [x] |

**신고 — 무겁게 만든 것**(2장 「LCP 는 화면 게이트가 아니다」). **새 의존성 없음**이고, 새 코드는
전부 앱 안의 TypeScript 다. 큰 라이브러리도 대용량 자산도 새 폰트도 들이지 않았다.

**다만 옮겨 적을 수가 없었다.** 그 절은 「`next build` 가 이미 출력하는 First Load JS 를 옮겨
적는 것으로 충분하다」고 하는데, **Next 16 + Turbopack 은 그 표를 더 이상 출력하지 않는다** —
라우트 목록만 나오고 크기 열이 없다. 대신 잰 것: `apps/seller` 의 클라이언트 청크 전체가
**1.80MB**(23개 파일, 압축 전). 라우트별 분해는 이 빌드에서 얻을 수 없다. 기록이므로 통과·실패는
없지만, **누적을 맡은 TASK-0097 은 새 측정 수단이 필요하다** — 7.1 에 적었다.

**U4(밀도 3단계) 해당 없음** — `apps/shop` 전용 항목이고, 미리보기는 STANDARD 고정이다.

### 6.3 문서

| # | 기준 | 결과 | 충족 |
| --- | --- | --- | --- |
| D1 | 상태를 `완료` 로 변경 + `docs/tasks/README.md` · `M05-catalog/README.md` 인덱스 갱신 | 이 문서 갱신. **인덱스 2곳은 오케스트레이터가 별도 커밋** — 병행 작업 중 `main` 에서 계속 움직이는 파일이다(CLAUDE.md 2장) | [x] |
| D2 | 등록 흐름을 `docs/design/pages.md` 판매자 절에 반영 | 「상품 편집기 규약」 절을 새로 넣었다 — 흐름, 여덟 가지 규약, 실패를 네 자리 중 어디에 그리는지. `/products/new` · `/products/[id]/edit` 행과 `/components/image-upload` 행도 함께 고쳤다(7.1) | [x] |
| D3 | 결정 변경 없음 확인 | 없음. `DECISIONS.md` 3장(상품 모델 · 구매 수량 제한 · 옵션 없는 상품 · 속성 검증)과 4장(낙관적 잠금, 덮어쓰기 없음), 11장(오류 전달) 전부 그대로 따랐다 | [x] |
| D4 | 새 환경변수 없음 확인 | 없음 | [x] |
| D5 | 새로 도입한 라이브러리 없음 확인 (8장) | 없음 | [x] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 옵션 값이 많으면 Variant 표가 수백 행이 된다 | 조합 상한 `PRODUCT_MAX_VARIANTS` 를 `@shopping/shared` 에서 읽어 초과 전에 안내한다(F9). 표는 행 단위로 렌더를 분리한다(F10) |
| R2 | 카테고리 변경 시 값 유지 규칙이 화면과 서버에서 갈릴 수 있다 | 규칙은 **화면 전용**이다. 서버는 최종 값만 검증한다. 규칙을 순수 함수로 분리해 테스트로 고정한다 |
| R3 | 미리보기가 구매자 상세(TASK-0043)와 어긋난다 | 미리보기는 **레이아웃 흉내**임을 화면에 명시하고, TASK-0043 완료 후 그 컴포넌트를 재사용하도록 교체한다 |
| R4 | 이미지 위젯(TASK-0033)이 선행이라 일정이 묶인다 | **닫혔다** — TASK-0033 이 완료라 `ProductImageUploader` 를 그대로 붙인다. 스텁은 인코더 · 전송 포트뿐이고 그것은 위젯 자신의 테스트 이음매다 |
| R5 | **이 화면을 브라우저로 열 수 없다** — 로그인 경로가 Google OAuth 뿐이고 데모 계정은 TASK-0024 | 0109 · 0110 · 0112 와 같은 방식으로 **조립된 화면에 axe** 를 돌린다(4장). Lighthouse 실측은 TASK-0024 가 붙은 뒤 TASK-0097 이 누적한다 |
| R6 | **옵션 축을 바꾸려면 초안을 지우고 다시 만들어야 한다** (TASK-0113 R8) | 수정 모드에서 축 컨트롤을 아예 내지 않고 이유를 적는다(F7b). 안전 조건은 여전히 표현할 수 없다 — 주문 테이블(M07)이나 상태 이력 컬럼이 먼저다. **이 TASK 가 편집기에서 불편을 실측하고 그것을 7.1 에 남긴다** |
| R7 | **`api-failure.ts` 가 세 벌**이고 HANDOFF 3.3 이 이 TASK 를 접점으로 지목했다 | 아래 7.1 에 판단과 근거를 적었다. **합치지 않는다** — 올바른 자리가 `packages/shared/src/api/` 이고 그것은 이 TASK 의 소유 밖이다 |
| R8 | **생성된 폼은 `useForm` 의 `initialValues` 를 한 번만 읽는다** | 필드 모양이 움직이면 인스턴스를 갈아야 하고, 그 사이의 입력은 사라진다. **정의가 오기 전에는 폼을 아예 마운트하지 않는다**(7.2). 카테고리를 바꿀 때는 바뀌는 순간의 값을 씨앗으로 붙잡아 넘긴다 |
| R9 | **상한을 넘으면 조합 전개가 빈 목록**이라 표가 통째로 비었다 | 축이 전개되지 않으면 행을 그대로 둔다. 축은 키 하나면 다시 맞고, 비우면 입력한 가격이 전부 사라진다. 그동안 저장만 막는다(F9) |

### 7.2 검사가 잡은 것 — 화면이 생기기 전에는 보이지 않던 두 가지

**둘 다 판매자가 실제로 입력을 잃는 버그**였고, 둘 다 화면을 조립해 몰아보고서야 드러났다.

| 무엇 | 어떻게 드러났나 |
| --- | --- |
| **정의가 오기 전에 폼을 그리면 인스턴스가 교체되며 입력이 사라진다** | 수정 모드의 저장이 검사에서 통째로 안 됐다. 클릭은 들어가는데 `submit` 이 안 났다 — `GET /attributes` 의 응답이 user-event 의 클릭 시퀀스 한가운데에 떨어져 폼이 교체되고, 눌린 버튼이 **문서에서 떨어진 폼**의 것이 되어 있었다. 실제 브라우저에서는 「상품명을 치다가 한 글자가 사라진다」로 나타난다 |
| **상한을 넘기면 표가 비어 입력한 가격이 전부 사라진다** | F9 를 화면에서 재려고 값을 하나 더했더니 180행이 0행이 됐다. 넘긴 판매자가 할 일은 값을 하나 빼는 것인데, 그러면 가격을 180번 다시 입력해야 한다 |

첫째는 「조립된 화면을 실제로 조작해 보지 않으면 순수 함수 검사가 전부 초록이어도 남는다」의
사례다. 두 모듈 다 분기 100% 였고 둘 다 각자로는 옳았다.

### 7.1 주인이 필요한 것 — 오케스트레이터 판단

| 무엇 | 이 TASK 의 판단 | 후보 |
| --- | --- | --- |
| `api-failure.ts` 세 벌 합치기 | **합치지 않았다.** 근거는 아래 | HANDOFF 3.3 그대로. 발동 조건은 `packages/shared` 를 여는 TASK |
| 옵션 축 변경(초안 전체 교체)의 안전 조건 | 편집기에서 실제로 불편하다 — 축을 잘못 만든 초안은 **지우고 다시 만드는 수밖에 없고**, 그 사이 이미지·속성·가격이 전부 날아간다. 다만 "한 번도 팔린 적 없다"를 판정할 근거는 여전히 없다 | TASK-0113 R8 그대로. 주문 테이블(M07) 또는 상태 이력 컬럼(TASK-0032) |
| `/components/image-upload` 개발 전용 화면의 삭제 | **지우지 않았다.** `pages.md` 는 "TASK-0114 가 위젯을 폼에 붙이면 삭제"로 적었지만, 그 화면이 위젯을 **인코더·전송 포트를 주입해 단독으로** 조작하는 유일한 자리이고 `image-upload-widget.spec.tsx` 의 대상이다. 편집기에 붙은 위젯은 폼 안이라 그 두 이음매를 그렇게 쓸 수 없다 | TASK-0116 또는 M15 |
| **발행 · 발행 취소 엔드포인트를 쓰는 화면** | 편집기는 쓰지 않는다(4장 「발행을 어느 요청으로 하는가」). 저장할 것이 없이 스위치만 넘기는 화면이 그 둘의 자리다 | TASK-0116 상품 목록 |
| **First Load JS 를 잴 방법** | Next 16 + Turbopack 이 빌드 표에서 크기 열을 없앴다. QUALITY-GATES 2장의 「신고」 절차가 전제하는 출력이 더 이상 없다 | TASK-0097(성능 누적) |
| **`ATTRIBUTE_IN_USE` 의 `params.count` 를 그리는 화면** | 이 화면에서 나오지 않는 코드다 — 속성 정의 삭제는 `apps/admin` 이다. HANDOFF 3.1 그대로 열려 있다 | M14 또는 `apps/admin` 을 여는 다음 TASK |

#### `api-failure.ts` — 합치지 않은 근거

HANDOFF 3.3 은 합치는 일을 **파일 이동이 아니라 설계 결정**으로 규정했다. 두 질문이 있었고 둘 다
이 TASK 에서 답이 나왔다.

| 질문 | 답 | 근거 |
| --- | --- | --- |
| `hasCode` 를 공통 API 로 둘 것인가 | **그렇다.** 이 화면이 필요로 한다 — 낙관적 잠금 409 는 배너, SKU 409 는 표 위, 판매자 상태 403 은 배너로 갈린다. seller 판에 admin · shop 과 **같은 시그니처**로 더했다 | 4장 오류 코드 배치표 |
| 메시지 `params` 를 보간할 것인가 | **판매자 화면은 필요 없다.** 유일하게 `params` 를 갖는 `PRODUCT_TOO_MANY_VARIANTS` 는 판매자 카탈로그가 `PRODUCT_MAX_VARIANTS` 를 import 해 문장에 넣는다(TASK-0113). 보간기는 admin 에 남고, 공통 판은 `paramsOf` 를 **넘겨주기만** 하면 된다 — 카탈로그가 템플릿을 쓸지 말지는 앱이 정한다 | `apps/seller/src/lib/errors.ts` 머리말 |

**그런데도 합치지 못한다. 갈 곳이 없다.**

| 후보 | 왜 아닌가 |
| --- | --- |
| `packages/shared/src/api/` | **옳은 자리이고, 이 TASK 의 소유 밖이다.** 계약은 TASK-0113 이 정의했고 이 TASK 의 「절대 건드리지 마라」에 그 경로가 있다 |
| `packages/ui` | 이 모듈은 `@shopping/shared` 의 오류 봉투를 읽는다. `packages/ui/src/form/server-errors.ts` 머리말이 정확히 그것을 거부한다 — "컴포넌트 라이브러리에 REST 클라이언트를 끌고 들어오는 것" |
| 새 패키지 | 세 앱의 `package.json` 을 함께 고쳐야 하고 `apps/shop` 은 TASK-0024 가 열려 있다 |

그리고 **셋 중 둘만 합치면 세 번째가 갈라진다.** `apps/shop` 판은 전수 카탈로그가 없는 것이
의도이고(그 파일의 표), seller 파일 머리말이 이미 "반쪽 이동은 공통 판과 남은 판이 어긋날 자유를
준다"고 경고한다. 같은 함정을 다른 조합으로 밟는 것이다.

**남긴 것**: 세 판의 함수 집합이 이제 같다(`apiFailure` · `hasCode` · `failureMessage` ·
`quotableRequestId`). 남은 차이는 각 앱의 `ApiConfigurationError` 임포트와 `failureMessage` 의
카탈로그 타입뿐이고, 둘 다 **인자로 바꿀 수 있는 것**이다 — 즉 자리만 생기면 이동은 기계적이다.

## 8. 확정된 버전

새 의존성 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-03 | 최초 작성. D-208 에 따라 TASK-0034 에서 분할 |
| 2026-09-04 | 카탈로그 도메인 오류 코드를 그리는 몫을 범위에 회수. TASK-0032 · 0036 의 4.10 이 TASK-0113·0114 로 넘겼으나 이 문서에는 없었다 |
| 2026-09-05 | **완료.** 라우트 둘, 순수 모듈 다섯(분기 100%), 컴포넌트 여섯, `packages/api-mocks` 의 상품 쓰기 대역과 픽스처 둘. `apps/seller` 테스트 **178 → 322**, `packages/api-mocks` **182 → 213**. 도메인 오류 코드 여섯의 자리를 정하고 검사로 고정했다. `packages/ui` 무변경. 착수 시 문서 일곱 군데를 먼저 고쳤고(아래), 구현 중 발견한 판단 셋을 문서에 회수했다 — 발행을 어느 요청으로 하는가(4장), 조합 상한을 넘겼을 때 표를 비우지 않는다(R9), 정의가 오기 전에는 폼을 마운트하지 않는다(R8 · 7.2) |
| 2026-09-05 | **착수 시 계획과 실제가 어긋난 곳 일곱 군데를 문서 쪽에서 고쳤다.** 코드를 먼저 만지지 않았다(CLAUDE.md 4장). ① **절단면 스키마 이름 셋이 존재하지 않는다** — `productWriteRequestSchema` · `productDetailResponseSchema` · `productAttributeValuesSchema` 는 TASK-0113 이 착수 시 이미 기존 이름으로 맞췄다. 대조표로 바꾸고 계획이 몰랐던 셋(`productPublishRequestSchema` · `variantDefaultsSchema` · `productImageInputSchema`)을 더했다. ② **조합 상한 100 → `PRODUCT_MAX_VARIANTS` = 200.** TASK-0113 이 같은 자리를 이미 고쳤고, 화면이 두 번째 숫자를 갖지 않는다 — F9 의 「101개」도 함께. ③ **P1(LCP)은 게이트가 아니다** — D-217 이후 QUALITY-GATES 2장이 성능을 게이트에서 뺐다. 대신 First Load JS 를 신고한다. ④ **경로에 `src/` 가 빠져 있었다.** ⑤ **도메인 오류 코드의 배치표를 4장에 새로 적었다** — 이 TASK 의 실제 몫이 그것인데 문서에는 "정한다"만 있었다. 자리는 필드 옆 · 표 위 · 토스트, 그리고 화면 전체가 막힌 상태를 위한 배너 넷이다. ⑥ **TASK-0113 7.3 이 넘긴 셋을 회수했다** — 초안 미완성 안내, 옵션 축 변경 불가 안내(F7b), `PRODUCT_*` 여섯 코드의 복구 수단(F11). ⑦ **브라우저로 열 수 없다는 사실과 그래서 P2 를 axe 로 재는 것**을 4장에 적었다(R5) |
