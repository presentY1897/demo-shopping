# TASK-0112: 프로필 · 배송지 화면

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M04 인증·계정 |
| 상태 | 완료 |
| 작성일 | 2026-09-03 |
| 브랜치 | `feature/profile-address-page` |
| 선행 작업 | TASK-0111, TASK-0023, TASK-0018, TASK-0017, TASK-0107 |

> **분할 유래** — TASK-0027(프로필 · 배송지 관리)의 화면 절반이다. D-208 에 따라 화면과 API 를 나눴고,
> API 는 TASK-0111 이 맡는다. 원본에 있던 **판매자 스토어 설정 화면**은 판매자 도메인으로 옮겨
> TASK-0109 가 담당한다.

## 1. 목적

`apps/shop` 마이페이지에서 프로필 · 표시 설정 · 배송지를 다루는 화면을 만든다. 배송지 입력은 **우편번호
검색 위젯**을 붙여 사용자가 주소를 직접 타이핑하지 않게 한다.

밀도 설정이 여기서 서버로 승격되므로, **밀도 3단계를 실제로 전환해 보는 첫 화면**이기도 하다.

## 2. 범위

### 포함
- `/mypage/settings` — 프로필(이름 · 아바타), 표시 밀도 3단계, 알림 수신 설정
- `/mypage/addresses` — 배송지 목록 · 추가 · 수정 · 삭제 · 기본 지정
- **우편번호 검색 위젯 연동** (외부 스크립트) + **로드 실패 시 수동 입력 폴백**
- **밀도 승격 로직** — 비로그인 localStorage 값을 로그인 직후 `PATCH /me/preferences` 로 올리고, 이후
  서버 값을 단일 출처로 삼는다
- 회원 탈퇴 화면 — 확인 다이얼로그(TASK-0017 의 파괴적 작업 규약) + 결과 안내
- 서버 검증 오류(400 `details`)를 해당 필드에 매핑
- `profileResponseSchema` · `addressSchema` · `userPreferenceSchema` 로 **모킹 데이터를 만들고 `parse` 로 검증**

### 제외 (이번에 하지 않는 것)
- **프로필 · 배송지 · 설정 API** → TASK-0111
- **판매자 스토어 설정 화면** → TASK-0109 로 이관
- 주문서에서의 배송지 선택 UI → M07(TASK-0050). 그 화면은 여기서 만든 배송지 카드 컴포넌트를 재사용한다
- 알림함 화면 → M13(TASK-0090). 이번에는 **수신 여부 스위치**만 만든다
- 실제 API 호출 테스트 — 백엔드는 모킹한다

## 3. 요구사항

### 기능 요구사항
- [x] 프로필을 수정하면 화면에 즉시 반영된다
- [x] 배송지를 추가·수정·삭제하고 기본을 지정할 수 있다
- [x] 기본 배송지가 목록에서 눈에 띄게 구분된다
- [x] 우편번호 검색으로 도로명·지번·우편번호가 자동 입력된다
- [x] 검색 위젯 로드가 실패해도 손으로 입력해 저장할 수 있다
- [x] 밀도를 바꾸면 화면이 즉시 그 밀도로 바뀌고 서버에 저장된다
- [x] 비로그인에서 바꾼 밀도가 로그인 후에도 유지된다
- [x] 탈퇴는 확인을 거쳐야 실행된다

### 비기능 요구사항
- **밀도 3단계 전부에서 레이아웃이 정상**이어야 한다 (`apps/shop` 화면이므로 P6 · U4 대상)
- 접근성 — 오류 메시지가 `aria-describedby` 로 필드에 연결된다

## 4. 설계

### 화면 / 컴포넌트

```
apps/shop/app/mypage/settings/page.tsx    프로필 · 밀도 · 알림
apps/shop/app/mypage/addresses/page.tsx   배송지 목록
        ├ AddressCard                     한 건 표시 + 기본 배지 + 수정 · 삭제
        └ AddressForm                     추가 · 수정 (우편번호 검색 포함)
```

`docs/design/pages.md` 1장의 `/mypage/addresses` · `/mypage/settings` 가 그대로 이번 범위다.

### 우편번호 검색

**다음(카카오) 우편번호 서비스를 실제로 붙인다.** 위젯이 준 값을 그대로 폼에 채우고 **정규화는 하지
않는다**. 버전은 8장에 적었다.

```
[우편번호 찾기] → 위젯 → 우편번호 · 도로명 주소 자동 입력 → 상세주소만 사용자 입력
```

**붙이기로 한 근거와 그 대가.** F6 의 측정 방법이 처음부터 "(스텁)" 이다 — 즉 이 TASK 의 게이트는
위젯이 실제로 뜨는 것을 확인하지 않으며, 확인할 수 있는 게이트도 없다(브라우저가 필요하고, 프론트
테스트는 외부 소켓을 열지 못하게 막혀 있다 — `packages/api-mocks` 의 3중 차단). 그래도 붙이는 것은
"주소를 손으로 타이핑하지 않게 한다"(1장)가 이 화면의 목적 절반이기 때문이다. 대신 대가를 문서에
적는다 — **버전을 고정할 수 없다.** 배포 채널이 `prod` 하나이고 파일명에 버전이 없다(8장).

**그래서 세 가지를 설계로 묶는다.**

| | |
| --- | --- |
| 지연 로드 | 스크립트는 폼을 열 때가 아니라 **검색 버튼을 누를 때** 받는다. 어느 라우트의 First Load JS 에도 들어가지 않는다 |
| 임베드 | 팝업이 아니라 **폼 안 컨테이너에 임베드**한다. 스크립트를 그때 받으니 `window.open` 이 클릭 제스처에서 떨어져 팝업 차단기를 탄다 — 그리고 막힌 팝업과 고장난 버튼은 사용자가 구분할 수 없다 |
| 주입 가능한 이음매 | 로더는 `AddressForm` 의 prop 이고 기본값이 실물이다. 검사는 모듈 경계에서 대체해 라우트를 앱과 똑같이 조립한 채로 위젯의 행동만 정한다 |

**폴백이 필수인 이유**: 외부 스크립트다. 차단되거나 죽으면 배송지를 아예 추가할 수 없게 되고, 그러면
주문도 못 한다. 로드 실패·타임아웃을 감지하면 세 필드를 직접 입력 가능한 상태로 바꾼다(F6b). 폴백은
예외 경로가 아니라 **기본 경로와 같은 자격**이다 — 스크립트가 없어도 저장까지 갈 수 있어야 한다.

### `/me` 를 부르는 방법 — `ApiClient.request`

`createApiClient` 에는 `/me` 계열 메서드가 **없다.** TASK-0111 은 스키마만 정의하고 클라이언트
메서드를 추가하지 않았고, `packages/shared` 는 이 TASK 의 소유 경로가 아니다.

그래서 이 화면은 `ApiClient.request({ path, schema })` 를 직접 쓰고, **엔드포인트마다 한 번씩만**
`apps/shop/src/lib/profile/client.ts` 에서 이름을 붙인다. 응답 타입은 전부
`packages/shared` 의 zod 스키마에서 나오므로 C1 은 그대로 성립한다 — 메서드가 있는 쪽과 없는 쪽의
차이는 "스키마를 어느 파일에서 한 번 적느냐" 뿐이다.

> **오케스트레이터 판단이 필요한 항목.** `packages/shared` 에 `/me` 메서드를 넣는 것이 옳다면 그것은
> 별도 TASK 이고, 이 파일은 그때 지워진다. 지금 지어내지 않는다.

### 밀도 값의 두 표현

`@shopping/ui` 는 밀도를 `1 | 2 | 3`(`DensityLevel`) 으로, `@shopping/shared` 는
`MINIMAL | STANDARD | MAXIMAL`(`DisplayDensity`) 로 부른다. 전자는 `data-density` 속성이자 CSS 의
입력이고, 후자는 Prisma enum 이다. **둘을 잇는 표는 어느 패키지에도 없고, 이 TASK 가 만든다.**

`apps/shop/src/lib/profile/density-preference.ts` 에 둔다. `packages/ui` 에 두면 컴포넌트
라이브러리가 API 계약을 알게 되고, `packages/shared` 에 두면 API 패키지가 CSS 의 표현을 알게 된다.
밀도 토글을 가진 앱은 `apps/shop` 하나뿐이므로(D-033) 앱이 맞다.

### 밀도 승격

```
비로그인   토글 → localStorage 저장 → 즉시 적용
로그인 직후 localStorage 에 값이 있으면 PATCH /me/preferences 로 1회 승격
이후       서버 값이 단일 출처. 토글은 서버에 저장하고 localStorage 는 캐시로만 유지
```

승격 판단(언제 올릴지)은 **클라이언트의 몫**이다. 서버는 일반 변경과 구분하지 않는다(TASK-0111 4장).

### 절단면 — `packages/shared` 의 zod 스키마

TASK-0111 이 정의한 스키마를 그대로 쓰고, **모킹 응답도 같은 스키마로 만든다.**

| 스키마 | 이 화면에서의 용도 |
| --- | --- |
| `profileResponseSchema` | 프로필 표시. 모킹 응답 `parse` |
| `profileUpdateRequestSchema` | 프로필 폼 클라이언트 검증 (TASK-0017 어댑터) |
| `addressSchema` | 배송지 카드 · 폼의 값 타입. 모킹 목록 `parse` |
| `addressCreateRequestSchema` · `addressUpdateRequestSchema` | 배송지 폼 검증 (우편번호 5자리 규칙 포함) |
| `addressListResponseSchema` | 목록 응답 모킹 |
| `userPreferenceSchema` · `userPreferenceUpdateRequestSchema` | 밀도 · 알림 설정 |

우편번호·전화번호 형식 규칙은 **스키마에만** 있다. 화면에 정규식을 다시 적으면 서버와 어긋난다.

### 실패를 읽는 자리 — `apps/shop` 에 **작은** `api-failure.ts` 를 들인다

TASK-0023 은 `apps/shop` 에 `errors.ts` 를 들이지 않기로 했다. 근거는 "shop 이 마주하는 실패가 둘뿐이라
`Record<UserFacingErrorCode, string>` 을 들이면 **닿는 문장이 하나뿐인 카탈로그**가 생긴다" 였다
(TASK-0023 4장, `docs/HANDOFF.md` 3.3).

**그 전제는 이 TASK 에서 절반만 바뀐다.** 이 화면은 실패를 다섯 가지 만난다 — 400(필드 검증),
401, 404(다른 곳에서 지워진 배송지), **409(기본 배송지 경쟁)**, 5xx. 실패를 **값으로 읽는 것**은
이제 필요하고, **코드 전수 카탈로그**는 여전히 필요하지 않다.

그래서 갈라 놓는다.

| | 넣는 것 | 넣지 않는 것 |
| --- | --- | --- |
| `lib/api-failure.ts` | `apiFailure` · `hasCode` · `quotableRequestId` — 두 콘솔의 것과 같은 판별 | |
| 문구 | 화면이 **실제로 분기하는 코드만** 담은 부분 레코드 (`mypage.errors`) | `Record<UserFacingErrorCode, string>` 전수 카탈로그 |

모르는 코드는 서버의 문장을 그대로 보여준다 — `packages/ui` 의 `serverFieldErrors` 가 이미 그렇게 하고,
빈 오류보다 서버 어휘가 낫다는 판단은 TASK-0117 4.1 의 것이다.

> **합치기는 여전히 이 TASK 의 일이 아니다.** `api-failure.ts` 가 이로써 세 벌이 된다. HANDOFF 3.3 은
> 합치는 일이 "파일 이동이 아니라 보간을 할 것인가 · `hasCode` 를 공통 API 로 둘 것인가 라는 설계
> 결정" 이고 **실제 접점은 TASK-0113 · 0114** 라고 적어 뒀다. 이 파일의 머리말이 나머지 두 벌과
> 무엇이 다른지를 적어, 합칠 때 읽을 것이 남게 한다. **오케스트레이터 판단이 필요한 항목.**

### 역할별 권한

`/mypage/*` 는 로그인한 모든 역할이 자기 계정에 대해 쓴다. 데모 계정도 동일하다 — 데모 사용자가
배송지를 만들어 보는 것이 데모의 목적이다(DECISIONS 2 "데모 데이터 조작").

## 5. 구현 계획

1. 설정 화면 골격 + 프로필 폼 (TASK-0017 어댑터)
2. 밀도 토글 연결 + 승격 로직 (TASK-0018 의 토글 UI 재사용)
3. 알림 수신 스위치
4. 배송지 목록 · 카드 (로딩 / 빈 / 에러)
5. 배송지 폼 + 우편번호 위젯 + 폴백
6. 기본 지정 · 삭제 (확인 다이얼로그)
7. 탈퇴 흐름
8. 밀도 3단계 × 3뷰포트 확인, 모킹 `parse` 테스트, 두 화면 전 상태 axe

## 6. 완료 기준 (Definition of Done)

측정은 2026-09-04, `feature/profile-address-page` 워크트리에서 로컬로 돌렸다.

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 실측 | 충족 |
| --- | --- | --- | --- | --- | --- |
| F1 | 프로필 수정 | 이름 변경 후 저장 | 요청이 `profileUpdateRequestSchema` 를 통과하고 화면에 반영 | 저장 후 상자에 남는 값은 **응답이 답한 값**이다 — `PATCH /me` 가 프로필 전체를 답하므로 그것으로 상태를 갈아 끼운다. 이메일은 상자가 아니라 텍스트라 편집 대상에서 빠진다 (`mypage-settings.spec.tsx` 5건) | [x] |
| F2 | 배송지 목록 | 모킹 3건 로드 | 카드 3개, 기본 1건에 배지 | 카드 3개, 배지 정확히 1개, 그 카드가 `집`. 기본인 카드에는 "기본으로" 버튼이 **없다**(비활성이 아니라 부재) | [x] |
| F3 | 배송지 추가 | 폼 입력 후 저장 | `addressCreateRequestSchema` 통과 요청 1회 | 실제 요청 본문을 가로채 그 스키마로 `parse` 해서 통과. 요청 1회, 목록 4건 | [x] |
| F4 | 기본 지정 | 다른 배송지의 "기본으로" 클릭 | 요청 1회, 배지가 옮겨감 | 배지가 `회사` 로 옮겨가고 배지 총 개수는 여전히 1. 옛 기본이 배지를 유지하지 않는 것은 응답을 끼워 넣지 않고 **다시 읽기** 때문이다 | [x] |
| F5 | 삭제 확인 | 삭제 클릭 | 확인 다이얼로그를 거쳐야 요청 발생 | 취소하면 `DELETE` **0회**, 목록 3건 그대로. 다이얼로그는 지울 대상의 이름과 주소를 적는다 | [x] |
| F6 | 우편번호 검색 | 위젯에서 주소 선택 (스텁) | 우편번호·도로명 자동 입력, 상세주소에 포커스 | 두 칸이 채워지고 포커스가 상세주소 상자로 간다. 포커스 이동은 `fieldIds` 가 만든 id 로 한다 — `Input` 이 ref 를 넘기지 않고, id 를 붙인 주체가 `FormField` 이기 때문 | [x] |
| F6b | 위젯 폴백 | 로더가 reject 하는 상황 | 세 필드를 직접 입력해 저장 성공 | 안내문이 뜨고 그대로 저장까지 간다. **잠긴 적이 없으므로 풀 것도 없다** — 세 칸은 언제나 편집 가능하고 폴백은 안내문 하나가 붙는 것뿐이다 | [x] |
| F7 | 밀도 즉시 반영 | 밀도 토글 3단계 전환 | 즉시 렌더 + 저장 요청 1회 | 클릭 직후 `data-density` 가 `3`, 이어서 서버 값이 `MAXIMAL`. 저장이 실패해도 **되돌리지 않고** "이 기기에는 남아 있다" 고 알린다 | [x] |
| F8 | 밀도 승격 | localStorage 에 `3`(맥시멀)을 둔 채 로그인 | 승격 요청 1회, 이후 서버 값 사용 | 계정이 `STANDARD` 인 상태에서 렌더하면 `MAXIMAL` 로 바뀐다. 저장된 값이 없으면 `PATCH /me/preferences` **0회** — 이 분기가 없으면 로그인마다 계정을 기본값으로 덮어쓴다 | [x] |
| F9 | 알림 설정 | 마케팅 수신 켜기·주문 수신 끄기 | 해당 필드만 담은 요청 1회 | 누른 칸만 바뀌고 나머지 둘은 `DEFAULT_USER_PREFERENCE` 그대로. 셋을 다 보내면 만지지 않은 값을 마지막으로 읽은 값으로 덮어쓴다 | [x] |
| F10 | 탈퇴 | 확인 후 탈퇴 | 확인 없이는 요청 0회, 확인 후 1회 + 결과 안내 | 확인 문구 없이 확인 버튼을 눌러도 `DELETE /me` **0회**. 문구를 입력하면 진행되고 결과에 **서버가 센 수**(지운 배송지 3, 끊은 세션 2)가 나온다. 실패하면 다이얼로그가 열린 채 남는다 | [x] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 이 TASK 는 **화면만 만드는 프론트 TASK** 이므로
1장 · 2장 · 5장(C1·C2) · 7장을 받는다.

| 장 | 적용 | 비고 |
| --- | --- | --- |
| 1장 코드 게이트 | Q1~Q4 · Q6 · Q7 전부 | |
| Q5 테스트 충실도 | **커버리지 수치 없음** → 아래 U 목록 | 대역: **백엔드 모킹.** 우편번호 위젯도 스텁 |
| 2장 화면 게이트 | **P2~P6** — `apps/shop` 이므로 **P6 적용** | **P1(LCP)은 게이트가 아니다** — 아래 참조 |
| **3장 API 게이트** | **해당 없음** | 엔드포인트를 추가하지 않는다. API 는 TASK-0111 |
| **4장 데이터 게이트** | **해당 없음** | `schema.prisma` 무변경 |
| 5장 계약 게이트 | **C1 · C2** | C3 는 TASK-0111 |
| 7장 문서 게이트 | D1~D5 | |

> **P1 이 표에서 빠진 이유.** 이 문서를 쓴 2026-09-03 에는 LCP 2.5s 가 화면 게이트였다. 그 뒤
> **D-217** 이 절대값·증분 두 기준을 차례로 폐기하고 성능을 "조사 도구" 로 옮겼다
> ([QUALITY-GATES 2장](../QUALITY-GATES.md)) — 화면 TASK 가 만든 회귀는 한 번도 잡히지 않았고
> 걸린 것은 전부 앱 셸의 기존 부채였기 때문이다. 대신 **무거운 것을 새로 들이면 신고한다.**

| # | 기준 | 측정 | 실측 |
| --- | --- | --- | --- |
| Q1 | 타입 검사 | `pnpm typecheck` | error 0 (18.9초) |
| Q2 | 린트 | `pnpm lint` | error 0, warning 0 (34.3초) |
| Q3 | 빌드 | `pnpm build` | 전 앱 성공 (17.1초). `/mypage/settings` · `/mypage/addresses` 가 **정적 라우트**로 생성된다 |
| Q4 | 테스트 | `pnpm test` | **3,103 통과 / 1 skip, 실패 0** (110~150초). 아래 「전체 실행의 흔들림」 참조 |
| Q5 | 테스트 충실도 | 화면 레이어 | 커버리지 수치 없음. 상호작용 목록은 아래 |
| P2 | 접근성 | 두 화면 전 상태에 axe (`wcag2a`·`wcag2aa`·`wcag21a`·`wcag21aa`·`best-practice`) | **violation 0** — 설정 화면 9상태(로딩·비로그인·읽기 실패·정상·필드 오류·탈퇴 다이얼로그·밀도 3단계) + 배송지 9상태(3건·빈·읽기 실패·폼 열림·폴백·삭제 확인·밀도 3단계). 검사 중 `heading-order` 위반을 **실제로 한 건 잡았다**(카드 제목이 `h3` 이라 `h1` 다음 단계를 건너뛰었다) |
| P3 | 반응형 | 360 / 768 / 1440px | 세 폭에서 배송지 목록이 같은 트리로 렌더된다. jsdom 은 레이아웃을 계산하지 않으므로 **이것이 자동으로 잴 수 있는 절반**이다 — 나머지 절반의 근거는 두 화면에 뷰포트 분기가 하나도 없다는 것이다(분기를 갖는 것은 헤더뿐, D-055) |
| P4 | 키보드 조작 | Tab 순회 · Enter · Escape | 탈퇴는 트리거 → 다이얼로그까지 키보드만으로 도달하고 **초기 포커스가 닫기 버튼**이라 잘못 누른 Enter 가 파괴하지 않는다. Escape 로 닫힌다. 배송지 폼은 입력·스위치·버튼 전부 탭으로 도달 |
| P5 | 상태 표현 | 로딩 / 빈 / 에러 | 두 화면 모두 구현. 읽기 실패는 빈 목록이 아니라 오류로 그린다 |
| P6 | 밀도 3단계 | 두 화면에서 단계 전환 | **6조합 전부 렌더**(화면 2 × 밀도 3) + 밀도 3단계 각각에서 axe violation 0 |
| U1 | 조건부 렌더 | 로딩·빈·에러·정상 | 4상태 전부. 배송지 목록은 `DataList` 가 로딩·빈·에러를 기본값 없는 필수 prop 으로 요구해 **빠뜨리면 컴파일되지 않는다** |
| U2 | 폼 검증 오류 | 우편번호 4자리 / 이름 공백 / 아바타가 URL 이 아님 | 해당 필드에 표시되고 `aria-invalid` · `aria-describedby` 로 연결된다. 규칙은 `packages/shared` 의 스키마에서 `safeParse` 로 빌려오고 문장만 앱의 것이다 |
| U3 | 중복 클릭 차단 | 같은 틱에 두 번 클릭 | 요청 1회 (프로필 저장·배송지 추가 양쪽). `userEvent` 는 이벤트 사이에 마이크로태스크를 흘려 첫 요청이 끝나 버리므로 `fireEvent` 로 같은 틱에 보낸다 — `useForm` 의 가드가 state 가 아니라 ref 인 이유가 정확히 그 상황이다 |
| U4 | 밀도 3단계 렌더 | 밀도 3값 × 화면 2 | 6조합 통과 |
| U5 | 키보드 조작 | 마우스 없이 배송지 추가 | **폴백 경로로** 성공 — 위젯이 죽은 상태에서 추가 버튼부터 저장까지 전부 키 입력 |
| U6 | 서버 오류 표시 | 500 모킹 | 오류가 뜨고 **입력값이 남는다**(프로필·배송지 양쪽). 500 에만 문의 번호가 붙는다 |
| C1 | 스키마 단일 출처 | 앱 코드 검색 | `apps/shop` 에 `/me` 응답 타입을 다시 정의한 곳 **0건**. 경로와 스키마가 만나는 곳도 `lib/profile/client.ts` **한 파일**뿐이다 |
| C2 | 모킹 검증 | 모킹 응답 전부를 zod 로 `parse` | 전부 통과. 대역의 모든 응답이 `defineFixture` 를 지나고 `registry.spec.ts` 가 픽스처 파일을 디스크에서 훑는다. **이 게이트가 실제로 드리프트를 한 건 잡았다** — 대역이 `DELETE /me/addresses/:id` 에 `isDefault: false` 를 답하고 있었는데 실제 API 는 지운 행을 있던 그대로 돌려준다 |

**새로 들인 것.** 런타임 의존성은 **다음(카카오) 우편번호 v2 스크립트 하나**이고, 배송지 폼의
검색 버튼을 눌렀을 때 받는다 — 폼을 열 때도, 페이지를 그릴 때도 아니다. 따라서 **어느 라우트의
First Load JS 에도 들어가지 않는다.** `zod` 를 `apps/shop` 의 의존성으로 추가했지만 저장소의 다른
네 패키지가 이미 같은 판(`^4.5.4`)을 쓰므로 lockfile 에 새로 받는 것은 없다. `next build` 가 이
설정에서 First Load JS 열을 출력하지 않아 옮겨 적을 수치가 없는 것은 TASK-0023 때와 같다.

**전체 실행의 흔들림 — 이 브랜치가 만든 것이 아니다.** `pnpm test` 5회 중 3회 전부 초록,
2회는 실패했고 실패한 파일이 매번 달랐다(`apps/admin/test/attributes-error-contract.spec.tsx`,
`apps/api/test/api/session-performance.spec.ts`). 둘 다 이 브랜치가 건드리지 않은 파일이고
**단독 실행에서는 항상 통과**한다 — 이 기계에서 6개 패키지를 동시에 돌리면 포화된다는 뜻이다.
`packages/api-mocks` 변경이 원인일 가능성은 직접 쟀다: 가장 자주 걸리는 admin 검사를 `/me` 대역이
등록된 채로 3회(1148 · 857 · 816ms), 등록을 뺀 채로 3회(1345 · 1277 · 941ms) 재면 **오히려 더
빠르고** 두 조건의 차이보다 같은 조건의 편차가 크다. 65ac82e 가 같은 진단을 남긴 그 종류다.

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태를 `완료` 로 변경 + 인덱스 2곳 갱신 | [x] 상태만. **인덱스는 오케스트레이터** — `docs/tasks/README.md` 와 `M04-auth/README.md` 는 작업자가 건드리지 않는다(CLAUDE.md 2장) |
| D2 | `docs/design/pages.md` 갱신 | [x] 밀도 절에 토글이 두 곳에 있다는 것과 서버 값을 클라이언트가 읽는 이유, 마이페이지 절에 「계정 화면 둘」 신설 |
| D3 | 결정 변경 없음 확인 | [x] 새 결정 없음. D-217(성능은 게이트가 아니다)과 D-208(화면·API 분할)을 **따랐을 뿐** 바꾸지 않았다 |
| D4 | 새 환경변수 | [x] **없다.** 우편번호 위젯은 키를 요구하지 않아 `.env.example` 무변경 |
| D5 | 새 라이브러리 버전 기록 | [x] 8장. 우편번호 위젯은 **버전을 고정할 수 없다**는 사실 자체를 값으로 적었다 |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 우편번호 위젯이 외부 스크립트에 의존한다 | 로드 실패 시 수동 입력 폴백을 **필수**로 구현한다(F6b). 테스트에서는 위젯을 스텁으로 대체한다 |
| R2 | 밀도 승격이 로그인 직후 여러 번 발생할 수 있다 | 승격은 세션당 1회로 표시(플래그)하고, 실패해도 화면 동작을 막지 않는다 |
| R3 | ~~프로필의 "연락처" 필드가 스키마에 없다~~ | **해소** — TASK-0111 이 `profileUpdateRequestSchema` 를 이름·아바타로 확정하며 닫았다. "`User` 에 컬럼이 없고, 주문서가 실제로 쓰는 값은 배송지의 수령인 번호이므로 계정에 사본을 두면 즉시 어긋난다"(TASK-0111 R1). 프로필 폼은 연락처를 다루지 않고 **배송지 폼에서만** 입력한다 |
| R4 | 탈퇴가 되돌릴 수 없다 | 확인 다이얼로그에 "무엇이 지워지고 무엇이 남는가"를 명시한다 — 배송지는 삭제, 주문 이력은 보존. 응답(`withdrawalResponseSchema`)이 지운 배송지 수·끊은 세션 수를 답하므로 **결과 안내는 추측이 아니라 서버가 센 수**다 |
| R5 | `createApiClient` 에 `/me` 메서드가 없다 | 지어내지 않는다. `ApiClient.request` + 공유 스키마로 부르고, 엔드포인트마다 이름은 앱 안에서 한 번만 붙인다(4장). `packages/shared` 확장은 별도 TASK — **오케스트레이터 판단** |
| R6 | `api-failure.ts` 가 세 벌이 된다 | 합치는 것은 HANDOFF 3.3 이 TASK-0113 · 0114 로 지목한 설계 결정이다. 이 TASK 는 shop 쪽만 **부분 레코드로** 들이고 머리말에 차이를 적는다(4장) |

## 8. 확정된 버전

| 패키지 | 버전 |
| --- | --- |
| 다음(카카오) 우편번호 서비스 v2 | `//t1.daumcdn.net/mapjsapi/bundle/postcode/prod/postcode.v2.js` — **버전 고정 불가** |

**"버전 고정 불가" 를 값으로 적는 이유.** D5 는 "새로 도입한 라이브러리 버전을 기록" 하라고 한다.
이 스크립트에는 적을 버전이 없다 — 배포 채널이 `prod` 하나이고 파일명이 `postcode.v2.js` 로 고정되어
제공자가 언제든 내용을 바꾼다. 빈칸으로 두면 "아직 안 정했다" 로 읽히므로, **고정할 수 없다는 사실
자체**를 값으로 적는다. 이것이 F6b(수동 입력 폴백)를 선택이 아니라 필수로 만드는 근거이기도 하다.

**키·환경변수 없음.** 이 위젯은 API 키를 요구하지 않으므로 `.env.example` 은 바뀌지 않는다(D4).
스크립트 주소는 코드 상수(`apps/shop/src/lib/profile/postcode.ts`)이고, 테스트는 로더를 스텁으로
대체하므로 어떤 게이트도 외부 호스트에 닿지 않는다.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-03 | 최초 작성. D-208 에 따라 TASK-0027 에서 분할. 스토어 설정 화면은 TASK-0109 로 이관 |
| 2026-09-04 | 구현 착수 전 정정. ① **P1(LCP)을 게이트에서 뺐다** — D-217 이후 성능은 게이트가 아니라 조사 도구다(6.2). ② 우편번호 위젯을 **붙이기로 확정**하고 근거·대가·버전을 4장 · 8장에 적었다. ③ `createApiClient` 에 `/me` 메서드가 없다는 사실과 `request` 로 부르는 방법을 4장에 적었다(R5). ④ `DensityLevel` ↔ `DisplayDensity` 매핑의 소유자를 정했다(4장). ⑤ `apps/shop` 의 실패 읽기 자리를 정했다(4장, R6). ⑥ R3 을 TASK-0111 의 결론으로 닫았다 |
| 2026-09-04 | 구현 완료. 게이트 4개 통과, 6장을 실측으로 채웠다. `docs/tasks/README.md` · `M04-auth/README.md` 인덱스는 오케스트레이터의 몫이다 |
