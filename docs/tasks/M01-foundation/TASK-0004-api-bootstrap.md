# TASK-0004: API 부트스트랩

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M01 기반 구축 |
| 상태 | 승인 대기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/api-bootstrap` |
| 선행 작업 | TASK-0002, TASK-0003 |

## 1. 목적

NestJS API 를 띄우고, 환경변수 검증과 헬스체크를 붙인다. 이후 모든 백엔드 작업이 여기에 얹힌다.

## 2. 범위

### 포함
- NestJS 애플리케이션 구성 (`apps/api`)
- **부팅 시 env 스키마 검증** (zod) — 누락·형식 오류 시 프로세스 종료
- `GET /health` — API 자신과 Meilisearch 연결 상태 반환 (DB 는 TASK-0005 에서 추가)
- 전역 예외 필터, 응답 포맷 통일, 요청 로깅
- CORS 설정 (세 웹 앱 오리진 허용)
- API 프리픽스 및 버전 규약 (`/api/v1`)

### 제외
- Prisma·DB 연결 (TASK-0005)
- 인증 (M04)
- 도메인 모듈 일체

## 3. 요구사항

### 기능 요구사항
- [ ] `pnpm --filter @shopping/api dev` 로 API 가 뜬다
- [ ] `GET /api/v1/health` 가 `{ status, search, uptime, version }` 을 반환한다
- [ ] Meilisearch 가 죽어 있으면 `search: "down"` 을 반환하되 API 자체는 200 을 유지한다
- [ ] 필수 환경변수 누락 시 **부팅 시점에** 누락 변수명을 포함한 메시지와 함께 종료한다
- [ ] 처리되지 않은 예외가 통일된 에러 응답 포맷으로 변환된다
- [ ] 허용되지 않은 오리진의 요청은 CORS 로 차단된다

### 비기능 요구사항
- 에러 응답에 스택 트레이스가 노출되지 않는다 (개발 환경 제외)
- 로그에 환경변수 값이 찍히지 않는다

## 4. 설계

```
apps/api/src/
├── main.ts
├── app.module.ts
├── config/         env 스키마 정의와 검증
├── common/         예외 필터, 응답 인터셉터, 로거
└── health/         헬스체크 모듈
```

### API

| 메서드 | 경로 | 응답 |
| --- | --- | --- |
| GET | `/api/v1/health` | `{ status, search, uptime, version }` |

### 에러 응답 포맷

```jsonc
{ "error": { "code": "VALIDATION_FAILED", "message": "...", "details": [] } }
```

## 5. 구현 계획

1. NestJS 스캐폴딩 + `packages/config` 설정 연결
2. env 스키마(zod) 정의와 부팅 시 검증
3. 전역 예외 필터 / 응답 인터셉터 / 로거
4. CORS 설정
5. health 모듈 + Meilisearch 연결 확인
6. health 단위 테스트

## 6. 완료 기준 (Definition of Done)

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 기동 | `pnpm --filter @shopping/api dev` | 지정 포트에서 응답 | [ ] |
| F2 | 헬스체크 | `curl localhost:4000/api/v1/health` | 200, `search: "ok"` | [ ] |
| F3 | 검색엔진 장애 격리 | Meilisearch 중지 후 헬스체크 | 200 유지, `search: "down"` | [ ] |
| F4 | env 누락 차단 | `DATABASE_URL` 제거 후 기동 | 누락 변수명 출력 후 종료 코드 ≠ 0 | [ ] |
| F5 | 에러 포맷 | 존재하지 않는 경로 요청 | 통일 포맷 JSON, 스택 미노출 | [ ] |
| F6 | CORS | 허용되지 않은 오리진에서 요청 | 브라우저에서 차단됨 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:

- **Q5(커버리지) 면제** — M05 부터 적용
- **Q6~Q7 해당 없음** — TASK-0007 에서 구축
- **3장 API 게이트**: A1(응답시간) 적용. A2~A4 는 인증 도입(M04) 이후 적용
- **4장(데이터) 해당 없음** — 스키마 없음

### 6.3 성능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| P1 | 기동 시간 | 실행부터 첫 응답까지 | 10초 이내 | [ ] |
| P2 | 헬스체크 응답 | 로컬 100회 호출 p95 | 100ms 이하 | [ ] |

### 6.4 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D3 | 새 환경변수를 `.env.example` 에 반영 | [ ] |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D5 | 확정 버전 기록 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | pnpm workspace 에서 NestJS 빌드 시 의존성 해석 실패 | 발생 시 해당 앱만 hoist 하거나 `node-linker` 조정 |

## 8. 확정된 버전

| 패키지 | 버전 |
| --- | --- |
| @nestjs/core | |
| meilisearch (client) | |

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
