# TASK-0059: 주문 상태 머신 구현

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M09 배송·주문관리 |
| 상태 | 승인 대기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/order-state-machine` |
| 선행 작업 | M08 완료 |

## 1. 목적

`SellerOrder` 상태 전이를 코드로 구현한다. 정의되지 않은 전이가 불가능하게 만든다.

## 2. 범위

### 포함
- 상태 전이 정의 테이블 (from → to, 허용 주체, 조건)
- 전이 실행 서비스 — 검증 → 상태 변경 → 이력 기록 → 후속 처리 트리거
- `OrderStatusHistory` 기록 (이전/이후·사유·주체)
- 권한별 허용 전이 (구매자·판매자·관리자·시스템)
- 부분 취소된 항목이 있을 때의 상태 판단
- 상태 변경 시 알림 이벤트 발행 자리 (M13 연결)
- 상태별 가능 액션 조회 API (화면이 버튼을 결정하는 근거)

### 제외
- 화면 (TASK-0060, 0063)
- 클레임 상태 (M10)

## 3. 요구사항

- [ ] 정의되지 않은 전이는 거부된다
- [ ] 권한 없는 주체의 전이는 거부된다
- [ ] 모든 전이가 이력으로 남는다
- [ ] 화면이 "지금 가능한 액션"을 API 로 알 수 있다
- [ ] 동시에 같은 전이가 두 번 요청돼도 한 번만 반영된다

## 4. 설계

상태 정의는 `docs/design/state-machines.md` 1장을 따른다.

```ts
transition(sellerOrderId, to, { actor, reason })
  ① 현재 상태 조회 (행 잠금)
  ② 전이 정의 확인 — 없으면 거부
  ③ 주체 권한 확인
  ④ 조건 확인 (예: SHIPPED 는 운송장 필요)
  ⑤ 상태 변경 + 이력 기록 (한 트랜잭션)
  ⑥ 후속 이벤트 발행
```

| 전이 | 허용 주체 |
| --- | --- |
| PAID → PREPARING | 판매자, 시스템(자동) |
| PREPARING → SHIPPED | 판매자 |
| SHIPPED → DELIVERED | 시스템(시뮬레이터), 판매자 |
| DELIVERED → CONFIRMED | 구매자, 시스템(D+7) |
| * → CANCELED | 클레임 처리 결과 (M10) |

## 5. 구현 계획

1. 전이 정의 테이블 (코드 상수)
2. 전이 서비스 + 검증
3. 이력 기록
4. 권한 확인
5. 가능 액션 조회 API
6. 이벤트 발행 자리
7. 전이 매트릭스 전수 테스트

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 정상 전이 | PAID → PREPARING → SHIPPED | 순차 성공 | [ ] |
| F2 | 잘못된 전이 | PAID → DELIVERED 시도 | 거부 | [ ] |
| F3 | 권한 | 구매자가 SHIPPED 로 전이 시도 | 403 | [ ] |
| F4 | 조건 | 운송장 없이 SHIPPED 시도 | 거부 | [ ] |
| F5 | 이력 | 전이 5회 후 조회 | 이력 5건, 주체·사유 포함 | [ ] |
| F6 | 동시성 | 같은 전이 동시 2회 | 1회만 반영 | [ ] |
| F7 | 가능 액션 | 각 상태에서 조회 | 정의와 일치하는 목록 반환 | [ ] |
| F8 | 전수 검증 | 전체 상태 × 전체 전이 조합 | 정의된 것만 성공 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5 강화** — 전이 매트릭스 분기 커버리지 100%
- **2장 해당 없음**
- **3~4장 전 항목 적용**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 구현과 `docs/design/state-machines.md` 1장 일치 확인. 어긋나면 **문서를 먼저 고친다** | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 전이 정의가 코드·문서 두 곳에 존재해 어긋남 | 코드의 정의 테이블에서 문서용 표를 생성하는 스크립트 검토 |
| R2 | 부분 취소 시 상태 판단이 모호 | 남은 항목 기준으로 유지하고 항목별 상태를 별도로 둔다 (M10 에서 확정) |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
