# TASK-0014: 디자인 토큰 · 밀도 3단계

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M03 디자인 시스템 |
| 상태 | 승인됨 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/design-tokens` |
| 선행 작업 | **M01 완료** (원래 M02 로 적혀 있었으나 실제 의존은 M01 의 Tailwind 공통 프리셋뿐이다) |

## 1. 목적

색·타이포·간격·radius 를 토큰으로 정의하고, **밀도 3단계를 CSS 변수 세트 3벌**로 만든다. 이후 모든 컴포넌트가 토큰만 참조하게 한다.

## 2. 범위

### 포함
- 색 토큰 (중립 계열 중심, 시맨틱 색: primary / danger / success / muted)
- 타이포 스케일, 폰트 스택 (한글 우선)
- 간격·radius·shadow·border 토큰
- **밀도 3단계 변수 세트** — 간격 배율, 폰트 배율, 그리드 열 수, 컴포넌트 패딩
- **밀도 × 브레이크포인트 매트릭스** — 열 수와 배율은 뷰포트에 따라 달라진다
- **터치 타깃 하한** — 밀도 배율과 무관하게 인터랙티브 요소 최소 44×44px
- 밀도 컨텍스트 (`data-density="1|2|3"` 을 root 에 부여)
- 밀도 상태 관리 훅 (localStorage 저장, 서버 값 연동 자리)
- Tailwind 프리셋에 토큰 연결
- 토큰 확인용 프리뷰 페이지

### 제외
- 실제 컴포넌트 (TASK-0015)
- 다크 모드 (범위 외)

## 3. 요구사항

- [ ] 하드코딩된 색·간격 값 없이 토큰만으로 스타일을 작성할 수 있다
- [ ] `data-density` 값을 바꾸면 간격·폰트·그리드가 즉시 바뀐다
- [ ] 세 앱이 같은 토큰을 공유하되 콘솔은 밀도 고정(2단계)이다
- [ ] 열 수와 배율이 뷰포트에 따라 달라진다
- [ ] 어떤 밀도에서도 버튼이 44px 미만이 되지 않는다
- [ ] 새로고침해도 선택한 밀도가 유지된다

## 4. 설계

**밀도만으로 열 수를 정하면 안 된다.** 360px 에서 맥시멀 6열이면 카드 하나가 55px 라 성립하지 않는다. 열 수와 배율은 **밀도 × 브레이크포인트 2차원**이다.

```css
/* 모바일 기본 */
[data-density="1"] { --space-unit: 5px; --font-scale: 1.02; --grid-cols: 1; }
[data-density="2"] { --space-unit: 4px; --font-scale: 1;    --grid-cols: 2; }
[data-density="3"] { --space-unit: 3.5px; --font-scale: .98; --grid-cols: 2; }

@media (min-width: 768px) {
  [data-density="1"] { --space-unit: 6px; --font-scale: 1.05; --grid-cols: 2; }
  [data-density="2"] { --space-unit: 4px; --font-scale: 1;    --grid-cols: 3; }
  [data-density="3"] { --space-unit: 3px; --font-scale: .95;  --grid-cols: 4; }
}
@media (min-width: 1280px) {
  [data-density="1"] { --grid-cols: 3; }
  [data-density="2"] { --grid-cols: 4; }
  [data-density="3"] { --grid-cols: 6; }
}
```

모바일에서는 **세 단계의 배율 차이를 데스크톱보다 좁힌다.** 좁은 화면에서 미니멀의 넓은 여백은 낭비이고, 맥시멀의 축소 배율은 글자를 못 읽게 만든다.

**터치 타깃**: `--font-scale` 이 줄어도 인터랙티브 요소는 최소 44×44px 를 유지한다. 시각적으로 작아 보이더라도 패딩으로 터치 영역을 확보한다.

```css
--touch-min: 44px;   /* 밀도와 무관한 고정값 */
```

전체 반응형 전략은 `docs/design/pages.md` 참조.

| 단계 | 성격 | 레퍼런스 |
| --- | --- | --- |
| 1 미니멀 | 넓은 여백, 큰 이미지, 정보 최소 | SSENSE, 아페쎄 |
| 2 표준 | 균형 | 무신사, 29CM |
| 3 맥시멀 | 조밀, 정보 최대 | 쿠팡, 알리익스프레스 |

**정보의 표시 여부**는 토큰이 아니라 컴포넌트가 밀도 값을 읽어 결정한다. 토큰은 공간·크기만 담당한다.

## 5. 구현 계획

1. 색·타이포·간격 토큰 정의
2. 밀도 3단계 변수 세트
3. Tailwind 프리셋 연결
4. 밀도 컨텍스트·훅 (localStorage)
5. 토큰 프리뷰 페이지
6. 세 앱에 프리셋 적용, 콘솔은 밀도 고정

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 토큰 적용 | 프리뷰 페이지 확인 | 색·타이포·간격 전 토큰 렌더 | [ ] |
| F2 | 밀도 전환 | 토글 조작 | 3단계 모두 간격·폰트·열 수 변화 | [ ] |
| F3 | 지속성 | 밀도 변경 후 새로고침 | 선택값 유지 | [ ] |
| F4 | 콘솔 고정 | seller/admin 접속 | 밀도 2단계 고정, 토글 없음 | [ ] |
| F5 | 하드코딩 금지 | `grep` 으로 hex·px 리터럴 검색 | 토큰 정의 파일 외 0건 | [ ] |
| F6 | 대비 | 텍스트/배경 조합 검사 | WCAG AA (4.5:1) 충족 | [ ] |
| F7 | 밀도 × 뷰포트 | 밀도 3 × 뷰포트 3 = 9조합 | 열 수가 매트릭스와 일치 | [ ] |
| F8 | 모바일 맥시멀 | 360px 맥시멀 | 2열, 카드 폭 150px 이상 | [ ] |
| F9 | 터치 타깃 | 맥시멀 단계 버튼 측정 | 44×44px 이상 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5(커버리지) 면제** — M05 부터 적용. 단 밀도 훅은 단위 테스트 필수
- **2장 화면 게이트**: P2(접근성)·P3(반응형) 적용. P1(LCP)·P5·P6 은 실제 화면부터
- **3~4장 해당 없음**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 밀도 3단계 규격을 `docs/design/pages.md` 에 실제 값으로 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 밀도 전환 시 레이아웃이 깨지는 조합 | 9조합(밀도 3 × 뷰포트 3)을 Storybook 툴바로 매번 확인 |
| R3 | 2차원 매트릭스로 토큰 정의가 복잡해짐 | 매트릭스는 열 수·배율에만 적용한다. **표시 항목은 밀도 1차원으로 유지**해 조합 폭발을 막는다 |

## 8. 확정된 버전

| 패키지 | 버전 |
| --- | --- |
| tailwindcss | |

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
