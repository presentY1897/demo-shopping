# TASK-0050: 주문서 화면

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M07 장바구니·주문 |
| 상태 | 완료 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/checkout-page` |
| 선행 작업 | TASK-0049 |

## 1. 목적

주문서 화면을 만든다. **진입 시 재고가 예약되고, 결제 직전까지의 흐름이 완성**된다.

## 2. 범위

### 포함
- 주문 항목 확인 (판매자별 그룹)
- 배송지 선택·신규 입력 (TASK-0111 의 배송지 API · `addressSchema` 재사용)
- 배송 요청사항
- 금액 요약 (상품금액·할인·배송비·총액)
- 쿠폰·적립금 영역 **자리** (M11 연결)
- 결제수단 선택 **자리** (M08 연결)
- **진입 시 재고 예약 + 남은 시간 표시**
- 예약 만료 시 안내·재시도
- 약관 동의
- 주문하기 → 주문 생성 → 결제 페이지 이동
- 이탈 시 예약 해제

### 제외
- 실제 결제 (M08)
- 쿠폰·적립금 적용 (M11)

## 3. 요구사항

- [ ] 주문서에 진입하면 재고가 예약된다
- [ ] 남은 시간이 표시되고 만료되면 안내가 나온다
- [ ] 페이지를 벗어나면 예약이 해제된다
- [ ] 금액이 장바구니와 일치한다
- [ ] 배송지를 선택하거나 새로 입력할 수 있다

## 4. 설계

```
장바구니 → [주문하기] → 주문서 진입
                         ├ 재고 예약 (15분)
                         ├ 타이머 표시
                         └ 이탈(beforeunload/라우트 변경) → 예약 해제 요청
```

**예약 해제를 이탈 시 시도하되 의존하지 않는다.** 브라우저를 강제 종료하면 신호가 안 온다. 만료 스케줄러(TASK-0051)가 최종 안전망이다.

타이머는 남은 시간이 3분 이하일 때 강조한다. 결제 중 만료되면 사용자가 당황하므로 미리 알린다.

### 4.1 주문서는 **표가 아니라 예약 묶음**이다 (2026-09-05)

4장의 흐름은 「주문서 진입 → 재고 예약」인데, TASK-0049 는 예약을 **주문 생성
안에서** 잡았다. 그 TASK 에 주문서가 없었기 때문이고, 승인된 4장 ③ 이 그렇게 적혀
있었다. 이제 주문서가 생기므로 예약이 제자리로 온다.

**새 표를 만들지 않는다.** 주문서 하나는 곧 **같은 `checkoutId` 를 가진 `HELD`
예약들**이다(TASK-0048 4.1 이 그 컬럼을 만들 때 이미 그 모양이었다). 그 행들이
`variantId` 와 `quantity` 를 들고 있고, 주문서가 그려야 하는 나머지 — 상품명 · 옵션 ·
사진 · 브랜드 · 가격 — 는 전부 `ProductVariant` 에서 따라간다. 표를 하나 더 만들면
같은 사실이 두 곳에 살고, 둘이 갈리는 날 어느 쪽이 맞는지 아무도 모른다.

그래서 **4장은 여전히 해당 없다** — 스키마가 바뀌지 않는다.

| 라우트 | 하는 일 |
| --- | --- |
| `POST /checkouts` | 고른 장바구니 줄로 예약을 잡고 주문서 id 를 낸다 |
| `GET /checkouts/:id` | 그 예약들로 주문서를 다시 그린다. 만료됐거나 풀렸으면 **404** |
| `DELETE /checkouts/:id` | 이탈. 그 시도의 예약 전부를 푼다 |

410 이 아니라 404 인 이유는 **만료와 해제를 화면이 구분할 이유가 없기** 때문이다. 둘 다
「이 주문서는 이제 없다」이고, 사람이 할 일은 장바구니로 돌아가는 것 하나다. 구분하려면
풀린 예약을 계속 들고 있어야 하는데, 그것은 없는 것을 기억하는 표를 만드는 일이다.

**주문서를 만드는 것은 장바구니의 「주문하기」다.** 화면이 `/checkout` 으로 먼저
가서 그 자리에서 예약하는 방법도 있지만, 그러면 새로고침 한 번에 예약이 한 벌 더
잡힌다. 장바구니가 만들고 `/checkout/{id}` 로 보내면, 새로고침은 **같은 주문서를
다시 읽는** 일이 된다.

### 4.2 3장이 적용된다 (2026-09-05 정정)

6.2 가 「3~4장 해당 없음」인데 4.1 이 엔드포인트 셋을 더한다. 4장은 그대로 해당
없고(스키마 변경이 없다), **3장은 전 항목 적용된다** — 남의 주문서를 읽거나 푸는
것이 표현 가능해졌으므로 A3·A4 가 특히 그렇다.

### 4.3 주문 생성은 이미 잡힌 예약을 **쓴다**

`POST /orders` 가 `checkoutId` 를 받는다. 오면 그 예약을 그대로 쓰고 새로 잡지
않는다 — 두 번 잡으면 한 사람이 같은 물건을 두 몫 잠근다.

`itemIds` 로 부르는 길도 남는다. 주문서를 거치지 않는 주문(API 직접 호출)이 여전히
성립해야 하고, 그 길에서 예약을 안 잡으면 오버셀이 뚫린다. **지켜야 하는 성질은
「주문에는 그것을 덮는 `HELD` 예약이 있다」이지 「예약이 어디서 잡혔나」가 아니다.**

### 4.4 이탈 해제는 **시도하되 의존하지 않는다**

4장이 이미 적어 둔 것이고, 여기서 지키는 방식은 `sendBeacon` 이다 — 페이지가 사라지는
중에 보내는 `fetch` 는 브라우저가 취소한다. 그래도 강제 종료에는 신호가 없고, 그때의
안전망은 만료 스케줄러(TASK-0051)다.

### 4.5 쿠폰·적립금·결제수단은 **자리만** 둔다

범위가 그렇게 적혀 있다. 자리를 「준비 중」으로 비워 두는 대신 **무엇이 들어올지**를
적는다 — 빈 상자는 만들다 만 화면으로 보이고, 이름이 붙은 빈 상자는 아직 안 온
기능으로 보인다.

### 4.6 「결제 단계로 이동」할 곳이 아직 없다

F7 이 「주문 생성 후 결제 단계로 이동」인데 결제는 M08 이고 `/checkout/complete` 도
아직 비어 있다. 없는 라우트로 보내면 404 이고, 그 자리에 또 껍데기를 만들면
**이번 TASK 가 없앤 껍데기를 다시 만드는 것**이 된다.

그래서 주문이 접수되면 **주문서가 그 자리에서 접수 상태로 바뀐다** — 주문번호와
「결제는 다음 단계」가 적힌다. M08 이 그 자리에 결제 위젯을 붙이거나
`/checkout/complete` 로 보내면 된다.

이때 예약은 **풀지 않는다.** 그 재고는 이제 주문의 것이고, 확정하는 것은 결제다.

## 5. 구현 계획

1. 주문서 데이터 로딩 + 재고 예약
2. 항목·금액 요약 표시
3. 배송지 선택·입력
4. 타이머 + 만료 처리
5. 쿠폰·결제수단 자리 배치
6. 약관 동의 + 주문 생성 연결
7. 이탈 시 해제

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 실측 | 충족 |
| --- | --- | --- | --- | --- | --- |
| F1 | 진입 시 예약 | 주문서를 열고 재고를 읽는다 | 가용재고 감소 | 「주문하기」가 `reserved` 를 올린다. 한 줄이 품절이면 **한 트랜잭션이라 앞선 예약도 롤백**된다 | [x] |
| F2 | 타이머 | 진입 후 대기 | 남은 시간 표시, 3분 이하 강조 | 매초 갱신, 정확히 3분에서 강조가 켜진다. 초는 올림이라 「0:00」이 시간이 남은 동안에는 나오지 않는다 | [x] |
| F3 | 만료 처리 | 시간이 지난 뒤 | 안내 + 재시도 | 화면 **전체**가 만료 안내로 바뀐다 — 일부만 회색으로 만들면 사람은 남은 것을 살 수 있다고 믿는다. 서버가 404 로 답하는 경우(이미 풀림)도 같은 화면이다 | [x] |
| F4 | 이탈 해제 | 화면을 벗어난다 | 가용재고 복원 | 언마운트(뒤로가기 포함)와 `pagehide` 둘 다에서 보낸다. `keepalive` 를 단 `fetch` 라 문서가 사라지는 중에도 도착한다 (4.4) | [x] |
| F5 | 금액 일치 | 장바구니 총액과 비교 | 일치 | 장바구니 · 주문서 · 저장된 주문이 **같은 계산 엔진**을 지난다. 주문의 실결제액이 주문서의 것과 1원도 다르지 않다 | [x] |
| F6 | 배송지 | 기존 선택·신규 입력 | 모두 동작 | 저장된 배송지를 라디오로 고르고, 새 배송지는 주소록 화면(TASK-0112)으로 간다 — 그쪽에 갔다 와도 주문서는 살아 있다(서버에 id 로 있고 예약은 그대로다) | [x] |
| F7 | 주문 생성 | 주문하기 클릭 | Order 생성 | 주문이 만들어지고 **예약은 그대로 잡혀 있다**(4.3 · 4.6). 「결제 단계로 이동」은 갈 곳이 없어 그 자리에서 접수 상태로 바뀐다 | [x] |
| F8 | 9조합 | 밀도 3 × 뷰포트 3 | 깨짐 0건 | 9조합 전부에서 타이머·상품·배송지·금액이 있고 주문 버튼이 하나 | [x] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **2장 전 항목 적용**
- **3장 전 항목 적용** (4.2 가 이유를 적는다) · **4장 해당 없음**

| # | 실측 (2·3장) |
| --- | --- |
| A1 | 판매자 2곳짜리 주문서 열기 **p95 23.37ms** (30회, 로컬). 여는 일이 예약 두 건을 잡는 트랜잭션이다 |
| A2 | 장바구니에 없는 줄 404 · 품절 409 · 「주문서와 장바구니 줄을 둘 다」 400 |
| A3 | 남의 주문서는 읽기도 풀기도 403. **푸는 쪽이 특히 중요하다** — 남이 부른 해제로 재고가 풀리면 그것만으로 남의 주문을 망칠 수 있다 |
| A4 | 토큰 없이 401 |
| A5 | 주문서 조회의 문장 수가 줄 1개일 때와 10개일 때 **똑같이 9** |
| A6 | 실 PostgreSQL |
| A7 | 주문서를 여는 일은 예약이고, 그 동시성은 TASK-0048 F2 가 이미 잰다. 여기서 더 잰 것은 **닫기의 멱등** — 두 번 보내도 두 번 풀리지 않는다 |

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [x] |
| D2 | 주문서 흐름을 `docs/design/pages.md` 에 반영 | [x] — `/checkout` 이 `/checkout/[id]` 가 됐고, **껍데기 라우트가 이제 없다**. `/checkout/complete` 는 아직 비어 있다는 것을 그 줄에 적었다 |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 15분 타이머가 심리적 압박 | 남은 시간을 과하게 강조하지 않는다. 만료돼도 재시도가 쉽다는 걸 안내 |
| R2 | 이탈 해제 요청이 유실 | `sendBeacon` 사용 + 스케줄러 안전망 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
| 2026-09-05 | 완료. 실측 채움 — 주문서 열기 p95 23.37ms · 줄 수가 조회 문장 수를 바꾸지 않는다(9=9). 타이머 스펙이 `urgent` 가 만료 뒤에도 참이라는 것을 잡아 주석을 고쳤다 |
| 2026-09-05 | 구현 착수. **4.1~4.5 를 더한다** — 주문서는 표가 아니라 같은 `checkoutId` 를 가진 예약 묶음이고(스키마 변경 없음), 그래서 엔드포인트 셋이 늘어 3장이 적용된다. 주문 생성은 이미 잡힌 예약을 쓴다 |
| 2026-09-03 | D-208 레이어 분할에 따라 참조 갱신 — 배송지는 TASK-0027 → **TASK-0111**(API · `addressSchema`). 배송지 카드 UI 는 TASK-0112 의 컴포넌트를 재사용한다 |
