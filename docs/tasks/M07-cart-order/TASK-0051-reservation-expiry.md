# TASK-0051: 예약 만료 스케줄러

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M07 장바구니·주문 |
| 상태 | 승인 대기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/reservation-expiry` |
| 선행 작업 | TASK-0048 |

## 1. 목적

만료된 재고 예약을 자동 해제한다. **이 스케줄러가 멈추면 재고가 잠긴다** — 그래서 헬스체크 대상이다. (D-026)

## 2. 범위

### 포함
- 만료 예약 정리 스케줄러 (주기 1분)
- 결제 대기 상태로 방치된 주문 처리 (PAYMENT_PENDING → PAYMENT_FAILED)
- 헬스체크에 마지막 실행 시각·처리 건수 노출
- 처리 지연 임계치 초과 시 degraded 표시
- 수동 실행 API (관리자)
- 정합성 점검 — `reserved` 캐시 vs HELD 합계

### 제외
- 관리자 화면 (M14)

## 3. 요구사항

- [ ] 만료된 예약이 1분 내 해제된다
- [ ] 해제된 만큼 가용재고가 복원된다
- [ ] 확정된 예약은 건드리지 않는다
- [ ] 스케줄러 상태를 헬스체크로 알 수 있다
- [ ] 다중 인스턴스에서도 중복 처리되지 않는다

## 4. 설계

```
매 1분:
  ① HELD && expiresAt < now 인 예약 조회 (배치 상한 적용)
  ② 각각 release() → reserved 감소, 상태 RELEASED
  ③ 연결된 주문이 PAYMENT_PENDING 이면 PAYMENT_FAILED 로 전이
  ④ 실행 결과 기록
```

**중복 실행 방지**: 배포 인스턴스가 늘어나면 스케줄러가 동시에 돈다. DB 어드바이저리 락으로 한 번에 하나만 실행되게 한다.

**정합성 점검**은 별도 주기(예: 1시간)로 돌려 `reserved` 캐시와 실제 HELD 합계를 대조하고, 어긋나면 로그와 알림을 남긴다. 자동 보정은 하지 않는다 — 원인을 모르는 채 값을 고치면 문제가 숨는다.

## 5. 구현 계획

1. 만료 조회·해제 배치
2. 주문 상태 전이 연결
3. 어드바이저리 락
4. 헬스체크 연동
5. 수동 실행 API
6. 정합성 점검 배치
7. 지연·실패 시나리오 테스트

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 만료 해제 | 만료 시각을 과거로 조작 | 1분 내 RELEASED, 가용재고 복원 | [ ] |
| F2 | 확정 보호 | CONFIRMED 예약 존재 | 건드리지 않음 | [ ] |
| F3 | 주문 전이 | 만료된 결제 대기 주문 | PAYMENT_FAILED 전환 | [ ] |
| F4 | 중복 방지 | 인스턴스 2개 동시 실행 | 처리 중복 0건 | [ ] |
| F5 | 헬스체크 | `/health` 확인 | `reservationExpiry` 상태·마지막 실행 시각 | [ ] |
| F6 | 지연 감지 | 스케줄러 중지 후 대기 | degraded 표시 | [ ] |
| F7 | 정합성 점검 | 캐시를 의도적으로 틀리게 함 | 점검 배치가 불일치 검출·기록 | [ ] |
| F8 | 대량 처리 | 만료 1000건 | 배치 상한 내에서 나눠 처리, 누락 0건 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5 강화** — 만료·해제 로직은 분기 커버리지 100%
- **2장 해당 없음**
- **3장**: A2~A4 적용 (수동 실행 API)
- **4장 해당 없음**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳, **M07 마일스톤 완료 처리** | [ ] |
| D2 | 헬스체크 항목 목록 갱신 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | **스케줄러 중단 시 재고 전면 잠김** | 헬스체크 + 외부 모니터링 알림. 수동 실행 API 로 즉시 복구 가능 |
| R2 | 정합성 자동 보정의 위험 | 자동 보정하지 않고 검출·기록만. 보정은 사람이 판단 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
