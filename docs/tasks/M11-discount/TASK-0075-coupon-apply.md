# TASK-0075: 쿠폰 적용 · 검증

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M11 할인 |
| 상태 | 승인됨 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/coupon-apply` |
| 선행 작업 | TASK-0072 |

## 1. 목적

주문서에서 쿠폰을 적용한다. **M07 계산 엔진의 `discounts` 목록에 쿠폰을 넣는 것**이 전부이며, 계산기 자체는 건드리지 않는다.

## 2. 범위

### 포함
- 적용 가능 쿠폰 조회 (주문 내용 기준 필터링)
- 쿠폰 검증 — 유효기간, 최소 주문금액, 적용 범위, 사용 여부
- 계산 엔진에 할인 항목으로 전달 (부담 주체 포함)
- 중복 사용 규칙 — 플랫폼 1장 + 판매자 쿠폰 판매자당 1장
- 주문서 쿠폰 선택 UI
- 최대 할인 조합 추천
- 주문 확정 시 쿠폰 사용 처리 (동시성 제어)

### 제외
- 적립금 (TASK-0076)
- 환불 시 복구 (TASK-0078)

## 3. 요구사항

- [ ] 적용 불가한 쿠폰은 목록에 나오지 않거나 사유가 표시된다
- [ ] 판매자 쿠폰은 해당 판매자 항목에만 안분된다
- [ ] 같은 쿠폰이 두 주문에 동시 사용되지 않는다
- [ ] 계산 엔진 코드를 수정하지 않고 연결된다
- [ ] 최대 할인 조합을 추천한다

## 4. 설계

```
쿠폰 선택 → discounts 목록 구성
[
  { type:'COUPON', amount: 5000, scope:'ORDER',  bearer:'PLATFORM', couponId },
  { type:'COUPON', amount: 3000, scope:'SELLER', targetId: sellerId, bearer:'SELLER', couponId }
]
→ calculateOrder(items, discounts, shipping)   ← M07 그대로
```

**계산기를 수정하지 않는 것이 이 TASK 의 성공 기준**이다. M07 에서 `discounts` 를 목록으로 받게 설계한 이유가 여기서 검증된다.

중복 사용: 플랫폼 쿠폰 1장 + 판매자별 판매자 쿠폰 1장. 플랫폼 쿠폰끼리는 중복 불가.

## 5. 구현 계획

1. 적용 가능 쿠폰 조회·필터
2. 검증 로직
3. 할인 목록 변환 (부담 주체 포함)
4. 중복 사용 규칙
5. 주문서 쿠폰 선택 UI
6. 최대 할인 조합 추천
7. 사용 처리 (동시성)

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 계산기 무수정 | git diff 확인 | `calculateOrder` 변경 0줄 | [ ] |
| F2 | 조회 필터 | 최소금액 미달 쿠폰 | 목록에서 제외 또는 사유 표시 | [ ] |
| F3 | 범위 안분 | 판매자 쿠폰 적용 | 해당 판매자 항목에만 안분 | [ ] |
| F4 | 중복 규칙 | 플랫폼 쿠폰 2장 시도 | 1장만 적용 | [ ] |
| F5 | 동시 사용 | 같은 쿠폰으로 동시 주문 2건 | 1건만 성공 | [ ] |
| F6 | 부담 주체 | 주문 저장 후 확인 | 쿠폰별 bearer 기록됨 | [ ] |
| F7 | 최대 조합 | 여러 쿠폰 보유 시 | 최대 할인 조합 추천 | [ ] |
| F8 | 금액 정확성 | 쿠폰 적용 주문 | 안분 합계 = 쿠폰 할인액 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5 강화** — 검증·중복 규칙 분기 커버리지 100%
- **2장 전 항목 적용**
- **3~4장 전 항목 적용**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 중복 사용 규칙을 `docs/design/pricing.md` 에 추가 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 계산기 수정이 필요해짐 = M07 설계 실패 | 그럴 경우 `pricing.md` 를 먼저 고치고 계산기와 테스트를 함께 갱신 |
| R2 | 최대 조합 계산이 무거움 | 보유 쿠폰 수가 적으므로 전수 탐색으로 충분. 상한을 두고 초과 시 근사 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
