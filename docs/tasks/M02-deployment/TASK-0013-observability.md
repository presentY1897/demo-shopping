# TASK-0013: 로그 · 에러 추적 · 모니터링

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M02 배포 파이프라인 |
| 상태 | 승인됨 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/observability` |
| 선행 작업 | TASK-0012 |

## 1. 목적

배포된 서비스에서 무슨 일이 일어나는지 볼 수 있게 한다. 이후 마일스톤에서 장애를 만났을 때 로그를 뒤질 수 있는 상태를 만든다.

## 2. 범위

### 포함
- 구조화 로깅 (JSON), 요청 ID 전파
- 민감정보 마스킹 (토큰, 카드번호, 이메일)
- 에러 추적 도구 연동 (Sentry 무료 플랜)
- 프론트 3개 앱에도 에러 추적 적용
- 헬스체크 외부 모니터링 (5분 주기, 실패 시 알림)
- **Vercel Analytics** — 페이지뷰·코어 웹 바이탈 (실사용자 기준 측정)
- 스케줄러 동작 여부를 헬스체크에 포함할 자리 마련

### 제외
- APM·분산 추적
- 대시보드 구축
- 로그 장기 보관
- **제품 분석 도구**(PostHog 등 이벤트·퍼널·세션 리플레이) — 데모 사이트라 트래픽이 없어 데이터가 쌓이지 않는다. 얻는 것에 비해 도입 비용이 크다

## 3. 요구사항

- [ ] 모든 요청 로그에 요청 ID 가 붙고 응답 헤더로도 전달된다
- [ ] 로그에 토큰·카드번호·이메일 전문이 남지 않는다
- [ ] 처리되지 않은 예외가 에러 추적 도구에 수집된다
- [ ] 프론트에서 발생한 에러도 수집된다
- [ ] 헬스체크 실패 시 알림이 온다

## 4. 설계

```
요청 → requestId 생성(또는 헤더 승계) → 로거 컨텍스트에 주입 → 응답 헤더 X-Request-Id
예외 → 전역 필터 → 구조화 로그 + Sentry 전송 (requestId 태그)
```

마스킹 대상: `authorization`, `cookie`, `cardNumber`, `cvc`, `email`(도메인만 남김), `phone`(뒷자리 마스킹)

> **주의 — 실제 컬럼 이름은 `cardNumber` 가 아니다 (2026-09-05, TASK-0053).** 가상 카드가
> 생기면서 그 필드는 `VirtualCard.number` 가 됐다. 이름으로 고르는 마스커는 그 행을 **그대로
> 지나친다.** 목록에 `number` 를 넣는 것은 너무 흔한 이름이라 위험하므로, R2 가 이미 답을
> 적어 두었다 — **허용 필드만 로깅**하는 화이트리스트 쪽이 맞다.
>
> 표시용 마스킹(`maskVirtualCardNumber`)은 그것과 별개로 이미 있다. 응답을 만들 때 전문이
> 서비스 밖으로 나가지 않으므로, 로거 마스킹은 **그래도 새는 것을 받는 마지막 그물**이지
> 대체재가 아니다.

**헬스체크 확장 지점**: 이후 재고 예약 만료 스케줄러, 데모 계정 정리 스케줄러, 검색 인덱싱 큐의 상태를 여기에 추가한다. 이번엔 스키마와 자리만 만든다.

## 5. 구현 계획

1. 구조화 로거 설정, 요청 ID 미들웨어
2. 마스킹 유틸 및 로거 연결
3. Sentry 프로젝트 생성, API 연동
4. 프론트 3개 앱 연동 (소스맵 업로드 포함)
5. 헬스체크 확장 스키마 (`checks: { name, status, lastRunAt }[]`)
6. 외부 모니터링 등록 및 알림 채널 연결

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 요청 ID | 요청 후 응답 헤더 확인 | `X-Request-Id` 존재, 로그와 일치 | [ ] |
| F2 | 마스킹 | 토큰 포함 요청 후 로그 확인 | 원문 노출 0건 | [ ] |
| F3 | 백엔드 에러 수집 | 의도적 예외 발생 | Sentry 에 requestId 와 함께 기록 | [ ] |
| F4 | 프론트 에러 수집 | 각 앱에서 의도적 예외 | 3개 앱 모두 수집, 소스맵으로 원본 위치 표시 | [ ] |
| F5 | 헬스체크 스키마 | `/health` 응답 | `checks` 배열 포함 | [ ] |
| F6 | 모니터링 알림 | API 를 일시 중지 | 5분 이내 알림 수신 | [ ] |
| F7 | Analytics | 세 앱 접속 후 대시보드 확인 | 페이지뷰·웹 바이탈 수집됨 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5(커버리지) 면제** — M05 부터 적용. 단 마스킹 유틸은 단위 테스트 필수
- **2장 화면 게이트 해당 없음**
- **4장 해당 없음**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳, **M02 마일스톤 완료 처리** | [ ] |
| D2 | 헬스체크 응답 스키마를 `docs/design/` 에 반영 | [ ] |
| D4 | Sentry DSN 등 환경변수 `.env.example` 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 무료 플랜 이벤트 한도 초과 | 샘플링 비율 조정, 로그 레벨 상향 |
| R2 | 마스킹 누락으로 민감정보 유출 | 화이트리스트 방식(허용 필드만 로깅)으로 설계 |

## 8. 확정된 버전

| 패키지 | 버전 |
| --- | --- |
| @sentry/node | |
| @sentry/nextjs | |
| @vercel/analytics | |
| pino (또는 nestjs-pino) | |

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
