# TASK-0043: 상품 상세 (밀도 3단계)

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M06 검색·탐색 |
| 상태 | 완료 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/product-detail` |
| 선행 작업 | TASK-0040 |

## 1. 목적

상품 상세 페이지를 만든다. **미니멀↔맥시멀 대비가 가장 극적으로 드러나는 화면**이다.

## 2. 범위

### 포함
- 이미지 갤러리 (확대, 썸네일, 스와이프)
- 옵션 선택 → Variant 확정, 재고에 따른 선택 제한
- 존재하지 않는 조합 처리
- 가격 표시 (정가·할인가·할인율)
- 상품 정보 (설명, 속성 표)
- 밀도 3단계 레이아웃 분기
- 장바구니 담기·바로구매 버튼 자리 (M07 연결) — **모바일은 하단 고정 바, 데스크톱은 우측 패널**
- 위시리스트 버튼 자리 (M13)
- 리뷰·문의 영역 자리 (M13)
- 판매자 정보·브랜드관 링크
- SEO·구조화 데이터 (Product 스키마)

### 제외
- 장바구니 실제 동작 (M07)
- 리뷰·문의 실제 동작 (M13)
- 추천 상품 (M13 또는 M15)

## 3. 요구사항

- [x] 옵션을 고르면 해당 Variant 의 가격·재고가 반영된다
- [x] 품절 조합은 선택할 수 없다
- [x] 밀도 3단계에서 정보량과 레이아웃이 달라진다
- [x] 모바일에서 이미지 스와이프가 동작한다

## 4. 설계

| 영역 | 미니멀 | 표준 | 맥시멀 |
| --- | --- | --- | --- |
| 레이아웃 | 이미지 전면, 긴 스크롤 | 좌 이미지 / 우 정보 | 좌 이미지 / 우 정보 + 사이드 정보 |
| 속성 표 | 접힘 | 주요 항목 | 전체 표 |
| 배송 정보 | 최소 | 요약 | 상세 + 예상 도착일 |
| 리뷰 | 링크만 | 요약 + 3건 | 요약 + 분포 + 5건 |
| 추천·묶음 | ✗ | ✗ | ✓ |
| 구매 유도 배지 | ✗ | 일부 | 전체(판매량·조회수·재고임박) |

**옵션 선택 로직**: 첫 옵션을 고르면 그와 조합 가능한 두 번째 옵션만 활성화한다. 존재하지 않는 조합을 고를 수 없게 한다.

### 4.1 손님이 상품을 볼 수 없다 (2026-09-05 정정)

`GET /products/:id` 는 `product.read` 를 요구한다. 카테고리 트리와 같은 문제다 — 로그아웃한
방문자와 검색 크롤러가 401 을 받고, 색인 대상인 화면이 성립하지 않는다. 게다가 그 라우트는
**초안과 판매중지 상품도 내려준다**. 상점이 그것을 보여 주면 검색에서 뺀 의미가 없다
(TASK-0038 F3 이 색인에서 뺀 바로 그 상태다).

TASK-0042 4.2 와 같은 모양으로 상점용 공개 라우트를 낸다.

| | |
| --- | --- |
| 라우트 | `GET /products/:id/detail` — 공개 |
| 무엇 | `ACTIVE` 만. 그 외는 **404** 다. 「없음」과 「보이면 안 됨」을 구별해 주면 비공개 상품의 존재가 새어 나간다 |
| 응답 | `{ product, seller: { id, brandName } }` — 판매자 정보·브랜드관 링크(2장)에 필요한 최소 |

### 4.2 R2 는 이미 해결돼 있다 (2026-09-05 확인)

「조합 맵을 서버에서 만들어 내려준다」는 TASK-0032 가 이미 했다.
`productVariantSchema.optionValueIds` 가 그 상품 자신의 축 순서대로 담긴 조합이고,
`options[].values[]` 가 축과 선택지다. **조합 맵을 새로 만들 것이 없다** — 화면은 variant 목록을
축별로 뒤집어 「이 값을 고르면 남는 조합이 있는가」를 답하면 된다.

`isActive` 와 `stock` 이 나뉘어 있는 것도 그대로 쓴다. F2(품절)와 F3(없는 조합)은 **다른 상태**다 —
품절은 있는 조합이고 없는 조합은 아예 SKU 가 없다. 화면도 다르게 말해야 한다.

### 4.3 속성 표에는 라벨이 필요하다 (2026-09-05)

2장이 「속성 표」를 담는데, `product.attributes` 는 **키로 된 가방**이다 —
`{ wool_ratio: 70 }`. 한국어 라벨과 표시 순서는 `AttributeDefinition` 에만 있고, 그것을 읽는
`GET /attributes` 는 퍼미션이 걸려 있다. `wool_ratio: 70` 을 그대로 그리면 만들다 만 화면이다.

그래서 4.1 의 응답이 **이미 풀린 표**를 함께 낸다.

```
attributes: [{ key: 'wool_ratio', label: '울 함량', value: 70 }, …]
```

카테고리 조상 사슬에서 모으고 가까운 조상이 이긴다 — 검색 필터가 쓰는 그 규칙이다
(`SearchService.filtersFor`). **정렬도 서버가 한다**: `sortOrder` 는 정의에 있고 화면에는 없다.

값의 표시(불리언 → 예/아니오, 배열 → 쉼표)는 화면 몫이다. 그것은 언어이지 데이터가 아니다.

## 5. 구현 계획

1. **상점용 공개 상세 라우트** (4.1) — 데이터 로딩은 그 위에서
2. 이미지 갤러리
3. 옵션 선택 로직 (조합 유효성·재고)
4. 가격 표시
5. 밀도별 레이아웃 3종
6. 각 영역 자리 배치
7. SEO·구조화 데이터

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 실측 | 충족 |
| --- | --- | --- | --- | --- | --- |
| F1 | 옵션 선택 | 색상·사이즈 선택 | 가격·재고·SKU 반영 | 아이보리·M → `LUMIKNIT-6` · 8개 남음. 고르기 전에는 SKU 를 아예 그리지 않는다. **옵션이 없는 상품은 고를 것 없이 바로** 가격이 나온다 — 그 한 variant 가 가격을 들고 있다(DECISIONS 3) | [x] |
| F2 | 품절 조합 | 블랙·S (재고 0) | 선택 불가 표시 | 「품절」 + `aria-disabled`, 클릭 거절 | [x] |
| F3 | 없는 조합 | 카멜·L, 카멜·XL (SKU 없음) | 비활성 처리 | 「준비되지 않음」 — **품절과 다른 낱말이다.** 하나는 기다리면 오고 하나는 오지 않으므로 다음 행동이 다르다 | [x] |
| F4 | 밀도 3단계 | 단계 전환 | 위 표대로 | 속성 표: 접힘 / 4행 / 전체. 배송: 한 줄 / 요약 / 문단+도착 예정. 추천 자리는 맥시멀만. 배지: 없음 / 판매량 / 판매량·평점·재고임박 | [x] |
| F5 | 갤러리 | 확대·썸네일·스와이프 | 동작 | 썸네일 3개, 확대 토글이 이름을 바꾼다. **스와이프는 스크롤 스냅이다** — 손으로 만든 제스처 핸들러가 아니라 브라우저의 것 | [x] |
| F5b | 모바일 전용 UI | 360px | 하단 고정 바, 데스크톱엔 없음 | 360 은 고정 바(수량 스테퍼 없음), 1440 은 패널(스테퍼 있음) | [x] |
| F5c | 단일 마운트 | DOM 검사 | 동시 렌더 없음 | 아홉 조합 전부에서 「장바구니 담기」가 **정확히 하나**다. CSS 로 숨긴 두 번째가 있으면 둘이 된다 | [x] |
| F6 | 9조합 | 밀도 3 × 뷰포트 3 | 깨짐 0건 | 아홉 조합 각각에서 제목·가격·두 축·구매 영역 하나를 확인. jsdom 은 그리지 않으므로 「깨짐」을 픽셀로 잴 수는 없고, **화면이 온전한가**를 잰다 | [x] |
| F7 | SEO | 페이지 소스 | Product 구조화 데이터 유효 | `Product` + `AggregateOffer`. 가격 범위는 **주문 가능한 것만** 센다(9개) — 품절 SKU 를 넣은 범위는 아무도 낼 수 없는 값을 약속한다. 리뷰가 0이면 `aggregateRating` 을 아예 넣지 않는다 | [x] |
| F8 | ~~성능~~ | ~~Lighthouse 모바일~~ | ~~LCP 2.5s 이하~~ — **게이트가 아니다** (아래) | [-] |
| F9 | 비공개 상품 | 초안·판매중지 상품 URL 접근 | 404 | [ ] |
| F10 | 손님 접근 | 토큰 없이 상세 진입 | 200 | [ ] |

**F8 은 이 TASK 의 통과 조건이 아니다** (2026-09-05 정정).
[품질 게이트 2장](../QUALITY-GATES.md)이 성능을 화면 게이트에서 뺐다 — 절대값 게이트와 증분
게이트를 차례로 시도한 끝에 「화면이 만든 회귀는 한 건도 없었다」는 결론이 나왔고, 측정 절차와
누적 실측은 TASK-0097 이 소유한다. 대신 **무거운 것을 새로 들이면 여기에 적는다.**

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **2장 전 항목 적용.** 이 화면이 P6 의 대표 검증 대상
- **3장 적용** (2026-09-05 정정) — 4.1 이 라우트를 하나 낸다. A1 · A2 · A5 · A6 과, **뒤집힌
  A4**(토큰 없이 200). A3 은 해당 없다 — 공개 라우트다. A7 도 해당 없다
- **4장 해당 없음** — 스키마는 그대로다

| # | 실측 (2장) |
| --- | --- |
| P2 | axe 8건 — 밀도 3단계 · 폰(고정 바) · 조합 거절 상태 · 확대 · 옵션 없는 상품 |
| P3 | 360 / 768 / 1440 × 밀도 3 = 아홉 조합 (F6) |
| P4 | Tab 으로 옵션 · 갤러리 · 구매 버튼 둘에 도달(45회 이내). **비활성 버튼도 탭으로 닿는다** — `aria-disabled` 라서, 이유를 읽을 수 있다 |
| P5 | 로딩 · 빈(이미지 없음) · 에러 · 정상. 없는 상품은 상태가 아니라 404 |
| P6 | 이 화면이 대표 검증 대상. 세 단계가 레이아웃·정보량 모두 다르다 |

| # | 실측 (3장) |
| --- | --- |
| A1 | 상세 한 건 = 상품 한 질의 + 판매자 한 질의 + 속성 표 한 질의 |
| A2 | 없는 id · 초안 · 판매중지 전부 404 |
| A4 | **뒤집힌 채로 통과** — 토큰 없이 200 |
| A5 | 속성 표는 조상 사슬을 한 질의로 훑는다. 카테고리 깊이와 무관하게 3건 |
| A6 | 실 PostgreSQL |

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [x] |
| D2 | 밀도별 상세 구성표를 `docs/design/pages.md` 에 반영 | [x] 옵션 선택 규칙과 「품절 ≠ 없는 조합」도 함께 적었다 |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 밀도별 레이아웃 3종의 유지 비용 | 공통 블록을 조합하는 방식으로 구성. 완전히 다른 3벌을 만들지 않는다 |
| R2 | ~~옵션 3개 이상인 상품의 조합 계산~~ | **해소됨** — TASK-0032 가 `optionValueIds` 로 이미 내려준다(4.2) |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-05 | 구현 완료. **옵션 선택의 탈출구를 「다시 눌러 해제」로 정했다** — 처음에는 충돌하는 축을 자동으로 푸는 코드를 썼는데, 그 코드에 닿을 수가 없었다(충돌을 일으킬 버튼이 바로 그 거절되는 버튼이다). 닿지 않는 분기를 지우고 `aria-pressed` 토글로 바꿨다 |
| 2026-09-05 | **4.3 을 더한다** — 속성 표의 한국어 라벨은 `AttributeDefinition` 에만 있고 그것을 읽는 라우트는 퍼미션이 걸려 있다. 4.1 의 응답이 이미 풀린 표를 함께 내도록 한다 |
| 2026-09-05 | 구현 착수. **4.1 을 더한다** — `GET /products/:id` 가 `product.read` 를 요구하고 초안·판매중지도 내려주므로 상점이 쓸 수 없다. TASK-0042 4.2 와 같은 모양으로 공개 라우트를 낸다. 그에 따라 6.2 의 「3장 해당 없음」을 바로잡고 F9·F10 을 더한다. **4.2 를 더한다** — R2 는 이미 해소돼 있음을 확인했다. **F8(LCP)은 게이트가 아니다** — 품질 게이트 2장이 성능을 화면 게이트에서 뺐고 TASK-0097 이 소유한다 |
| 2026-09-02 | 최초 작성 |
