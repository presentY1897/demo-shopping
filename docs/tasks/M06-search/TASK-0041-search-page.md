# TASK-0041: 검색 페이지 · 자동완성 · 필터

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M06 검색·탐색 |
| 상태 | 승인됨 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/search-page` |
| 선행 작업 | TASK-0039, TASK-0040 |

## 1. 목적

검색 화면을 만든다. **필터 UI 가 속성 정의로부터 자동 생성**되는 것이 핵심이다.

## 2. 범위

### 포함
- 헤더 검색창 + 자동완성 드롭다운 (키보드 조작)
- 검색 결과 페이지 (목록 그리드 재사용)
- **필터 패널 자동 생성** — 필터 스키마 API 응답으로 UI 구성
- 필터 타입별 위젯 (선택형·범위형·불리언)
- **모바일은 바텀시트, 데스크톱은 좌측 사이드 패널** — 같은 라우트 안에서 컴포넌트 분기
- 패싯 개수 표시, 결과 0건인 값 비활성화
- 정렬 선택
- 적용된 필터 칩 + 개별·전체 해제
- URL 동기화 (필터·정렬·검색어가 URL 에 반영, 공유·뒤로가기 동작)
- 검색어 없음·결과 없음 상태

### 제외
- 인기 검색어·최근 검색어 (M13 에서 검토)

## 3. 요구사항

- [ ] 카테고리를 바꾸면 필터 항목이 달라진다
- [ ] 필터를 걸면 URL 이 바뀌고 새로고침해도 유지된다
- [ ] 자동완성을 키보드로 선택할 수 있다
- [ ] 결과 0건이 되는 필터 값은 비활성 표시된다
- [ ] 밀도 3단계가 결과 목록에 적용된다

## 4. 설계

```
검색 → 필터 스키마 API → 필터 패널 렌더 → 사용자 선택 → URL 갱신 → 검색 API → 결과 + 패싯 갱신
```

**필터 UI 를 하드코딩하지 않는 이유**: 관리자가 속성을 추가하면 필터도 따라 생겨야 한다. D-005 의 "설정 가능"이 검색까지 이어지는 지점이다.

URL 동기화는 포트폴리오에서 중요하다. 필터를 건 상태의 링크를 공유할 수 있어야 실제 서비스처럼 보인다.

## 5. 구현 계획

1. 검색창 + 자동완성 (디바운스·키보드)
2. 결과 페이지 골격
3. 필터 패널 자동 생성
4. 타입별 필터 위젯
5. URL 동기화
6. 필터 칩·해제
7. 빈 상태·오타 안내

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 자동완성 | "원피" 입력 | 후보 표시, ↑↓·Enter 로 선택 | [ ] |
| F2 | 필터 자동 생성 | 코트 → 신발 카테고리 전환 | 필터 항목이 각각 다르게 생성 | [ ] |
| F3 | 관리자 연동 | 관리자가 속성 필터 노출 켜기 | 검색 화면에 새 필터 등장 | [ ] |
| F4 | URL 동기화 | 필터 3개 적용 후 새로고침 | 상태 유지, 뒤로가기 정상 | [ ] |
| F5 | 패싯 개수 | 필터 목록 확인 | 개수 표시, 0건 값 비활성 | [ ] |
| F6 | 오타 검색 | "코투" 검색 | 결과 표시 + 보정 안내 | [ ] |
| F7 | 결과 없음 | 존재하지 않는 검색어 | 빈 상태 + 대안 제시 | [ ] |
| F8 | 밀도 적용 | 3단계 전환 | 결과 목록 밀도 변화 | [ ] |
| F9 | 모바일 필터 | 360px 에서 필터 열기 | 바텀시트로 열림, 적용 후 닫힘 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **2장 전 항목 적용**
- **3장 해당 없음** — API 는 TASK-0039
- **4장 해당 없음**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 검색 페이지 구성을 `docs/design/pages.md` 에 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 필터가 많으면 패널이 길어짐 | 기본 5개만 펼치고 나머지는 접기. `sortOrder` 순 |
| R2 | 자동완성 요청 과다 | 디바운스 250ms + 최소 2글자 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
