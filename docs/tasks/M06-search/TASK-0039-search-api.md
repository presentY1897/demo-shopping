# TASK-0039: 검색 API (필터 · 정렬 · 패싯)

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M06 검색·탐색 |
| 상태 | 승인 대기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/search-api` |
| 선행 작업 | TASK-0038 |

## 1. 목적

검색·필터·정렬·패싯을 하나의 API 로 제공한다. 필터 항목은 **카테고리의 속성 정의로부터 자동 도출**된다.

## 2. 범위

### 포함
- 검색 API — 키워드, 카테고리, 가격 범위, 속성 필터, 재고 유무, 정렬, 커서 페이지네이션
- **패싯 집계** — 필터 후보별 개수 반환
- 필터 스키마 API — 해당 카테고리에서 노출할 필터 정의(`isFilterable=true`)
- 자동완성 API (접두어 검색)
- 오타 보정 동작 확인
- 검색어 로깅 (인기 검색어 기반 데이터)

### 제외
- 화면 (TASK-0041)
- 개인화 추천

## 3. 요구사항

- [ ] 오타를 입력해도 결과가 나온다
- [ ] 카테고리마다 다른 필터 목록이 반환된다
- [ ] 패싯에 각 값의 상품 개수가 포함된다
- [ ] 정렬(관련도·최신·가격·판매량·평점)이 동작한다
- [ ] 필터를 걸어도 응답이 빠르다

## 4. 설계

```
GET /api/v1/search?q=코트&categoryId=3&price=50000..200000&attr.fit=오버&sort=price_asc&cursor=...

응답: { items, facets: { brandName: {...}, fit: {...} }, total, nextCursor }
```

필터 스키마는 `AttributeDefinition` 의 `isFilterable` 을 그대로 쓴다. **관리자가 속성에 필터 노출을 켜면 검색 화면에 필터가 생긴다.** 코드 변경이 없다.

## 5. 구현 계획

1. 검색 쿼리 빌더 (키워드·필터·정렬·커서)
2. 패싯 집계 연결
3. 필터 스키마 API
4. 자동완성 API
5. 검색어 로깅
6. 성능 측정·튜닝

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 키워드 검색 | "코트" 검색 | 관련 상품 반환 | [ ] |
| F2 | 오타 보정 | "코투" 검색 | 코트 상품 반환 | [ ] |
| F3 | 자동완성 | "원피" 입력 | 접두어 일치 후보 반환 | [ ] |
| F4 | 카테고리별 필터 | 코트·신발 카테고리 각각 조회 | 서로 다른 필터 목록 | [ ] |
| F5 | 패싯 개수 | 필터 조회 | 각 값의 개수 포함, 실제 결과와 일치 | [ ] |
| F6 | 복합 필터 | 카테고리+가격+속성 동시 적용 | 조건 모두 만족하는 결과 | [ ] |
| F7 | 정렬 5종 | 각 정렬 적용 | 순서 정확 | [ ] |
| F8 | 페이지네이션 | 커서 이동 | 중복·누락 0건 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **3장 API 게이트 전 항목 적용.** A1 목표를 **p95 200ms 이하**로 강화 — 검색은 체감이 직결된다
- **2장 해당 없음**
- **4장 해당 없음**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 검색 쿼리 파라미터 규약을 `docs/design/pages.md` 에 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 속성 필터 키가 카테고리마다 달라 쿼리 조립이 복잡 | 필터 스키마 API 가 키 목록을 내려주고 클라이언트는 그대로 전달만 한다 |
| R2 | 검색 품질이 기대에 못 미침 | 순위 규칙 조정 → 동의어 보강 → 그래도 부족하면 D-013 재검토 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
