# TASK-0056: 결제 웹훅 (멱등 처리)

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M08 결제 |
| 상태 | 완료 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/payment-webhook` |
| 선행 작업 | TASK-0055 |

## 1. 목적

토스 웹훅을 수신해 결제 상태를 동기화한다. **웹훅은 중복 도착과 순서 뒤바뀜을 전제로 설계**한다.

## 2. 범위

### 포함
- 웹훅 엔드포인트 (인증·서명 검증)
- **멱등 처리** — 같은 이벤트가 여러 번 와도 한 번만 반영
- 순서 역전 처리 (오래된 이벤트가 나중에 도착)
- 원문을 `PaymentEvent` 에 보존
- 처리 실패 시 재시도 큐
- 웹훅 미수신 대비 **대사(reconciliation) 배치** — 주기적으로 토스 상태 조회 후 대조
- **`UNRESOLVED` 상태 추가** (D-220) — 닿지 못한 승인이 갇히지 않게 한다
- 헬스체크에 마지막 웹훅 수신 시각

### 제외
- 가상 카드 (웹훅 없음, 동기 처리)

## 3. 요구사항

- [x] 같은 웹훅이 3번 와도 상태가 한 번만 변경된다 — 그리고 **도착 기록은 세 줄 남는다**
- [x] 오래된 이벤트가 나중에 와도 최신 상태를 덮어쓰지 않는다 — 본문의 `status` 를 읽는 코드가 한 줄도 없다
- [x] 서명이 유효하지 않으면 거부된다 — 시크릿이 없는 배포에서는 **전부** 거부된다
- [x] 웹훅을 놓쳐도 대사 배치가 상태를 맞춘다
- [x] **닿지 못한 승인이 `UNRESOLVED` 로 남고, 대사가 그것을 승인 또는 실패로 보낸다**
- [x] 모든 웹훅 원문이 보존된다 — 파싱본이 아니라 **문자열 그대로**라 서명을 다시 검증할 수 있다

## 4. 설계

```
웹훅 수신 → ① 서명 검증
           ② eventId 로 중복 확인 (이미 처리했으면 200 반환하고 종료)
           ③ 이벤트 시각과 현재 상태 비교 → 오래된 이벤트면 기록만 하고 무시
           ④ 상태 반영
           ⑤ PaymentEvent 기록
```

**중복에도 200 을 반환한다.** 에러를 반환하면 PG 가 계속 재전송한다.

**대사 배치**를 두는 이유: 웹훅은 유실될 수 있다. 결제는 돈이 걸린 영역이라 "못 받았으면 그만"이 허용되지 않는다. 주기적으로 PENDING·불일치 결제를 조회해 실제 상태와 맞춘다.

### 4.1 닿지 못한 승인은 **`UNRESOLVED`** 다 — 상태를 하나 더한다 (D-220)

TASK-0055 의 승인은 토스에 닿지 못하면(`TOSS_UNREACHABLE`) 결제를 `FAILED` 로 적었다.
낙관적으로 `AUTHORIZED` 로 두면 그 불일치가 **매입할 때가 되어서야 돈으로** 나타나기
때문이고, 그 판단 자체는 옳다.

문제는 그다음이었다. `paymentTransitions` 에서 **`FAILED` 는 나가는 화살표가 없는
종착지**인데, 닿지 못했다는 것은 「거절당했다」가 아니라 **「승인됐는지 우리가
모른다」**이고 실제로 저쪽에서는 승인이 나 있을 수 있다. 그때 이 TASK 의 대사
배치(F6)가 그 결제를 찾아내도 **옮길 곳이 없었다.**

**상태를 하나 더한다.** 사용자가 2026-09-05 에 승인했고 근거는 D-220 이다.

```
READY ──→ AUTHORIZED ──→ PAID
  │           ↑
  ├──→ FAILED │
  │           │
  └──→ UNRESOLVED ──┴──→ FAILED
```

| 규칙 | 왜 |
| --- | --- |
| **닿지 못했을 때만** 들어간다 | 거절은 답이다. 답이 있으면 `FAILED` 다 |
| 나가는 길은 **대사만** 연다 | 사용자의 조작으로 옮길 근거가 우리에게 없다 |
| 매입이 아니라 `AUTHORIZED` 로 나간다 | 「승인된 결제를 매입한다」가 한 곳에만 있게 된다 |
| 그동안 **주문 미완료 · 예약 유지** | 놓아 버리면 대사가 「승인됐다」를 확인해도 팔 물건이 없다 |
| 화면은 **「확인 중, 다시 결제하지 마세요」** | 거절과 문장이 달라야 한다. 그것이 상태를 나눈 이유다 |

### 4.2 그래서 프로바이더가 **셋을 구분해 답해야** 한다

`AuthorizeResult` 는 `{ approved, paymentKey, reason }` 이었다. 불리언 하나로는 「거절」과
「모름」을 말할 수 없어, **판별 유니온**으로 바꾼다.

```ts
type AuthorizeResult =
  | { outcome: 'approved'; paymentKey: string }
  | { outcome: 'declined'; reason: string }
  | { outcome: 'unknown'; reason: string }
```

덤으로 타입이 정확해진다 — 예전 모양은 `paymentKey` 가 승인됐을 때만 있는데도 늘
`string | null` 이라, 부르는 쪽이 있을 리 없는 널을 매번 확인했다.

**가상 카드는 `unknown` 을 내지 않는다.** 우리 프로세스 안에서 트랜잭션으로 끝나 「모르는」
결과가 생길 수 없기 때문이고, 재현 장치의 `timeout` 모드도 한도를 쓰기 **전에** 돌아오므로
불일치가 없다. 이 상태는 **남의 서버가 있는 프로바이더의 것**이다.

### 4.3 「재시도 큐」는 **결제사의 재전송**이다 (F7)

2장이 「처리 실패 시 재시도 큐」를 적었지만, 큐를 만들지 않았다. **이미 하나 있기
때문**이다 — PG 는 2xx 가 아닌 응답에 대해 스스로 재전송한다. 우리가 큐를 하나 더
두면 같은 일을 두 벌로 하게 되고, 그 둘은 언젠가 서로 다른 말을 한다.

이 선택이 성립하는 조건이 정확히 하나이고, 그것은 이미 갖춰져 있다: **중복이
무해해야 한다.** 4.1 의 「본문을 믿지 않고 다시 묻는다」가 그것을 만든다 — 재전송된
웹훅도 저쪽에 다시 물어 같은 답을 얻고, 상태는 한 번만 옮겨진다.

큐가 필요해지는 지점은 따로 있다. **결제사가 재전송을 포기한 뒤**이고, 그때 우리를
구하는 것은 큐가 아니라 대사 배치다(F6) — 웹훅이 영영 안 와도 결제는 끝난다.

### 4.4 서명 규약이 **우리 것**이다

토스 테스트 환경은 웹훅 시크릿을 발급하지 않는다. 그래서 헤더 이름과 알고리즘을
우리가 정했다(`x-toss-signature`, 원문 바이트의 HMAC-SHA256).

**그것을 순수 함수 두 개로 떼어 둔 이유가 그것이다.** 실 계약이 생기면 결제사가 정한
규약으로 `payment-webhook.ts` 의 상수 하나와 `signWebhook` 을 바꾸면 되고, 나머지 —
원문 보존, 멱등, 대사와 공유하는 문 — 는 그대로다. 지금 검사가 재는 것도 「토스의
서명을 우리가 검증하는가」가 아니라 **「서명되지 않은 요청이 들어오지 못하는가」**다.

## 5. 구현 계획

1. 웹훅 엔드포인트 + 서명 검증
2. 멱등 키 저장·확인
3. 순서 역전 처리
4. 상태 반영 + 이벤트 기록
5. 실패 재시도
6. 대사 배치
7. 헬스체크 연동

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 정상 수신 | `payment-webhook.spec.ts` — 서명된 웹훅 1건 | 저쪽에 **다시 물어** 상태가 움직이고 원문이 남는다 | [x] |
| F2 | 멱등 | 같은 웹훅 3회 | 상태 전이는 한 번, 응답은 3회 다 200, `PaymentEvent` 는 **3줄**, 토스 조회는 1회 | [x] |
| F3 | 순서 역전 | 오래된 이벤트를 나중에 전송 | 최신 상태 유지. 본문의 상태를 **읽지 않으므로** 덮어쓸 방법이 없다 | [x] |
| F4 | 서명 검증 | 변조 · 없음 · 시크릿 미설정 | 셋 다 401 | [x] |
| F5 | 원문 보존 | 저장된 `raw` + `signature` 만으로 `hasValidSignature` 재검증 | 통과. 재직렬화했다면 실패한다 | [x] |
| F6 | 대사 배치 | `payment-reconcile.spec.ts` — 웹훅 없이 배치만 실행 | 상태가 맞춰지고 `AppMeta` 에 실행 시각이 남는다 | [x] |
| F7 | 재시도 | 4.3 — **결제사의 재전송**이 그 역할을 한다 | 중복이 무해하므로 재전송이 곧 재시도다 | [x] |
| F8 | 닿지 못한 승인 | 프로바이더가 `unknown` 을 답하게 하고 승인 | 결제 `UNRESOLVED`, 주문 미완료, **예약 유지**, 재결제 409 | [x] |
| F9 | 대사가 그것을 푼다 | 저쪽이 「승인됨」·「없음」인 두 경우 | 각각 **`PAID` 까지**(주문 `PAID` · 예약 `CONFIRMED` · 재고 감소)와 `FAILED`(예약 유지 · **재결제가 다시 열린다**) | [x] |

**실측**: 웹훅 단위 23건 · 웹훅 통합 19건 · 대사 단위 17건 · 대사 통합 10건. 전체 게이트
`typecheck` · `lint` · `build` · `test` 4개 초록(5,182건 통과 / 1건 skip).

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5 강화** — 멱등·순서 처리 분기 커버리지 100%
  → `src/payment/payment-webhook.ts` 와 `src/payment/payment-reconcile.ts` 둘 다 **100%**.
  `vitest.config.mjs` 에 파일별 임계값으로 걸었다. 서비스 쪽(`*.service.ts`)은 걸지 않았다 —
  입출력이 섞인 파일에 100%를 걸면 닿을 수 없는 갈래를 지우게 된다.
- **2장 해당 없음**
- **3장**: A1·A2 적용. **A3·A4 해당 없음** — 웹훅은 서명 검증으로 인증한다
- **4장 전 항목 적용**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [x] |
| D2 | 웹훅·대사 흐름을 `docs/design/state-machines.md` 3장에 반영 | [x] — `UNRESOLVED` 와 그 두 화살표 |
| D4 | `TOSS_WEBHOOK_SECRET` `.env.example` 반영 + 오너 체크리스트 | [x] |

### 6.4 검사가 닿지 못하는 것

| # | 무엇 | 왜 |
| --- | --- | --- |
| 1 | **진짜 토스 웹훅** | 테스트 환경이 웹훅 시크릿을 발급하지 않아 서명 규약이 우리 것이다(4.4). 재는 것은 「토스의 서명을 검증하는가」가 아니라 「서명되지 않은 요청이 못 들어오는가」다 |
| 2 | 결제사의 **실제 재전송 정책** | F7 이 그것에 기대고 있다(4.3). 재전송이 오지 않아도 대사가 같은 일을 하므로 결제는 끝나지만, 「몇 번 만에 오는가」는 저쪽 사정이다 |
| 3 | 두 인스턴스가 **실제로 겹쳐 도는** 대사 | 락 구간이 짧아 겹침 자체는 막지 않는다. 그때 장부가 안 어긋나는 것은 결제 행 잠금이 보장하고, 그쪽은 이미 재고 있다 |
| 4 | 최악의 한 주기 **150초** | 산술 근거이지 실측이 아니다 |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 로컬 개발에서 웹훅 수신 불가 | 터널링 도구 사용법을 README 에 기재. 대사 배치로도 검증 가능 |
| R2 | 대사 배치가 과도한 API 호출 | **유예 60초 + 한 주기 10건.** 막 끊긴 건을 바로 물으면 얻는 답이 「아직 처리 중」뿐이라 호출 한 번을 버린다. 상한이 10인 것은 한 건이 결제사와의 왕복이라, 최악의 한 주기(10 × 마감 15초 = 150초)가 stale 임계치 5분보다 짧아야 하기 때문이다 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-05 | **완료.** 4.3·4.4 를 더한다 — 재시도 큐는 결제사의 재전송이고(중복이 무해해야 성립하며 4.1 이 그것을 만든다), 서명 규약은 테스트 환경에 시크릿이 없어 우리 것이다. 6.4 에 검사가 닿지 못하는 것을 적었다 |
| 2026-09-05 | 착수. **4.1 의 선택이 결정됐다** — 사용자 승인으로 `UNRESOLVED` 를 더한다(D-220). 4.2 를 더한다 — 프로바이더가 거절과 모름을 구분해 답해야 해서 `AuthorizeResult` 가 판별 유니온이 된다. F8 · F9 추가 |
| 2026-09-05 | **4.1 을 더한다** — TASK-0055 가 닿지 못한 승인을 `FAILED` 로 적는데 그 상태에서 나올 길이 없다. 대사 배치(F6)가 무엇을 할 수 있는지가 이 TASK 의 설계 선택이 된다 |
| 2026-09-02 | 최초 작성 |
