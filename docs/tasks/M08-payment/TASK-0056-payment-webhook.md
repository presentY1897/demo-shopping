# TASK-0056: 결제 웹훅 (멱등 처리)

| 항목 | 내용 |
| --- | --- |
| 마일스톤 | M08 결제 |
| 상태 | 승인 대기 |
| 작성일 | 2026-09-02 |
| 브랜치 | `feature/payment-webhook` |
| 선행 작업 | TASK-0055 |

## 1. 목적

토스 웹훅을 수신해 결제 상태를 동기화한다. **웹훅은 중복 도착과 순서 뒤바뀜을 전제로 설계**한다.

## 2. 범위

### 포함
- 웹훅 엔드포인트 (인증·서명 검증)
- **멱등 처리** — 같은 이벤트가 여러 번 와도 한 번만 반영
- 순서 역전 처리 (오래된 이벤트가 나중에 도착)
- 원문을 `PaymentEvent` 에 보존
- 처리 실패 시 재시도 큐
- 웹훅 미수신 대비 **대사(reconciliation) 배치** — 주기적으로 토스 상태 조회 후 대조
- 헬스체크에 마지막 웹훅 수신 시각

### 제외
- 가상 카드 (웹훅 없음, 동기 처리)

## 3. 요구사항

- [ ] 같은 웹훅이 3번 와도 상태가 한 번만 변경된다
- [ ] 오래된 이벤트가 나중에 와도 최신 상태를 덮어쓰지 않는다
- [ ] 서명이 유효하지 않으면 거부된다
- [ ] 웹훅을 놓쳐도 대사 배치가 상태를 맞춘다
- [ ] 모든 웹훅 원문이 보존된다

## 4. 설계

```
웹훅 수신 → ① 서명 검증
           ② eventId 로 중복 확인 (이미 처리했으면 200 반환하고 종료)
           ③ 이벤트 시각과 현재 상태 비교 → 오래된 이벤트면 기록만 하고 무시
           ④ 상태 반영
           ⑤ PaymentEvent 기록
```

**중복에도 200 을 반환한다.** 에러를 반환하면 PG 가 계속 재전송한다.

**대사 배치**를 두는 이유: 웹훅은 유실될 수 있다. 결제는 돈이 걸린 영역이라 "못 받았으면 그만"이 허용되지 않는다. 주기적으로 PENDING·불일치 결제를 조회해 실제 상태와 맞춘다.

## 5. 구현 계획

1. 웹훅 엔드포인트 + 서명 검증
2. 멱등 키 저장·확인
3. 순서 역전 처리
4. 상태 반영 + 이벤트 기록
5. 실패 재시도
6. 대사 배치
7. 헬스체크 연동

## 6. 완료 기준

### 6.1 기능

| # | 기준 | 측정 방법 | 목표 | 충족 |
| --- | --- | --- | --- | --- |
| F1 | 정상 수신 | 웹훅 1건 전송 | 상태 반영, 이벤트 기록 | [ ] |
| F2 | 멱등 | 같은 웹훅 3회 전송 | 상태 1회만 변경, 200 응답 | [ ] |
| F3 | 순서 역전 | 오래된 이벤트 나중 전송 | 최신 상태 유지, 기록만 | [ ] |
| F4 | 서명 검증 | 서명 변조 후 전송 | 401 | [ ] |
| F5 | 원문 보존 | 이벤트 조회 | payload 원문 확인 가능 | [ ] |
| F6 | 대사 배치 | 웹훅 차단 후 결제, 배치 실행 | 상태가 맞춰짐 | [ ] |
| F7 | 재시도 | 처리 중 예외 발생 | 재시도 후 성공, 중복 반영 없음 | [ ] |

### 6.2 품질 게이트

[공통 품질 게이트](../QUALITY-GATES.md) 적용. 예외:
- **Q5 강화** — 멱등·순서 처리 분기 커버리지 100%
- **2장 해당 없음**
- **3장**: A1·A2 적용. **A3·A4 해당 없음** — 웹훅은 서명 검증으로 인증한다
- **4장 전 항목 적용**

### 6.3 문서

| # | 기준 | 충족 |
| --- | --- | --- |
| D1 | 상태 갱신 + 인덱스 2곳 | [ ] |
| D2 | 웹훅·대사 흐름을 `docs/design/state-machines.md` 3장에 반영 | [ ] |

## 7. 리스크 / 열린 질문

| # | 내용 | 대응 |
| --- | --- | --- |
| R1 | 로컬 개발에서 웹훅 수신 불가 | 터널링 도구 사용법을 README 에 기재. 대사 배치로도 검증 가능 |
| R2 | 대사 배치가 과도한 API 호출 | 대상 범위를 PENDING·최근 N시간으로 제한 |

## 8. 확정된 버전

해당 없음.

## 9. 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 |
