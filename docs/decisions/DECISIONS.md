# 최종 결정 사항 (Decisions)

> 이 문서는 **현재 유효한 결정만** 담는다. 논의 과정과 폐기된 안은 `docs/decisions/YYYY-MM-DD-session-NN.md` 에 있다.
> 결정이 바뀌면 세션 파일에 이력을 남기고 **이 문서의 해당 항목을 갱신**한다.
>
> 최종 갱신: 2026-09-02

## 1. 제품

| 항목 | 결정 |
| --- | --- |
| 목표 | 취업 포트폴리오용 쇼핑몰. 데모가 아닌 **실제 운영 가능한 수준** |
| 형태 | **멀티 셀러 마켓플레이스** (구매자 / 판매자 / 사이트 관리자) |
| 초기 상품군 | **패션** 중심. 카테고리·속성은 코드 수정 없이 추가·설정 가능 |
| 디자인 | 전체는 미니멀·심플 |
| 상품 표현 | **미니멀 ↔ 맥시멀 3단계**를 **사용자가 토글**. `apps/shop` 전체 적용, 콘솔은 제외 |
| 레퍼런스 | 국내외 패션 커머스(무신사, 29CM, SSENSE 등) + 기능은 알리익스프레스/아마존/쿠팡 부분 참고 |
| 다국어 | **구조만 선반영, 한국어·KRW 우선**. UI 문구는 메시지 파일로 분리, 금액은 통화 포함 모델링 |

## 2. 앱 구성 · 계정 · 인증

| 항목 | 결정 |
| --- | --- |
| 앱 분리 | **독립된 Next.js 앱 3개** — `apps/shop` / `apps/seller` / `apps/admin` |
| 도메인 | 커스텀 도메인 구매 후 `shop.` / `seller.` / `admin.` 서브도메인 |
| 세션 | **앱별 독립**. 쿠키는 각 서브도메인 한정(`Domain` 미지정) |
| 실계정 로그인 | **Google OAuth 2.0**. 이메일/비밀번호 자체 가입은 구현하지 않음 |
| 토큰 | JWT (access + refresh), refresh 는 httpOnly 쿠키 |
| 역할 | BUYER / SELLER / ADMIN, **다대다** (판매자도 구매자일 수 있음) |
| 데모 계정 | 각 앱에서 **1인당 임시 계정 즉시 발급**. 가상 카드 1장 자동 지급 |
| 데모 만료 | 기본 **24시간** 후 계정과 생성 데이터 자동 삭제 |
| 데모 격리 | 상품 카탈로그는 **공용**, 개인 데이터(장바구니·주문·리뷰·판매자 상품)만 **격리** |
| 데모 권한 | 관리자 데모는 파괴적 작업 차단 (범위 미확정) |

**앱별 독립 세션의 이점**: 세 역할을 브라우저 탭 3개로 동시에 열 수 있다. 판매자가 발송 처리하면 구매자 탭에서 상태 변화를 즉시 확인할 수 있어 데모 시연에 유리하다.

## 3. 카탈로그

| 항목 | 결정 |
| --- | --- |
| 상품 모델 | **Product + ProductOption + ProductVariant(SKU) 3단**. 재고·가격은 Variant |
| 옵션 없는 상품 | Variant 1개를 강제 생성해 예외 분기를 없앤다 |
| 가격 | Variant 에 절대값. Product 에 목록용 최저가 캐시 |
| 카테고리 | 자기 참조 트리. `path` 캐시로 하위 전체 조회 |
| 속성 정의 | **테이블** (`AttributeDefinition`) — 카테고리 연결, 상위에서 상속 |
| 속성 값 | **JSONB** (`Product.attributes`) — 조회 시 조인 없음 |
| 속성 검증 | DB 가 강제하지 못하므로 저장 전 zod 동적 검증이 **필수** |

## 4. 재고

| 항목 | 결정 |
| --- | --- |
| 차감 시점 | **예약 → 확정**. 주문서 진입 시 15분 TTL 예약, 결제 승인 시 확정 |
| 가용재고 | `stock` − 유효한 HELD 예약 합계 |
| 동시성 | `UPDATE ... WHERE stock - reserved >= qty` 조건부 갱신 |
| 이력 | `StockLedger` 원장에 입고·판매·취소·반품입고·조정 전부 기록 |
| 만료 처리 | 스케줄러 필수. **헬스체크 대상** (멈추면 재고가 잠긴다) |

## 5. 주문 · 배송

| 항목 | 결정 |
| --- | --- |
| 주문 구조 | **2단 분할** — `Order`(결제 단위) → `SellerOrder`(배송·취소·정산 단위) → `Shipment` |
| 상태 위치 | 상태는 `SellerOrder` 에 붙는다 |
| 구매확정 | 수동 또는 **D+7 자동**. 정산과 적립금 지급의 트리거 |
| 배송 | 운송사 API 연동 없음. 도메인·상태 전이는 실제와 동일하게 구현, 운송·추적은 **가상 처리** |
| 취소·반품 | **부분 취소·부분 반품 지원**. 항목·수량 단위로 신청 |
| 교환 | **구현하지 않음**. 반품 후 재구매로 안내 |

## 6. 결제

| 항목 | 결정 |
| --- | --- |
| 프로바이더 | **가상 카드 + 토스페이먼츠 테스트** 2종 병행 |
| 추상화 | `PaymentProvider { authorize, capture, cancel, refund }` 뒤에 두 구현 |
| 가상 카드 | 한도·사용액을 가진 원장. 발급·차감·복구. **실패 시나리오 재현용** |
| 토스 | 테스트 키로 결제창 → 승인 → 취소/부분취소/환불 + 웹훅. 실 결제 없음 |
| 웹훅 | 중복 도착을 전제로 **멱등 처리**. 원문은 `PaymentEvent` 에 보존 |

## 7. 할인 · 정산

| 항목 | 결정 |
| --- | --- |
| 할인 수단 | 상품 할인 + **쿠폰**(플랫폼·판매자) + **적립금** |
| 부담 주체 | 쿠폰마다 기록. 플랫폼 쿠폰·적립금은 플랫폼 부담, 판매자 쿠폰만 정산 차감 |
| 적립 시점 | **구매확정 시** 지급 |
| 환불 시 적립금 | 현금이 아니라 **적립금으로 복구** |
| 계산 규칙 | `docs/design/pricing.md` 가 단일 출처. 코드보다 문서가 먼저 |
| 정산 | 주간 정산서 생성 → 관리자 승인 → 지급완료(가상) |
| 수수료 | 카테고리 기본율 + 판매자 개별율(우선) |

## 8. 회원 부가 기능

리뷰·평점(구매 검증, 사진, 판매자 답변) / 위시리스트 / 최근 본 상품 / 앱 내 알림 / 상품 Q&A / 판매자 팔로우 / **가상 카드 발급**

- 리뷰는 `orderItemId` unique 로 **구매 검증을 스키마가 강제**한다
- 알림은 앱 내 알림센터만. 이메일·푸시는 범위 외

## 9. 기술 스택

| 영역 | 결정 |
| --- | --- |
| 저장소 형태 | pnpm workspace **모노레포** |
| 프론트엔드 | **Next.js** (App Router) + TypeScript × 3앱 |
| 백엔드 | **NestJS** + TypeScript (`apps/api` 단일) |
| DB | **PostgreSQL** + Prisma |
| 검색 | **Meilisearch** + 상품 변경 이벤트 기반 인덱싱 파이프라인 |
| 이미지 | **Cloudflare R2** (S3 호환, presigned URL 직접 업로드) |
| 타입 공유 | `packages/shared` 단일 출처 |

```
apps/api      NestJS
apps/shop     구매자 (포트 3000)
apps/seller   판매자 (포트 3001)
apps/admin    관리자 (포트 3002)
packages/shared  공용 타입 · zod 스키마 · API 클라이언트
packages/config  eslint / tsconfig / prettier 프리셋
packages/ui      공통 UI 컴포넌트
```

### 검색 엔진 선정 근거

- **채택(Meilisearch)**: 오타 보정·접두어 검색·패싯·동의어 기본 제공, 한국어 세그멘터 내장, 컨테이너 1개(~100MB)
- 탈락(PostgreSQL FTS): 한국어 형태소 분석이 약하고 랭킹·패싯을 직접 구현해야 함
- 탈락(Elasticsearch + nori): 한국어 품질은 최상이나 메모리 2GB+ 로 배포 비용 과다
- 리스크: 복합명사 분해 정확도가 nori 대비 낮음 → 실제 상품명으로 검증, 부족하면 동의어 사전 보완

### 이미지 스토리지 선정 근거

Cloudflare R2 는 **전송량 요금이 0원**이고 S3 호환 API 라 이전이 쉽다. S3 는 전송량 과금, Vercel Blob 은 무료 한도가 작고 벤더 종속이 강하다.

## 10. 인프라 · 배포

| 항목 | 결정 |
| --- | --- |
| 원격 저장소 | GitHub **공개** 저장소 `presentY1897/demo-shopping` |
| SSH | `~/.ssh/config` 의 `github-presenty` 별칭 (계정별 키 분리) |
| origin URL | `git@github-presenty:presentY1897/demo-shopping.git` |
| 프론트 배포 | **Vercel** × 3 프로젝트 |
| 백엔드/DB/검색 | **Railway 또는 Render** (api, PostgreSQL, Meilisearch) |
| 배포 시점 | **M02 (초반)**. 이후 모든 마일스톤을 배포된 상태로 검증 |

## 11. 개발 · 문서 규칙

| 항목 | 결정 |
| --- | --- |
| git 레이아웃 | `bare + worktree`. 루트에 `.bare/`, `main/`, `feature-<name>/` |
| 브랜치 | `feature/<name>`, `fix/<name>`, `chore/<name>`, `docs/<name>` |
| 커밋 | Conventional Commits, 본문은 한국어 |
| git identity | `presenty1897 / presenty1897@gmail.com` (저장소 로컬 설정) |
| TASK 분할 | **한 TASK = 하나의 작업 목적** |
| TASK 문서 | `docs/tasks/M<NN>-<name>/TASK-<0000>-<slug>.md`. **번호는 전역 4자리 평면** |
| TASK 작성 시점 | **M01~M15 전량 선작성 완료** (100개). 마일스톤별로 나눠 쓰지 않는다 |
| 진행 방식 | TASK 문서 작성 → **사용자 승인** → 구현 |
| 완료 판정 | 각 TASK 의 **정량적 완료 기준**을 모두 충족 |
| 품질 게이트 | `docs/tasks/QUALITY-GATES.md` 공통 적용, 각 TASK 는 **예외만** 명시 |
| 커버리지 게이트 | 80%, **M05 부터 적용** (그 전은 스캐폴딩이라 면제) |
| 설계 변경 시 | **작업 중단 → 설계 문서 수정 → TASK 수정 → 승인 → 재개.** 코드를 먼저 고치지 않는다 |
| 설계 문서 | `docs/design/` 4종을 항상 최신으로 유지 |
| 언어 | 대화·사용자 노출 문구는 한국어, 코드 식별자·주석은 영어 |
| 비밀값 | 커밋 금지. `.env.example` 만 커밋 |

### 설계 문서

| 파일 | 내용 |
| --- | --- |
| `docs/design/pages.md` | 앱별 전체 페이지 목록, 라우팅, 밀도 3단계 |
| `docs/design/erd.md` | 데이터 모델과 관계, 설계 근거 |
| `docs/design/state-machines.md` | 주문·재고·결제·클레임·정산·데모 상태 전이 |
| `docs/design/pricing.md` | 금액 계산 순서와 환불 안분 규칙 |

## 12. 마일스톤

| | 마일스톤 | 주요 내용 |
| --- | --- | --- |
| M01 | 기반 구축 | 워크스페이스, 공유 설정, 로컬 인프라, API/웹 부트스트랩, CI |
| M02 | 배포 파이프라인 | 도메인, Vercel×3, Railway, R2, 자동 배포 |
| M03 | 디자인 시스템 | 토큰·밀도 3단계, 공통 UI 패키지, 영역별 레이아웃 |
| M04 | 인증·계정 | Google OAuth, JWT, 역할, 데모 계정 발급·만료 |
| M05 | 카탈로그 | 카테고리 트리, 속성 정의, 상품·옵션·Variant, 이미지 업로드, 시드 |
| M06 | 검색·탐색 | Meilisearch 인덱싱, 검색·필터·정렬, 목록/상세 3단계 뷰 |
| M07 | 장바구니·주문 | 장바구니, 주문서, 재고 예약, 금액 계산 엔진 |
| M08 | 결제 | 프로바이더 추상화, 가상 카드, 토스 연동, 웹훅 |
| M09 | 배송·주문관리 | 상태 머신, 판매자 주문 처리, 주문내역, 구매확정 |
| M10 | 취소·반품·환불 | 부분 취소, 반품 플로우, 환불 안분 |
| M11 | 할인 | 쿠폰(플랫폼·판매자), 적립금 |
| M12 | 정산 | 수수료 설정, 정산서 생성, 승인·지급 |
| M13 | 회원 부가 | 리뷰, 위시리스트, 최근 본, 알림, Q&A, 팔로우 |
| M14 | 관리자 | 사용자·판매자 관리, 카테고리·속성 관리, 신고, 대시보드 |
| M15 | 마무리 | 성능·접근성, 문서, README·데모 시나리오 |

전체 **100개 TASK**. 마일스톤별 개수: M01 7 / M02 6 / M03 6 / M04 8 / M05 10 / M06 7 / M07 7 / M08 7 / M09 6 / M10 7 / M11 7 / M12 4 / M13 9 / M14 5 / M15 4

## 13. 데모 데이터

판매자 15, 상품 800, 카테고리 40(3단계), 리뷰 3000, 주문 500.

- 검색 결과에 페이지네이션이 생기고 패싯 분포가 자연스러워지는 최소 규모
- 이미지는 Unsplash·Pexels 무료 라이선스 사진을 카테고리별로 확보해 재사용
- **실제 브랜드명은 사용하지 않는다.** 상표권 문제이고 공개 저장소이므로 가상 브랜드명을 생성한다

## 14. 미확정

| # | 항목 |
| --- | --- |
| Q-09 | UI 컴포넌트 라이브러리 (자체 구현 vs shadcn/ui) — M03 에서 결정 |
| Q-10 | 테스트·CI 범위 (E2E 도입 여부, 커버리지 게이트) — M05 진입 전 결정 |
| Q-11 | 관리자 데모 계정의 권한 제한 범위 — M04 에서 결정 |
| Q-12 | 구매할 도메인 이름 — M02 에서 결정 |
