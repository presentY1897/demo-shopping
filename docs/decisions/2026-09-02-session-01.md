# 2026-09-02 · Session 01

## 확정 사항

### D-001. 프로젝트 목표
취업 포트폴리오용 쇼핑몰. 데모 수준이 아니라 **실제 운영 가능한 수준**까지 구현한다.

### D-002. 역할 분리
**사용자(구매자) / 판매자(셀러) / 사이트 관리자(어드민)** 3개 역할. 각 역할별로 페이지 영역을 분리한다.

### D-003. 데모 계정
백엔드에서 **가짜 데모 계정을 즉석 발급**하여, 방문자가 가입 절차 없이 각 역할의 기능을 체험할 수 있게 한다.

### D-004. 디자인 방향
전체적으로 **미니멀·심플**. 단 상품 페이지는 **미니멀 ↔ 맥시멀 3단계**로 표현 밀도를 전환할 수 있게 한다.

### D-005. 상품 카테고리
초기 데이터는 **패션** 중심. 카테고리와 상품 속성은 코드 수정 없이 **추가·설정 가능한 구조**여야 한다.

### D-006. 레퍼런스
국내외 유명 패션 커머스를 주 레퍼런스로 하고, 기능적으로는 알리익스프레스 / 아마존 / 쿠팡을 부분 참고한다.

### D-007. 저장소 구조
`bare + worktree` 레이아웃을 사용한다.

```
shopping/
├── .bare/            # bare git 디렉터리
├── .git              # "gitdir: ./.bare"
├── main/             # main 브랜치 워크트리
└── feature-<name>/   # 기능별 워크트리 (브랜치 feature/<name>)
```

- 기능마다 브랜치와 워크트리를 분리한다.
- git identity 는 저장소 로컬 설정: `presenty1897 / presenty1897@gmail.com`

### D-008. 진행 방식
작업은 `docs/tasks/` 에 계획 문서를 먼저 작성하고, **사용자 승인 후 구현**한다.
세션에서 정해진 사항은 `docs/decisions/` 에 누적 기록한다.

### D-009. 기술 스택
pnpm workspace **모노레포**로 프론트/백엔드를 분리한다.

| 영역 | 선택 | 비고 |
| --- | --- | --- |
| 프론트엔드 | Next.js (App Router) + TypeScript | |
| 백엔드 | NestJS + TypeScript | |
| DB | PostgreSQL + Prisma | Prisma 는 기본 선택. 이견 있으면 TASK 문서에서 재논의 |
| 검색 | Meilisearch | D-013 참조 |
| 인증 | JWT (access + refresh) + httpOnly 쿠키 | 세부는 인증 TASK 에서 확정 |

선택 이유: TypeScript 단일 언어로 개발 속도를 유지하면서, 프론트/백엔드 분리 및 레이어드 아키텍처를 보여줄 수 있어 풀스택 포트폴리오로 균형이 좋다.

### D-010. 결제 · 배송 범위
- 결제: **토스페이먼츠 테스트 연동**. 결제창 호출 → 승인 → 취소/부분취소/환불, 웹훅 수신까지 실제 흐름대로 구현한다. 실 결제는 발생하지 않는다.
- 배송: 운송사 API 연동은 하지 않는다. 대신 **배송 도메인(출고, 운송장 발급, 배송중/완료, 반품/교환)을 실제와 동일하게 구현하고 상태 전이는 가상으로 진행**시킨다.

### D-011. 상품 3단계 뷰의 주체
**사용자 토글**. 방문자가 목록/상세의 정보 밀도를 미니멀↔맥시멀 3단계로 직접 전환한다. 판매자가 상품별로 강제하지 않는다.
선택 값은 사용자 설정으로 유지되어 재방문 시에도 보존한다.

### D-012. 원격 저장소 · 배포
- 원격: **GitHub 공개 저장소**
- 배포: **Vercel**(web) + **Railway/Render**(api, PostgreSQL, Meilisearch) + 오브젝트 스토리지(상품 이미지)
- 채용 담당자가 접속 가능한 데모 URL 확보를 목표로 한다.

### D-013. 검색 엔진 — Meilisearch
PostgreSQL FTS / Meilisearch / Elasticsearch+nori 를 비교한 결과 **Meilisearch** 채택.

- 채택 이유: 오타 보정·접두어(자동완성)·패싯·동의어를 기본 제공하고, 한국어 세그멘터를 내장한다. 컨테이너 1개(~100MB)로 운영 가능해 Railway/Render 구성에 그대로 얹힌다.
- 탈락 이유: PostgreSQL FTS 는 한국어 형태소 분석이 약하고 랭킹/패싯을 직접 구현해야 한다. Elasticsearch+nori 는 한국어 품질이 가장 좋지만 메모리 2GB+ 요구로 배포 비용이 과하다.
- 상품 변경 이벤트로 인덱스를 동기화하는 **인덱싱 파이프라인**을 함께 구현한다.
- 리스크: 복합명사 분해 정확도는 nori 대비 낮다. 실제 상품명 데이터로 검증하고, 부족하면 동의어 사전으로 보완한다.

### D-014. 다국어 · 다통화
**구조만 잡고 한국어/KRW 우선**. UI 문구는 메시지 파일로 분리하고 금액은 통화 정보를 포함해 모델링한다. 문구 하드코딩을 금지해 이후 언어 추가 시 구조 변경이 없도록 한다.

### D-015. 데모 계정 정책
**1인당 임시 계정 발급 + 자동 만료**.

- 방문자가 역할(구매자/판매자/관리자)을 선택하면 임시 계정을 즉시 발급한다.
- 일정 시간(기본 24시간) 후 계정과 해당 계정이 생성한 데이터를 일괄 삭제한다.
- 상품 카탈로그는 공용으로 공유하고, 장바구니·주문·리뷰·판매자 상품 등 개인 데이터만 격리한다.
- 데모 계정의 권한 범위(특히 관리자)는 파괴적 작업을 막도록 별도 TASK 에서 정의한다.

### D-016. 인증 수단 — Google 로그인
실계정 로그인은 **Google OAuth 2.0** 을 사용한다.

- 이메일/비밀번호 자체 회원가입은 구현하지 않는다. 비밀번호 저장·재설정·이메일 인증 부담을 없애고, 실제 커머스의 소셜 로그인 우선 흐름과도 맞다.
- 데모 계정은 이 경로를 타지 않는다. 별도 발급 엔드포인트로 즉시 임시 계정을 만든다(D-015).
- 세션은 JWT(access + refresh), refresh 토큰은 httpOnly 쿠키로 관리한다.
- 판매자·관리자 권한은 로그인 수단과 무관하게 별도 역할 승격 절차로 부여한다.

### D-017. 원격 저장소 및 SSH 계정 분리
- 원격: `git@github-presenty:presentY1897/demo-shopping.git` (GitHub 공개 저장소)
- 이 머신의 기본 SSH 키는 다른 GitHub 계정(`metallJellyfish`)에 연결되어 있다. `~/.ssh/config` 의 **`github-presenty` 별칭**이 `presentY1897` 전용 키를 사용하므로, origin URL 은 `github.com` 대신 이 별칭을 쓴다.
- `main` 브랜치를 push 하고 upstream 을 설정했다.

### D-018. TASK 문서의 정량적 완료 기준
모든 TASK 문서는 **측정 가능한 완료 기준(Definition of Done)** 항목을 포함한다.

- "구현 완료" 같은 주관적 표현 금지. 각 항목은 **측정 방법과 목표값**을 명시한다.
- 기준을 모두 충족해야 TASK 상태를 `완료` 로 변경할 수 있다.
- 템플릿: `docs/tasks/TASK-TEMPLATE.md`

### D-019. 결정 문서 이원화
`docs/decisions/` 를 두 층으로 나눈다.

- `DECISIONS.md`: **현재 유효한 최종 결정만** 주제별로 취합. 이 문서만 읽어도 현재 상태를 파악할 수 있어야 한다.
- `YYYY-MM-DD-session-NN.md`: 세션별 원본 이력. 논의 과정·선정 근거·폐기안 포함.
- 결정이 나오면 세션 파일에 기록한 뒤 `DECISIONS.md` 의 해당 항목을 갱신한다.

### D-020. TASK 분할 기준
**한 TASK = 하나의 작업 목적.** 여러 목적이 섞이면 쪼갠다. 다만 이상적으로 잘게 쪼개지 않는다 — 반나절~하루, 커밋 5~15개 규모를 기준으로 한다.

- 예: "모노레포 초기 세팅"은 워크스페이스 골격 / 공유 설정 / 로컬 인프라 / API 부트스트랩 / Prisma / 웹 부트스트랩 / 개발 워크플로 7개로 분리했다.
- (D-018 의 정량적 완료 기준은 그대로 유효)

### D-021. TASK 문서 번호·디렉터리 체계
**마일스톤 디렉터리 + 전역 4자리 평면 번호**.

```
docs/tasks/README.md                 마일스톤 요약
docs/tasks/M01-foundation/README.md  해당 마일스톤 TASK 목록
docs/tasks/M01-foundation/TASK-0001-workspace-skeleton.md
```

- **디렉터리는 그룹핑만, 번호는 전역**이다. TASK 가 다른 마일스톤으로 옮겨가도 번호는 불변이고 `TASK-0042` 참조가 깨지지 않는다.
- 마일스톤 디렉터리는 착수 시점에 생성한다.
- 검토한 대안: 계층 번호(`TASK-001-1`)는 중간 삽입 시 재번호가 필요해 탈락. 단일 디렉터리는 200개를 넘으면 재편이 필요해 탈락.
- 1000개 규모에서는 markdown 이 아니라 이슈 트래커로 가야 한다는 점을 전제로 한 선택이다.

### D-022. 설계 문서 신설 (`docs/design/`)
TASK 와 무관하게 **항상 최신 상태를 유지하는 설계 문서**를 둔다. 운영 규칙은 `DECISIONS.md` 와 동일하게 현재 상태만 담는다.

| 파일 | 내용 |
| --- | --- |
| `pages.md` | 앱별 페이지 목록, 라우팅, 밀도 3단계 |
| `erd.md` | 데이터 모델과 관계, 설계 근거 |
| `state-machines.md` | 주문·재고·결제·클레임·정산·데모 상태 전이 |
| `pricing.md` | 금액 계산 순서와 환불 안분 규칙 |

구현 시작 후 DB 의 단일 출처는 `schema.prisma` 이고, `erd.md` 의 역할은 관계도와 설계 근거로 좁아진다. 컬럼 타입을 두 곳에 적으면 반드시 어긋나기 때문이다.

### D-023. 주문 구조 — 2단 분할
한 주문에 여러 판매자 상품이 섞이는 것을 허용하고, 주문을 2단으로 나눈다.

```
Order (결제 1건) → SellerOrder (판매자별, 배송·취소·정산 단위) → Shipment
```

- 결제는 주문 단위 1건, 배송·취소·환불·정산은 SellerOrder 단위
- 상태는 `SellerOrder` 에 붙는다. 판매자 A는 배송완료인데 B는 준비중일 수 있어야 한다
- 대가: 결제 1건에 부분 취소가 여러 번 발생한다. 토스 부분취소 API 와 누적 취소액 추적이 필요하다

### D-024. 상품 모델 — 3단 구조
`Product` + `ProductOption` + `ProductVariant(SKU)`.

- 재고·가격은 **Variant** 단위. 블랙 M만 품절되는 게 정상이다
- 옵션 정의 테이블을 따로 두는 이유는 화면 때문이다. 없으면 옵션 선택 UI 를 그릴 때 모든 Variant 를 훑어 값을 역산해야 하고 표시 순서를 저장할 곳이 없다
- 가격은 Variant 절대값 + Product 에 목록용 최저가 캐시. "기본가 + 추가금"은 할인 계산 시 적용 대상이 모호해진다
- 옵션 없는 상품도 Variant 1개를 강제 생성해 예외 분기를 없앤다

### D-025. 상품 속성 — 정의는 테이블, 값은 JSONB
`AttributeDefinition`(테이블) + `Product.attributes`(JSONB).

- 정의가 테이블이어야 상품 등록 폼과 검색 필터 UI 를 정의로부터 자동 생성할 수 있다
- 값까지 테이블로 빼면(순수 EAV) 상품 목록 20개 조회에 조인이 폭증한다
- 필터링·패싯은 Meilisearch 가 담당하므로 DB 는 값을 통째로 읽고 쓰기만 하면 된다
- **대가**: DB 가 타입·범위를 강제하지 못한다. 저장 전 정의를 읽어 zod 스키마를 동적 생성해 검증하는 코드가 필수다
- 무결성을 포기하는 것이 아니라 **강제 주체를 DB 에서 애플리케이션으로 옮기는** 선택이다. "무엇이 올바른 값인가"는 정의 테이블에 남는다

### D-026. 재고 — 예약 후 확정
주문서 진입 시 15분 TTL 로 재고를 예약하고, 결제 승인 시 확정한다.

- 가용재고 = `stock` − 유효한 HELD 예약 합계
- 동시성은 `UPDATE ... WHERE stock - reserved >= qty` 조건부 갱신으로 막는다
- `StockLedger` 원장에 입고·판매·취소·반품입고·조정을 전부 기록한다
- **만료 스케줄러가 멈추면 재고가 잠긴다.** 헬스체크 대상에 포함한다

### D-027. 취소 · 반품 범위
- **취소(배송 전) + 반품(배송 후)** 을 구현하고, **부분 취소·부분 반품**을 지원한다
- **교환은 구현하지 않는다.** 회수+재배송+재고 2회 이동+차액 정산이 엮여 상태 머신이 두 배가 되고, 얻는 것에 비해 비용이 크다. 반품 후 재구매로 안내한다
- **구매확정** 개념을 둔다(수동 또는 D+7 자동). 배송완료 직후 정산하면 반품 시 회수할 수 없기 때문이며, 정산과 적립금 지급의 트리거가 된다

### D-028. 앱 3개 분리 + 앱별 독립 세션
`apps/shop` / `apps/seller` / `apps/admin` 을 독립된 Next.js 앱으로 분리한다. (D-009 의 `apps/web` 단일 구성을 **대체**)

- 커스텀 도메인을 구매해 `shop.` / `seller.` / `admin.` 서브도메인에 배포한다
- **세션은 앱별로 독립**이다. 쿠키에 `Domain` 을 지정하지 않아 서브도메인 간 공유되지 않는다
- 데모 계정은 각 앱에서 따로 발급한다
- 부수 효과(이점): 세 역할을 탭 3개로 동시에 열 수 있다. 판매자가 발송 처리하면 구매자 탭에서 즉시 확인되어 데모 시연 동선이 오히려 좋아진다
- 공통 UI 는 `packages/ui` 로 공유한다

### D-029. 할인 — 쿠폰 + 적립금
- 상품 할인(정가↔판매가) + 쿠폰(플랫폼·판매자) + 적립금
- **부담 주체를 반드시 기록한다.** 플랫폼 쿠폰·적립금은 플랫폼 부담, 판매자 쿠폰만 정산에서 차감한다. 이 구분이 없으면 정산이 단순 곱셈이 되어 도메인으로서 의미가 없다
- 적립은 **구매확정 시** 지급하고, 환불 시 **현금이 아니라 적립금으로** 복구한다
- 계산 순서와 안분 규칙은 `docs/design/pricing.md` 가 단일 출처다

### D-030. 회원 부가 기능 범위
리뷰·평점 / 위시리스트 / 최근 본 상품 / 알림 / 상품 Q&A / 판매자 팔로우 **전부 포함**.

- 리뷰는 `orderItemId` unique 로 **구매 검증을 스키마가 강제**한다. 구매하지 않은 사람은 행을 만들 수 없고 중복 리뷰도 DB 가 막는다
- 알림은 앱 내 알림센터만. 이메일·푸시는 범위 외

### D-031. 결제 수단 — 가상 카드 + 토스 병행
`PaymentProvider { authorize, capture, cancel, refund }` 인터페이스 뒤에 두 구현을 둔다.

- **가상 카드**(자체 구현): 발급 → 한도·잔액 차감 → 즉시 승인·거절. 데모 계정 발급 시 1장 자동 지급
- **토스페이먼츠**(테스트 연동): 실제 결제창 흐름
- 가상 카드를 넣는 이유:
  1. 방문자가 토스 테스트 카드번호를 찾을 필요 없이 바로 결제된다
  2. **한도 초과·잔액 부족·승인 거절·지연을 의도적으로 재현**할 수 있다. 결제 실패 시 재고 예약이 제대로 해제되는지 시연 가능하다
  3. 프로바이더가 둘이어야 추상화가 장식이 아닌 실제 설계가 된다
- 토스 테스트 환경은 정해진 테스트 카드번호만 받으므로, 가상 카드는 토스를 거치지 않는 별도 경로다

### D-032. 정산 범위
주간 정산서 생성 → 관리자 승인 → 지급완료(가상)까지 구현한다.

```
지급액 = 판매액 − 플랫폼 수수료 − 판매자 부담 쿠폰 − 반품 차감
```

- 수수료는 카테고리 기본율 + 판매자 개별율(우선)
- 실제 이체는 없다. 상태만 바뀐다
- 쿠폰 부담 주체·적립금·반품·구매확정이 여기서 전부 만난다. 도메인 설계를 보여주는 최종 지점이다

### D-033. 표시 밀도 3단계 적용 범위
`apps/shop` **전체**에 적용한다. 판매자·관리자 콘솔은 제외한다 — 관리 화면은 정보 밀도가 높은 것이 항상 정답이다.

- 데이터는 항상 전부 내려주고 **표시 여부와 레이아웃만** 바꾼다
- 간격·폰트 스케일·radius·그리드 열 수를 **CSS 변수 3세트(디자인 토큰)** 로 정의하고 모든 컴포넌트가 토큰만 참조한다
- 따라서 **디자인 토큰 시스템이 M03(초반)에 있어야 한다.** 나중에 확대하려면 만든 컴포넌트를 전부 고쳐야 한다
- 설정 저장: 로그인 사용자는 `UserPreference`, 비로그인은 localStorage

### D-034. 이미지 스토리지 — Cloudflare R2
- 전송량 요금 0원. 이미지가 많은 커머스에서 결정적이다
- S3 호환 API 라 AWS SDK 를 그대로 쓰고 이전도 쉽다
- presigned URL 로 브라우저에서 직접 업로드해 서버가 파일을 거치지 않는다
- 탈락: S3(전송량 과금), Vercel Blob(무료 한도 작고 벤더 종속)

### D-035. 데모 시드 데이터 규모
판매자 15 / 상품 800 / 카테고리 40(3단계) / 리뷰 3000 / 주문 500.

- 검색에 페이지네이션이 생기고 패싯 분포가 자연스러워지는 최소 규모다. 100개 규모면 필터를 걸었을 때 결과가 0이 되어 Meilisearch 가 아무것도 보여주지 못한다
- 이미지는 Unsplash·Pexels 무료 라이선스 사진을 카테고리별로 확보해 재사용한다
- **실제 브랜드명은 사용하지 않는다.** 상표권 문제이며 공개 저장소이므로 가상 브랜드명을 생성한다

### D-036. 마일스톤 구성 (M01 ~ M15)
`DECISIONS.md` 12장 참조.

- **M02 를 배포 파이프라인으로 앞당긴다.** 뼈대만 있는 상태에서 배포를 먼저 뚫어두면 이후 모든 마일스톤을 배포된 상태로 검증할 수 있고, 환경변수·CORS·쿠키 도메인 같은 배포 이슈를 원인 범위가 좁을 때 만난다
- 금액 계산은 M07 에서 "할인 목록을 받아 계산하는" 구조로 먼저 만들고 M11 에서 쿠폰·적립금 규칙을 꽂는다. 주문 로직을 두 번 만들지 않기 위해서다

### D-037. TASK 문서 전량 선작성 + 설계 변경 시 중단 규칙
M01~M15 의 TASK 문서 **100개를 지금 전부 작성**한다. (D-021 의 "착수 시점 생성" 방침을 **대체**)

검토한 대안과 판단:

- 처음 제안한 "마일스톤 완료 후 다음 것 작성"의 근거로 든 예시(후속 TASK 에서 Prisma 버전·인증 미들웨어 형태를 모른다)는 **틀렸다.** 버전은 앞선 마일스톤에서 확정되면 참조만 하면 되고, 인증 구조는 D-016·D-028 로 이미 정해져 있어 지금 기술할 수 있다.
- 실제로 지금 모르는 것은 **작업 분해 크기 추정의 정확도** 하나다. M01 을 실제로 수행해봐야 "반나절" 추정이 맞는지 알 수 있고, 어긋나면 이후 분할 단위가 조정된다.
- 남는 비용은 검토 부담(한 번에 100개)과 변경 전파(설계가 바뀌면 관련 TASK 를 함께 수정)뿐이며, 둘 다 "지금 쓰지 말자"의 근거는 되지 못한다.

**핵심 운영 규칙**:

```
구현 중 설계 변경이 필요한 문제 발견
  → 해당 작업 중단
  → 설계 문서(docs/design/, DECISIONS.md) 수정
  → 영향받는 TASK 문서 수정
  → 사용자 승인
  → 재개
```

코드를 먼저 고치고 문서를 나중에 맞추지 않는다. 문서가 실제와 어긋나기 시작하면 이 체계 전체가 무의미해진다.

### D-038. 공통 품질 게이트 분리
TASK 100개가 동일한 품질 게이트 표를 반복하면 100번 중복된다. `docs/tasks/QUALITY-GATES.md` 로 분리하고 각 TASK 는 이를 참조한 뒤 **예외만** 명시한다.

게이트 구성: 코드(Q1~Q7) / 화면(P1~P6) / API(A1~A5) / 데이터(S1~S4) / 문서(D1~D5).
커버리지 게이트(Q5, 80%)는 **M05 부터 적용**한다. 그 전 마일스톤은 스캐폴딩이라 의미 있는 테스트 대상이 없다.

### D-039. 워크트리별 포트 격리
`PORT_OFFSET` 환경변수 하나로 전 포트를 일괄 이동하고, `COMPOSE_PROJECT_NAME` 으로 Docker 컨테이너·볼륨도 분리한다.

- 포트를 하드코딩하면 두 번째 워크트리에서 `pnpm dev` 가 실패한다. D-007 의 워크트리 병행 작업과 충돌한다
- `COMPOSE_PROJECT_NAME` 을 분리하지 않으면 두 워크트리가 **같은 DB 컨테이너를 공유**해 마이그레이션이 서로를 덮어쓴다
- 워크트리별 `.env.local`(gitignore)에 오프셋만 지정한다

### D-040. 동시 변경 제어 방식 — 데이터 성격별로 분리
| 대상 | 방식 | 이유 |
| --- | --- | --- |
| 사람이 편집하는 데이터 (카테고리·상품 정보) | **낙관적 잠금**(`version`) | 충돌을 사용자에게 알리고 선택하게 한다. last-write-wins 는 남의 작업을 조용히 지운다 |
| 수량·잔액 (재고·적립금·가상카드) | **행 잠금으로 직렬화** | 알릴 대상이 없다. 순서대로 처리하고 부족한 요청만 실패시킨다 |
| 트리 구조 이동 (카테고리) | **어드바이저리 락** | 경로 재계산이 겹치면 트리가 깨진다 |

반영 시점도 조작에 따라 나눈다 — 트리 이동·정렬은 **즉시 반영**(실패 시 원위치 복구), 이름·설명 편집은 **폼 + 저장 버튼**.

### D-041. id 는 재사용하지 않는다
카테고리를 포함한 모든 엔티티에서 삭제된 id 를 재사용하지 않는다. 결번은 그대로 둔다.

재사용하면 과거 로그·주문 스냅샷·검색 인덱스에 남은 id 가 **엉뚱한 대상을 가리킨다.** 데이터가 조용히 잘못되는 종류의 문제라 추적이 어렵다. 표시 순서는 `sortOrder` 라는 별개 값으로 관리하므로 결번이 있어도 무관하다.

### D-042. 1회 주문 최대 구매 수량 제한
판매자가 `Product` / `ProductVariant` 에 `maxPurchaseQuantity` 를 설정할 수 있다. (Variant 값이 상품 값보다 우선, null 이면 무제한)

- 한 사람이 전 재고를 쓸어가는 것을 막는다. 데모에서도 재고 1개짜리를 한 명이 독점하면 다른 방문자가 체험할 수 없다
- 검증은 **장바구니·주문서·재고예약·주문생성 네 곳 전부**에서 한다. 하나라도 빠지면 우회 경로가 된다
- 1인당 **누적** 구매 제한은 범위 밖이다. 재구매·반품 후 재주문까지 고려하면 규칙이 복잡해진다

### D-043. 데모 계정 초기 데이터 구체화
빈 계정을 주면 방문자가 주문내역·정산·통계에서 전부 "데이터 없음"을 본다. 기능이 있어도 전달되지 않는다.

| 역할 | 초기 데이터 |
| --- | --- |
| 구매자 | 가상카드 1장, 배송지 1개, 쿠폰 3장, 적립금 잔액, **주문 3건(서로 다른 상태)**, 위시리스트·최근본 |
| 판매자 | ACTIVE 스토어, **상품 12개(공용 카탈로그에서 복제)**, **주문 5건**, 정산 2회차, 리뷰 4건 |
| 관리자 | 별도 생성 없음 (전체 조회라 이미 채워져 있다) |

데모 계정은 이 데이터를 **자유롭게 수정·삭제하고 새로 만들 수 있다.** 직접 만들어보는 것이 데모의 목적이다. 관리자 데모는 생성은 되지만 남이 만든 것은 건드릴 수 없다(D-024 Q-11 규칙).

### D-044. Cloudflare DNS + Vercel — 프록시 비활성
도메인은 Cloudflare 에서 사고 DNS 를 관리하되, **Vercel·API 로 향하는 레코드는 프록시를 끈다(DNS only).**

프록시를 켜면 Cloudflare CDN 과 Vercel CDN 이 이중으로 걸려 캐시 무효화가 어긋나고, 도메인 검증·SSL 발급 실패나 리다이렉트 루프가 생긴다. `cdn`(R2) 레코드만 프록시를 쓴다.

배포까지 Cloudflare(Pages/Workers)로 옮기는 선택지는 채택하지 않는다. Next.js App Router 의 SSR 은 Vercel 이 네이티브라 제약이 없다.

### D-045. 콜드 스타트 대응
무료·저가 플랜 백엔드는 무활동 시 잠든다(Render 무료 15분, 재기동 30초~1분). 첫 방문자가 빈 화면을 보지 않도록 대응한다. (TASK-0101)

- 진입 시 비차단 **프리워밍**(`/health` 백그라운드 호출)
- 3초 이상 지연되면 "서버를 준비하는 중입니다" 안내
- 깨어남 감지 후 자동 재요청
- LCP 게이트는 콜드 스타트 상황에서 제외하고 워밍 후 측정한다

### D-046. SEO 전략
`apps/shop` 만 색인 대상이고 판매자·관리자 앱은 전체 noindex 다. (TASK-0102)

- 상품 상세는 **ISR**, 카테고리는 SSR, 홈은 SSG + 재검증
- **검색 결과와 필터 적용 목록은 noindex** — 속성 필터가 자동 생성되어 URL 조합이 수백 개가 되고, 전부 색인되면 중복 콘텐츠가 된다. canonical 을 기본 카테고리 URL 로 모은다
- 구조화 데이터(Product·BreadcrumbList·Organization), 동적 OG 이미지, sitemap 자동 생성

### D-047. 한글 자모 · 초성 자동완성
Meilisearch 는 접두어 검색과 오타 보정을 제공하지만 **한글 조합 중 상태(`코ㅌ`)나 초성 검색(`ㅋㅌ`)은 지원하지 않는다.** D-013 에서 언급한 한국어 한계의 구체적 사례다.

색인에 보조 필드(`nameJamo` 자모 분해, `nameChosung` 초성)를 추가하고 검색어도 같은 방식으로 변환해 매칭한다. 가중치는 **완성형 > 자모 > 초성** 순이다. (TASK-0103)

### D-048. 검색 인덱싱 반영 목표 2초
인덱싱 워커 폴링 주기를 1초로 두고 상품 변경이 **2초 내** 검색에 반영되게 한다. (기존 5초에서 단축)

### D-049. CI/CD 실패 알림 — Discord webhook
GitHub Actions 실패·배포 실패·스모크 테스트 실패를 Discord webhook 으로 즉시 받는다. 무료이고 모바일 푸시가 바로 온다.

**성공은 알리지 않는다.** 성공까지 알리면 알림 피로로 결국 전부 무시하게 된다.

### D-050. Storybook 도입 (M03)
`packages/ui` 컴포넌트를 Storybook 으로 문서화한다. (TASK-0104, Q-13 해소)

- 이 프로젝트는 **같은 컴포넌트가 밀도 3단계로 달라지는 것**이 특징이다. 툴바에서 밀도를 전환하며 조합을 확인하는 데는 앱 내 프리뷰 페이지보다 Storybook 이 확실히 낫다
- 배포하면 포트폴리오 자산이 된다
- 비용: 배포 대상 하나 추가, 스토리 유지 부담 → **새 컴포넌트를 만들 때 스토리를 함께 작성**하는 것을 M03 이후 컴포넌트 TASK 의 완료 기준에 넣는다
- 시각적 회귀 테스트(Chromatic)는 도입하지 않는다

### D-051. 다크모드는 M15 이후로 미룬다
지금은 구조만 갖추고 실제 지원은 나중에 한다. (Q-14 해소)

- TASK-0014 의 **"색 하드코딩 금지"(F5)** 규칙이 지켜지면, 나중에 다크 색 토큰 세트 하나를 추가하는 일로 끝난다
- 지금 넣으면 검증 조합이 **밀도 3 × 테마 2 = 6배**로 늘어 모든 화면 TASK 의 부담이 커진다
- 다크모드를 나중에 추가하는 비용이 낮으려면 하드코딩 금지가 실제로 지켜져야 한다. 이 규칙을 완화하지 않는다

### D-052. 텔레메트리 — Vercel Analytics 만 도입
에러 추적(Sentry)은 이미 M02 에 있고, 여기에 **Vercel Analytics** 만 추가한다. (TASK-0013, Q-15 해소)

- 설정 한 줄, 무료. 페이지뷰와 코어 웹 바이탈을 **실사용자 기준**으로 측정할 수 있어 성능 TASK(0097)의 근거 자료가 된다
- **PostHog 등 제품 분석 도구는 도입하지 않는다.** 데모 사이트라 트래픽이 없어 이벤트·퍼널 데이터가 쌓이지 않는다. "사용자 행동을 측정해 개선했다"는 스토리를 만들 데이터가 없는데 도구만 붙이는 건 장식이다

### D-053. 시드 이미지 — 2계층 + AI 생성
무료 스톡만으로는 상품 페이지가 채워지지 않는다. 스톡은 낱장이라 **같은 상품의 다른 각도**와 **색상 옵션별 컷**을 만들 수 없고, 톤도 제각각이라 목록에서 커머스처럼 보이지 않는다. (Q-16 해소)

800개 전부가 필요하지는 않다는 점을 이용해 계층을 나눈다.

| 계층 | 수량 | 이미지 |
| --- | --- | --- |
| **쇼케이스** | 20개 | **AI 생성** 4~5장 (각도·색상 옵션·디테일) — 상세 페이지 시연용 |
| 일반 | 780개 | 스톡 1~2장, 카테고리별 풀에서 재사용 — 목록·검색·필터용 |

- 800개 × 5장을 생성하면 비용·시간이 과하다. 상세의 완성도는 20개로 전달된다
- AI 생성의 한계(봉제·소재 디테일이 뭉개짐)를 감안해 **확대 컷을 피하고 전체 실루엣 위주**로 구성한다
- **생성 이미지임을 README 에 밝힌다.** 감추면 신뢰를 잃고, 밝히면 데이터 조달까지 고려한 판단으로 읽힌다
- **실제 브랜드명은 여전히 사용하지 않는다.** 상표권이며 공개 저장소·공개 URL 기준으로 "데모용"은 방어가 되지 않는다 (D-035 유지)

### D-054. 반응형 전략
지금까지 문서에 있던 것은 **검증 기준**(P3 게이트: 360/768/1440px 깨짐 0건)뿐이었고 전략이 없었다. `docs/design/pages.md` 에 정리한다.

**브레이크포인트**: Tailwind 기본값(sm 640 / md 768 / lg 1024 / xl 1280 / 2xl 1536). 검증은 360 / 768 / 1440 세 지점.
`apps/shop` 은 **모바일 퍼스트**, 콘솔 둘은 **데스크톱 퍼스트**.

**밀도 × 뷰포트가 이 프로젝트 고유의 문제다.** 기존 TASK-0014 토큰 설계가 `[data-density="3"] { --grid-cols: 6 }` 처럼 뷰포트를 무시하고 있었다. 360px 에서 6열이면 카드 하나가 55px 라 성립하지 않는다.

열 수와 배율은 **2차원 매트릭스**로 바꾼다.

| | ~639 | 768 | 1280+ |
| --- | --- | --- | --- |
| 미니멀 | 1 | 2 | 3 |
| 표준 | 2 | 3 | 4 |
| 맥시멀 | 2 | 4 | 6 |

**표시 항목(브랜드·평점·판매량 등)은 밀도 1차원으로 유지한다.** 여기까지 2차원이 되면 조합이 9개로 폭발한다. 모바일에서 열 수를 줄이면 카드가 커지므로 맥시멀 정보도 들어갈 자리가 생긴다. 카드 내부는 **컨테이너 쿼리**로 자기 폭에 반응하게 해 미디어 쿼리 나열을 피한다.

**터치 타깃 하한**: `--touch-min: 44px` 고정값. 밀도 배율이 줄어도 인터랙티브 요소는 44×44px 를 유지한다. 시각적으로 작아 보이더라도 패딩으로 터치 영역을 확보한다.

**콘솔 모바일 대응 범위**: `apps/seller` 의 **주문 목록·주문 상세·발송 처리 3화면만** 전용 레이아웃(카드 변환)으로 제대로 만들고, seller 나머지와 admin 전체는 가로 스크롤로 두되 깨지지만 않게 한다.

- 판매자가 폰으로 주문을 확인하고 발송하는 것은 실제 패턴이다(쿠팡 윙 앱)
- 관리자 대시보드를 폰으로 보는 경우는 드물고, 관리 화면 30여 개를 전부 카드 변환하는 비용은 얻는 것에 비해 크다
- 가로 스크롤 표는 **첫 열을 고정**해 무엇에 대한 행인지 잃지 않게 한다

### D-055. 모바일은 반응형 + 조건부 컴포넌트 (별도 페이지 없음)
`m.도메인` 이나 `/m/*` 같은 **별도 모바일 경로를 만들지 않는다.** URL 과 코드베이스는 한 벌이다.

- canonical / alternate 를 쌍방 관리해야 하고 하나만 틀려도 색인이 분산된다. Google 은 모바일 우선 색인 기준으로 반응형을 권장한다
- 앱이 이미 3개(shop/seller/admin)인데 모바일까지 나누면 최대 6개가 된다
- **밀도 3단계 × 모바일/데스크톱 = 6벌** 유지는 이 프로젝트에서 특히 나쁘다
- 무신사·29CM·SSENSE 는 반응형이다. 쿠팡의 `m.` 도메인은 레거시 + 앱 전환 유도 목적이며 포트폴리오에서는 오히려 낡은 선택으로 읽힌다

**다만 반응형이 모든 것을 CSS 로만 늘였다 줄이는 것을 뜻하지는 않는다.** 모바일에만 존재하는 UI 패턴은 **같은 라우트 안에서 컴포넌트를 갈아 끼워** 처리한다.

| 화면 | 모바일 | 데스크톱 |
| --- | --- | --- |
| 상품 상세 | 하단 고정 구매 바 | 우측 패널 버튼 |
| 필터 | 바텀시트 | 좌측 사이드 패널 |
| 이미지 갤러리 | 스와이프 | 썸네일 + 확대 |
| 헤더 | 햄버거 | 메가메뉴 |
| seller 주문 목록 | 카드 | 표 |

**두 컴포넌트를 모두 렌더하고 CSS 로 숨기지 않는다.** DOM 이 두 배가 되고 접근성 트리도 중복된다. 뷰포트 훅으로 하나만 마운트한다.

(D-054 의 "전용 레이아웃"이라는 표현이 별도 페이지로 오해될 수 있어 여기서 명확히 한다)

## 논의 중 / 미확정

| # | 항목 | 상태 |
| --- | --- | --- |
| Q-09 | UI 컴포넌트 라이브러리 (자체 구현 vs shadcn/ui) | M03 에서 결정 |
| Q-10 | 테스트·CI 범위 (E2E 도입, 커버리지 게이트) | M05 진입 전 결정 |
| Q-11 | 관리자 데모 계정의 권한 제한 범위 | M04 에서 결정 |
| Q-12 | 구매할 도메인 이름 | M02 에서 결정 |


## 해소된 질문

| # | 항목 | 결론 |
| --- | --- | --- |
| Q-01 | 기술 스택 | D-009 |
| Q-02 | 원격 저장소 및 배포 | D-012 |
| Q-03 | 결제·배송 범위 | D-010 |
| Q-04 | 상품 3단계 뷰의 주체 | D-011 |
| Q-05 | 다국어·다통화 | D-014 |
| Q-06 | 데모 계정 정책 | D-015 |
| Q-07 | GitHub 저장소 및 인증 | D-017 |
| Q-08 | 상품 이미지 스토리지 | D-034 |
| Q-13 | Storybook 도입 | D-050 |
| Q-14 | 다크모드 시점 | D-051 |
| Q-15 | 텔레메트리 | D-052 |
| Q-16 | 시드 이미지 조달 | D-053 |

## 변경 이력

| 날짜 | 내용 |
| --- | --- |
| 2026-09-02 | 최초 작성 (D-001 ~ D-008) |
| 2026-09-02 | 기술 스택·배포·검색·데모 정책 확정 (D-009 ~ D-015) |
| 2026-09-02 | Google 로그인, 원격 저장소 연결, 문서 규칙 보강 (D-016 ~ D-019) |
| 2026-09-02 | 도메인 설계 인터뷰 — TASK 분할·문서 체계·주문/상품/재고/결제/할인/정산 구조 확정 (D-020 ~ D-036) |
| 2026-09-02 | TASK 문서 100개 전량 선작성, 공통 품질 게이트 분리 (D-037 ~ D-038) |
| 2026-09-02 | 문서 검토 피드백 반영 — 포트 격리, 동시성 제어, 구매 수량 제한, 데모 초기 데이터, 콜드 스타트, SEO, 한글 자동완성 (D-039 ~ D-049) |
| 2026-09-02 | Storybook 도입, 다크모드 보류, Vercel Analytics 채택 (D-050 ~ D-052) |
| 2026-09-02 | 시드 이미지 2계층 전략 + AI 생성 확정 (D-053) |
| 2026-09-02 | 반응형 전략 수립 — 밀도 × 뷰포트 매트릭스, 터치 타깃 하한, 콘솔 모바일 범위 (D-054) |
| 2026-09-02 | 모바일은 반응형 + 조건부 컴포넌트로 확정, 별도 페이지 없음 (D-055) |
