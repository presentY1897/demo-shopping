# 인계 — 다음 세션이 여기서 이어받는다

> **지금 무엇이 열려 있는지**만 담는다. 결정은 `docs/decisions/`, 작업의 설계·검증은 TASK 문서,
> 진척은 `docs/tasks/README.md`, 사용자가 할 일은 [`OWNER-CHECKLIST.md`](./OWNER-CHECKLIST.md) 가
> 맡는다. **항목이 닫히면 여기서 지운다** — 이력을 쌓는 곳이 아니다.

마지막 갱신: 2026-09-04

## 1. 바로 착수할 수 있는 것

`docs/tasks/README.md` 에서 상태가 `승인됨` 이고 **선행이 모두 `완료`** 인 것을 고른다.

**선행은 TASK 문서 머리표의 `선행 작업` 줄이 단일 출처다. 거기 적힌 것을 하나도 빠짐없이 본다.**
이 규칙을 두 가지 방식으로 어겼고, 둘 다 실제로 틀린 후보를 올렸다.

> - **마일스톤 단위 선행**(`M06 완료`)이면 그 마일스톤이 통째로 끝나야 한다. TASK 번호만 훑으면
>   안 된다 — 이 실수로 TASK-0113 을 착수 후보로 올린 적이 있다.
> - **선행이 여러 개면 전부 본다.** TASK-0037 의 선행은 `TASK-0113, TASK-0036` 인데 0036 만 보고
>   "바로 착수 가능" 으로 올린 적이 있다(2026-09-04 에 정정). 하나만 미완이어도 막힌다.

2026-09-04 기준:

| TASK | 범위 | 비고 |
| --- | --- | --- |
| TASK-0012 배포 자동화 · 환경변수 관리 | CI/CD | 선행 0010 완료. **Discord webhook URL 이 있어야 끝난다** — 4장이 채택한 실패 알림 경로이고 R6 이 "1분 내 알림" 이다. [`OWNER-CHECKLIST.md`](./OWNER-CHECKLIST.md) A-6 |
| TASK-0021 Google OAuth 로그인 | `apps/api/auth` + 세 앱 | 선행 0020 완료. 외부 의존 없음. **인증이 붙으면 TASK-0013(Sentry) 시점이 온다** |

**둘은 파일이 겹치지 않아 동시에 돌릴 수 있다.** 0012 는 `.github/workflows` · `render.yaml` · 배포
문서, 0021 은 `apps/api/src/auth` · `packages/shared` 다.

**M04·M05 는 인증이 뿌리다.** 지금 착수 가능한 것이 둘뿐인 이유이기도 하다.

```
0021 ─→ 0022 ─┬─→ 0023 ─→ 0024 ─→ 0025
              ├─→ 0108 ─→ 0113 ─→ 0114 · 0115 · 0037
              └─→ 0111 ─→ 0112
```

**0022(JWT)가 병목이다.** 그것만 끝나면 `0023 ∥ 0108 ∥ 0111` 셋이 동시에 열리고, 그 뒤로 M04 화면
셋(0109 · 0110 · 0112)과 M05 의 0113 이 갈라진다. 2절의 이월 항목 대부분이 그 경로에서 회수된다.

## 2. 이월된 항목 — 주인이 정해진 것

닫힌 TASK 가 `[-]` 로 넘긴 것들이다. 받는 TASK 를 할 때 **함께 회수한다.**

**받는 TASK 문서에 적혀 있어야 이 표를 지울 수 있다.** 이 표에만 있고 받는 문서에는 없으면, 이 파일이
정리되는 순간 항목이 사라진다 — 실제로 아래 세 건이 그 상태였다(2026-09-04 에 회수). `적힘` 칸이 그
확인란이다.

| 무엇 | 어디서 넘어왔나 | 받는 곳 | 적힘 |
| --- | --- | --- | --- |
| 기본 **SKU 접두사**가 약 65초마다만 바뀐다 | TASK-0036 7.5 | TASK-0113 | ✅ 범위 · 4장 · F12 |
| 속성 삭제 시 **사용 상품 수** 안내 (`params`) | TASK-0031 F5 | TASK-0113 (+ `apps/admin` 소유 TASK) | ⚠️ 범위 · R6 — **받는 곳 절반이 미정**, 아래 3.1 |
| 카탈로그 **도메인 오류 코드** | TASK-0032 4.10 · TASK-0036 4.10 | TASK-0113 · 0114 | ✅ 0113 범위 · F13 / 0114 범위 |
| 업로드 이미지의 **수명 관리** (고아 객체 정리) | TASK-0033 F6 | TASK-0113 | ✅ 범위 |
| 이미지 **순서 · 대표** 확인 화면 | TASK-0033 F3 · F4 | TASK-0043 · 0116 | ❓ 미확인 |
| **재고 이력 화면** | TASK-0036 | TASK-0116 | ❓ 미확인 |
| 앱 셸 **번들 크기** (LCP 실측 누적) | TASK-0018 · 0029 · 0031 | TASK-0097 | ❓ 미확인 |
| Lighthouse **자동화** | TASK-0104 | TASK-0098 | ❓ 미확인 |

`❓` 는 받는 문서에 적혀 있는지 아직 확인하지 않았다는 뜻이다. 해당 TASK 에 착수할 때 먼저 본다.

## 3. 주인이 정해지지 않은 것 — 판단이 필요하다

> 착수 전에 어느 TASK 가 받을지 정한다. 정하고 나면 2절로 옮긴다.

### 3.1 속성 삭제 안내의 `apps/admin` 쪽 절반

TASK-0031 F5 가 넘긴 "사용 상품 수" 는 **차단은 되고 수만 못 싣는** 상태다. 채우려면 오류 `params`
계약과 그것을 그리는 화면이 둘 다 필요한데, **둘의 소유자가 갈린다.**

| 조각 | 주인 |
| --- | --- |
| `packages/shared` 의 오류 `params` 계약 | **TASK-0113** (그 문서 R6 에 적었다) |
| 속성 삭제 엔드포인트가 수를 싣기 · `apps/admin` 카탈로그 문장 | **미정** — 속성 정의 API 와 관리자 화면은 TASK-0030 · 0031 이 닫고 갔다 |

**M14(TASK-0093~0095)가 `apps/admin` 을 다시 여는 첫 지점**이라 후보이긴 하나, 그때까지 미뤄 둘 일인지
아니면 TASK-0113 착수 시 작은 후속으로 붙일지는 정해지지 않았다. 정하고 나면 2절로 옮긴다.

### 3.2 `api-failure.ts` 가 두 벌이고, 이미 갈라졌다

`apps/seller/src/lib/api-failure.ts` 와 `apps/admin/src/lib/api-failure.ts` 다. TASK-0033 이 합칠 자리로
옮기려면 두 앱을 동시에 고쳐야 해서 미뤘고, 양쪽 파일 머리말에 그 사실을 적어 뒀다.

**임포트 하나만 다른 것이 아니다.** 실제 차이는 셋이고, 세 번째는 화면에 보이는 차이다.

| | seller (112줄) | admin (131줄) |
| --- | --- | --- |
| `ApiConfigurationError` 임포트 | 자기 앱 것 | 자기 앱 것 |
| `hasCode(failure, code)` | **없다** | export 한다 |
| 메시지 `params` 보간 | **안 한다** | `paramsOf(failure.details)` 로 한다 |

갈라진 것은 사고가 아니라 **양쪽에 근거가 적힌 의도된 분기**다 — seller 의 `lib/errors.ts` 머리말이
"No placeholders. `apps/admin` 은 두 메시지에 params 를 보간한다" 고 명시한다.

**그래서 합치는 일의 성격이 다르다.** 파일을 `packages/` 로 옮기는 이동이 아니라, *보간을 할 것인가*
(하면 seller 메시지 카탈로그에 플레이스홀더가 필요하다) 와 *`hasCode` 를 공통 API 로 둘 것인가* 를
정하는 **설계 결정**이다. `세 번째 앱이 필요로 할 때` 라는 발동 조건도 그 전제 위에 있었으므로 다시
본다 — 2절의 도메인 오류 코드 작업(TASK-0113 · 0114)이 양쪽 카탈로그를 함께 건드리므로 **그때가 실제
접점**이다.

## 4. 사용자가 할 일

[`OWNER-CHECKLIST.md`](./OWNER-CHECKLIST.md) 가 단일 출처다.

**하나가 막고 있다 — A-6 Discord webhook.** 없으면 TASK-0012 를 완료로 끝낼 수 없다(1절). 채널에서
webhook 을 만들어 URL 을 GitHub Secrets 에 등록하고 **이름만** 알려주면 된다. 설정 5분.

## 5. 이 저장소에서 실제로 물린 함정

새로 오는 사람이 같은 데 빠지지 않도록. **전부 실제로 겪은 것이다.**

| 함정 | 어디에 적혀 있나 |
| --- | --- |
| CI 가 트리거조차 안 되는데 `gh pr checks` 는 초록으로 보인다 (3회) | `CLAUDE.md` 2장 |
| 앵커 없는 `.gitignore` 패턴이 소스를 조용히 가린다 — **커밋도 테스트도 성공한다** | `.gitignore` 주석 |
| Lighthouse 가 실행 디렉터리에 `C:\Users\...` 프로필을 만든다 (1,264개 파일을 커밋할 뻔했고, 그 뒤 `main` 에도 한 번 더 남았다) | TASK-0097 1장 |
| `git worktree add` 를 **다른 워크트리 안에서** 돌리면 거기에 중첩 생성된다. 상대 경로가 현재 위치 기준이라 `main` 이 오염된다 | 이 표 |
| rebase 후 `@shopping/shared` 의 `dist` 가 낡아 typecheck 이 깨진다 | `CLAUDE.md` 2장 머지 흐름 |
| 성능 게이트가 화면이 아니라 앱을 재고 있었다 | D-217 |
| **이월 항목이 이 파일에만 있고 받는 TASK 문서에는 없었다** (3건). 이 파일은 닫히면 지우는 곳이라 그대로 뒀으면 사라졌다 | 2절 `적힘` 칸 |
| **플랫폼이 이미 하는 일을 직접 만들었다** — Vercel 은 pnpm 모노레포의 미영향 프로젝트를 자동으로 건너뛴다. "4개가 다 빌드된다" 를 헛빌드로 단정하고 스크립트를 만들었는데, 그 PR 은 `packages/shared` 를 바꿨으므로 **넷 다 빌드가 맞았다.** 문서를 먼저 읽었으면 안 만들었다 | TASK-0010 4.1 |
