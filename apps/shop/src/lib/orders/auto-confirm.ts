/**
 * 자동 구매확정에 대해 **화면이 아는 것** (TASK-0064 F8).
 *
 * ## 날짜는 서버가 준다
 *
 * 예정 시각은 `sellerOrder.autoConfirmAt` 으로 온다. 화면이 이력의 배송완료 줄에
 * 이레를 더할 수도 있었고 실제로 처음엔 그렇게 만들었지만, **그 덧셈은 데모에서
 * 틀린다** — 배포가 `FULFILLMENT_PACE=demo` 면 배송도 확정도 시간이 압축되고
 * (배송완료 5분 뒤), 그 설정은 어떤 응답에도 실리지 않아 화면이 알 방법이 없다.
 * 「7일 뒤」라고 적어 놓고 5분 뒤에 확정되면 사람은 화면이 거짓말했다고 읽는다.
 *
 * ## 그래도 이 상수가 여기 있는 이유
 *
 * 화면은 날짜 옆에 **실제 서비스의 규칙**을 함께 적는다. 그 문장은 계산이 아니라
 * 이 서비스의 약속이고, 두 값이 나란히 있어야 압축된 배포에서 사람이 「지금 보는
 * 것이 시연용으로 짧아진 시간」임을 스스로 안다 — 화면이 「지금 압축 중입니다」라고
 * 단언하지 않고도 그렇게 된다. 압축 여부를 화면은 모르고, 모르는 것을 말하지 않는
 * 것이 이 파일의 규칙이다.
 *
 * 그래서 이 값은 서버의 `AUTO_CONFIRM_AFTER_MS` 를 다시 적은 것이 **아니다.**
 * 서버의 그것은 배치가 언제 도는지를 정하고, 이것은 사람에게 말할 규칙의 숫자다.
 * 규칙이 바뀌면 둘 다 바뀌어야 하며, 그때 화면만 옛 숫자를 말하는 것을
 * `test/mypage-auto-confirm.spec.tsx` 가 잡는다.
 */
export const AUTO_CONFIRM_DAYS = 7
