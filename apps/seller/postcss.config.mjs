// Tailwind v4 runs as a single PostCSS plugin; the configuration itself lives
// in CSS (src/app/globals.css and @shopping/config/tailwind/preset.css).
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
