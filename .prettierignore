# Root ignore list. Prettier reads only the `.prettierignore` next to the
# directory it runs from, and the root run (`pnpm format:check`) covers the whole
# repository, so every generated path has to be listed here.
node_modules
dist
build
.next
storybook-static
coverage
next-env.d.ts
pnpm-lock.yaml

# Markdown is written by hand and reviewed as prose. Prettier realigns every
# table cell (its CJK width math disagrees with the source) and reformats the
# fenced code samples, which rewrites documentation instead of formatting it.
*.md

# Vendored from https://render.com/schema/render.yaml.json — kept byte for byte
# so refreshing it produces a readable diff.
apps/api/test/ci/schemas/

# Generated/local image execution artifacts; reviewed JSON is formatted at commit time.
product-images/*-calls/
product-images/*-prompts/
product-images/*-qa/
product-images/*-timing.json
product-images/*-summary.json
product-images/*-products.json
product-images/*-audit.json
product-images/*-plan.json
product-images/*-applied.json
product-images/*-baseline.json
product-images/*-preservation.json
product-images/*-check.json
product-images/*-backup.json
product-images/*-completion.json
product-images/*-intervals.json
product-images/*-pressure*.json
